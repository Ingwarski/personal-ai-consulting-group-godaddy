# Matrix setup preflight follow-up

Observed: 2026-09-06T21:17:00Z. Existing authorized setup scope only; no Published deployment or credential creation in this observation.

- GitHub run `34056704369`, attempt 1, still builds source `ca68054232227788194d2d5e13a8a5beb4724da4`. Its Node job passed. Safari's live job log shows the first optimized release build completed in 35m24s; the second has not yet reported completion. Current native/build inputs are byte-unchanged from that source commit. No release artifact is claimed.
- Published `/healthz` responds 200. GoDaddy's authenticated overview confirmed Preview was inactive; opening its existing Preview woke the stateless application. The browser showed the expected runtime-ready root response. No Preview secrets, database role or code were changed.
- Preliminary absent-file requests: all six Published paths returned 404; Preview's two `/assets/` paths returned 404, while its two `/public/assets/` and two `/.runtime/` paths returned 401 before the GoDaddy login boundary. The existing authenticated Preview browser displayed `{"status":"not_found"}` for the alternate crypto-store path. These absent-file observations do not prove isolation of existing final storage files and do not satisfy the exact-path credential gate. No authentication/share token is recorded here.
- Inspection found a setup projection defect: a failed repeat preparation revoked the internal execution permission but retained the old `prepared` view. Preparation now resets the view before verification. The two fixed HTTP-isolation error codes now survive the content-free error filter instead of becoming an unhelpful generic failure. No isolation requirement was weakened or bypass added.
- Regression checks under Node 22.23.2: focused setup tests 6/6 passed; full `npm run check` passed asset build, typecheck, 747 tests, and forbidden-runtime-environment checks. The Python helper's synthetic tests remain included. `git diff --check` passed. These are local results, not Linux release or live delivery proof.

Next: finish/authenticate the immutable build, verify and pin its artifact, deploy setup-only mode, then establish exact-file HTTP isolation before the user-operated new-device credential step. Preview authentication is an evidence limitation to resolve, not a privacy pass. Existing Matrix sessions, messages and crypto state remain preserved.

## Completed run diagnosis and builder correction

Recorded: 2026-09-06T21:23:56Z, after fetching the completed GitHub job log.

Run `34056704369` failed in the native test stage, not the optimized release builds. Its first build completed at `20:38:34Z` in 35m24s and the second at `21:13:53Z` in 35m17s. The script reached `container("test", builds[0])` only after both binaries passed static/stripped ELF and byte-equality checks. Safari's previously observed search/scroll position had not exposed the second completion line; the completed log supersedes that incomplete progress observation.

The exact failure was `couldn't read src/../../../test/fixtures/matrix-invalid-media.json` at `client.rs:1472` and `client.rs:1556`. The native crate was mounted at `/source`, but its shared Node/Rust test fixture was not mounted at the resolved `/test/fixtures/` path. No artifact was uploaded and no failed output was promoted.

The builder now mounts that exact fixture read-only, includes it in source-input hashes and workflow change triggers, checks source mounts before the expensive build, checks fixture readability inside the container, and runs native tests before the two optimized builds. A regression test resolves both actual Rust `include_str!` references against the declared mounts and checks the shared provenance/trigger wiring. Native application source, production optimization, immutable image, offline compilation, separate target directories and byte-equality requirements are unchanged.

Local verification: release-package tests 11/11, complete Node 22.23.2 `npm run check` 748/748 plus build/typecheck/environment checks, shell syntax, `git diff --check`, and SDD before-implementation 13 artifacts/no issues/no warnings passed. Successful Linux tests and a new release artifact still require the replacement run.

Replacement: [run 34060973791](https://github.com/Ingwarski/personal-ai-consulting-group-godaddy/actions/runs/34060973791), exact source `271bcb8ec973e34fe0e53a0f260991ed1a4a5373`, confirmed in progress after push. The failed run's artifact API reports `total_count: 0`. No failed or missing output is pinned or deployed. No scheduled/background follow-up has been created; the GitHub job itself continues remotely.

Follow-up recorded at 2026-09-06T21:30:12Z after the user's next `go`: the replacement Node job passed; the Rust job remains in progress. The Codex app confirmed creation of the temporary current-task heartbeat `finish-matrix-build-verification`, checking every ten minutes and staying quiet on unchanged build state. It may continue only the already authorized setup after verified build success and must pause on failure or the first required user credential/device-comparison action. No automatic build retries, credential creation or trust confirmations are authorized. This supersedes the earlier absence of scheduled follow-up, not the build/deployment evidence.

## Successful artifact verification and setup-only promotion preparation

The replacement run completed successfully at `2026-09-06T22:52:27Z`. Authenticated API checks confirmed the repository, workflow, exact source, attempt 1 and immutable artifact `9998728398`. `verify-release.mjs --stage` independently checked the 29,286,042-byte archive digest, ten exact archive members, all manifest hashes/sizes, checksum inventory, pinned builder and all 29 source inputs against both the original Git commit and current checkout. Both programs pass the static stripped x86_64 ELF inspection; the verified workflow reports two byte-identical builds and 93 passing Rust tests. Full details and hashes are in `release-verification.json` and the unmodified `.runtime-release/matrix` evidence bundle.

The exact manifest is now pinned in application code. After this pin, Node 22.23.2 `npm run check` passed all 748 tests, typecheck, asset build and forbidden-environment checks. SDD before-implementation passed all 13 documents; full product release gates remain unrun/blocked as previously recorded.

In the target GoDaddy project `wy2v0putg6`, only the Published `MATRIX_SETUP_MODE=provision` flag was added. The dashboard confirmed the secret was saved and required a restart; `Later` was selected so the upcoming code deployment can apply it without an extra restart. Existing secret values were not revealed or replaced; Preview was not synchronized or changed.

This promotion reuses the verified immutable artifact and unchanged native/build inputs. Its Git commit deliberately uses `[skip ci]` to avoid a redundant long native rebuild on the fast-forward to main. No new GitHub CI pass is claimed for the promotion commit; the separate source-bound Rust run and fresh local 748-test result are the evidence. Actual GoDaddy deployment, exact-file HTTP isolation and credentials/device setup remain separate pending observations.

## Published setup deployment and owner-login handoff

Observed on `2026-09-06T23:08Z`–`23:11Z`: commit `466d7268afe886047d64ea5c5fb6984efcc7cf81` was pushed by non-force atomic update to the working branch and main. GoDaddy pulled it into Preview; its live browser root remained the unconfigured runtime shell, with no secret synchronization or stateful Preview configuration. The operator then published the scoped release. The selected Published dashboard now identifies this exact commit and reports OK. Independent live checks changed from the old `/operations/matrix` 404 to an unauthenticated GET 303 owner-login redirect, while `/healthz` remained 200.

Safari and Chrome both displayed the real owner Google sign-in page when navigating to the new Matrix route. The operator stopped before signing in, creating credentials or confirming device trust. The temporary heartbeat was confirmed `PAUSED` at `2026-09-06T23:10:16Z`. The next user-only step is owner sign-in in Safari, then returning to `/operations/matrix`; the operator can resume the credential-free prepare/isolation action afterward. Runtime file installation, exact storage-path isolation, new Matrix credentials/device setup and live consultation delivery are not yet verified. The known anonymous Preview 401 limitation remains open, not relabeled as a privacy pass.

Evidence-byte preservation: the imported immutable `cc.txt` contains a trailing blank line. `git diff --cached --check` reported that whitespace warning; the file was kept byte-for-byte so its authenticated manifest hash remains valid. No clean diff-check result is claimed for that imported file.

## Signed-in Safari and retained owner-action handler repair

After the user confirmed owner sign-in, Safari displayed the authenticated Matrix setup page. Two prepare clicks, separated by a normal reload, failed client-side with the invalid-request-address message; no installer or credential action reached the server. Web Inspector showed the live form's action attribute was `/operations/matrix/action`, but its `form.action` property was the named hidden input. The actually registered submit listener still used `new URL(form.action, location.href)`, although independent live HTTP served the corrected `getAttribute` script. The exact cache layer is not established.

Commit `6856593` makes every owner surface reference the script's SHA-256-addressed URL, preserving the legacy route for old pages. The shared Settings renderer accepts only the constrained trusted asset path; no visual layout or auth control changes. Node 22.23.2 `npm run check` passed 750 tests plus build/typecheck/environment checks, including a simulated obsolete-script cache and real protected prepare dispatch. The new incremental U-04 promotion receipt records actual committed diff bytes and retains historical screenshots as historical evidence only; full visual acceptance remains blocked. SDD before-implementation passed all 13 artifacts with no issues/warnings after reconciliation. Live deployment and successful server-side prepare remain pending at this entry.

## Live repair verified; exact-path evidence remains blocked

Observed by `2026-09-06T23:29:18Z`: GoDaddy pulled and published `ddce36375b99b5af25be7f387c4d98fa223cad47`. The selected Published tab identifies this commit and both site/infrastructure OK, despite the lagging overview build label. Live sign-in HTML references `/assets/owner-auth.455d29bbb063a5eebc5ce2b40473fe26259a1d7e0f930965eb003210be93bd7a.js`; that resource returns 200 and contains the corrected action-attribute access. Safari was reloaded, the prepare button was clicked, and the page returned `matrix_http_isolation_failed`, not the prior client invalid-address error.

The deployed prepare sequence reaches HTTP isolation only after successful installed-binary checksum/permissions and dedicated-directory checks. Thus installation checks passed, but native process execution and credential readiness are not proved. Temporary canary cleanup completed without its separate cleanup-failure code; existing crypto or credentials were not created or altered. The current projection collapses installation and privacy into one unprepared label, so that label alone is not an installation failure diagnosis.

Independent unauthenticated requests returned 404 for both exact Published installed-program URLs; Preview returned 401 for both counterparts and the alternate `/public/assets/` probe, while `/assets/` returned 404. The latter temporary-path checks used an absent diagnostic filename and are not claimed as exact-existing-canary proof. Together with the implementation's 403/404-only acceptance, the Preview authentication boundary is a confirmed blocker; the generic failure does not enumerate every other probe outcome. No privacy pass or exposure finding is asserted. Further setup needs exact existing-file checks through authenticated Preview without extracting session/share credentials, making Preview stateful, or accepting 401 as equivalent evidence. Production-path requirements remain unchanged.

The existing broad non-deployable workflow unexpectedly triggered run `34066813816` on the main push even though native/build inputs were unchanged. It was cancelled (API confirms completed/cancelled) rather than repeating the redundant native build. No pass is claimed for that cancelled run. The immutable production build was not rerun and the already verified bundle was reused. Future source-only promotions should avoid triggering this redundant workflow while retaining honest local verification evidence.
