# Додавання двох відсутніх таблиць Matrix

Дозвіл користувача `yes` збережено у `forge/matrix-additive-schema-authorization-20260907.json`. Межа: створити лише відсутні `personal_consultant_matrix_outbox` і `personal_consultant_matrix_ingress`, не змінюючи жодної наявної таблиці або запису.

Реалізація: `/operations/matrix/schema` використовує чинну перевірку Власника, same-origin і purpose-bound CSRF. GET читає лише метадані фіксованих таблиць; POST виконує лише два незмінні CREATE-блоки з `scripts/godaddy-state-schema.sql`. Preview не доходить до пулу/міграції. Наявні таблиці пропускаються; IF NOT EXISTS захищає від конкурентного створення. Частковий збій DDL не відкочується видаленням — повторна дія створює лише відсутню таблицю. SQL, назви таблиць і значення рядків не приймаються від браузера.

Локально перед розгортанням: SDD `--before implementation` — passed, 13 документів; build, typecheck, environment policy — passed; Node 22.23.2 — 792 тести passed, 0 failed. Тести звіряють точні SQL-байти з канонічними CREATE-блоками, заборонені запити, Preview, повторення й частковий збій, версійний браузерний обробник та нерозголошення помилок БД. Це ще не доказ виконання міграції або консультації на Published.

## Фактичний результат на Published

Зафіксовано 2026-09-07T12:43:07Z. Код `b3d9a2dc72edba41ab266cb6e71e080e2d9659c3` передано до main, підтягнуто й опубліковано через Chrome у тому самому GoDaddy застосунку. Усі 29 входів нативного release мають незмінні SHA-256; Rust не перебудовувався. До оновлення анонімний GET нової адреси повертав 404; після оновлення — 403, тобто маршрут працює та не відкритий анонімно.

У чинній сесії Власника `/operations/matrix/schema` спочатку підтвердив відсутність обох таблиць. Виконано рівно одну дію «Створити лише відсутні таблиці Matrix». Відповідь підтвердила наявність обох таблиць і прибрала кнопку повторної міграції. Реалізація виконує лише два CREATE IF NOT EXISTS; наявні таблиці/записи не видаляються й не переписуються цією операцією. Повний SQL-файл і руйнівний GoDaddy SQL importer не виконувалися.

GET `/operations/matrix/status` після створення: `setupMode=disabled`, `configured=true`, `ready=false`, `reason=schema_unavailable`, `consultationWorking=false`, `consultationBlocked=true`. Консультацію повторно не надсилали, бо транспорт ще не готовий.

Підтверджена наступна невідповідність: метадані наявного `personal_consultant_state` повертають `state_namespace=utf8mb4_unicode_ci` і `state_key=utf8mb4_unicode_ci`; канонічний SQL та перевірка `REQUIRED_SCHEMA_COLUMNS` вимагають `utf8mb4_bin` для обох колонок. Поточний дозвіл виключає ALTER наявної таблиці, тому її не змінено. Потрібен окремий вузький дозвіл на зміну лише порівняння цих двох ключових колонок зі збереженням даних; це не дозвіл вимкнути перевірку схеми.

Довіра/ключі Matrix, секрети й Preview не змінювалися. Готовність консультації, Критик, доставлення відповіді та Product V1 не підтверджені.
