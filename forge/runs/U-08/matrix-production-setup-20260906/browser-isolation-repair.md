# Authenticated Preview isolation repair

Scope: existing authorized setup-only work; no new credentials, trust confirmation, stateful Preview, database/schema/domain or unrelated repository changes. Canonical production private-path requirement remains unchanged.

The previous anonymous verifier blocked on GoDaddy Preview HTTP 401. The repair retains harmless exact-path probes for at most three minutes only when all six Published checks pass and every failed Preview check is specifically HTTP 401. A fixed, content-addressed, GET-only Preview page uses its own browser session to perform the checks. Cookies/share credentials and response bodies never leave that origin. The owner page and verifier bind exact origins, expected popup/opener, nonce and verifier version; completion also binds the initiating owner session and its existing purpose-specific CSRF protection.

A separate harmless public control file must return its exact bytes through Preview. This is a positive authentication/shared-persistent-mount check: absent-file 404 or generic denial is insufficient. The four private store/spool aliases must each return 403/404 with bounded bodies free of canary, ELF and binding signatures; both runtime-program counterpart URLs must also be inaccessible. The latter does not claim that Preview installed Published's runtime programs. Redirects, 200/401, opaque/service-worker responses, oversized data and errors fail closed. No manual status-entry or accept-401 bypass was added.

Before readiness the server validates challenge freshness and retained file identity, repeats Published probes, removes only its owned test files, and revalidates installation. Abort/timeout/shutdown/replay fails closed. A crash may leave harmless unknown files; no broad orphan-file deletion is authorized. Successful browser assistance is explicitly labeled `browser_assisted_http_isolation`, not a server-attested remote proof or twelve anonymous checks. The server trusts the authorized sole owner's controlled browser as the executor of this evidence; this distinction is visible in the setup page.

Read-only targeted review by the separate `preview_proof_review` agent identified an async shutdown race. The repair tracks the complete active action, rechecks closed state after awaited boundaries and prevents a post-close challenge; regressions cover shutdown during preparation and initial HTTP probes. No extra trust-boundary blockers were reported in that review.

Local verification under Node 22.23.2: all 760 tests passed, plus typecheck, asset build and environment-policy checks. Tests cover positive-control mismatch, wrong origin/window/nonce/version, malformed/reordered reports, actual canary leaks, marker/ELF bodies, opaque/redirect/error responses, cleanup and owner-session binding. SDD before-implementation passes all 13 artifacts; this is not full product release acceptance or live privacy proof. Live deployment and real authenticated Preview result remain pending at this record.

The verified native/build input paths remain unchanged from source `271bcb8ec973e34fe0e53a0f260991ed1a4a5373`. This source-only promotion uses `[skip ci]` to avoid the unrelated broad Rust verification workflow; the already source-bound immutable production build and fresh local suite are the evidence, not a new CI pass.
