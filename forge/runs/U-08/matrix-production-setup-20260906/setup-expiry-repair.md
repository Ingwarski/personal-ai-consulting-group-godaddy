# Відновлення після завершення сеансу Matrix

Зафіксовано 2026-09-07T11:18:23Z. Користувач повідомив, що підтвердження спочатку в Safari, потім у Chrome завершилося помилкою Element. На Published через Chrome повторно спостерігався `matrix_setup_unavailable`, але сторінка продовжувала показувати попереднє порівняння й кнопку підтвердження. Помилка Element вже не була відкрита під час огляду; її точний текст не відтворюється як спостережений факт.

Код має незмінний 15-хвилинний строк процесу Node/Rust і п'ятихвилинний строк окремого SAS. Нове порівняння не поновлює строк процесу. Час спостережень узгоджується із завершенням процесу за таймером, але попередній код не зберіг точну причину його завершення. Підтверджений дефект: обробник помилки зберігав старий `view.status`, отже показував недійсне порівняння як чинне.

Вузьке виправлення Node:

- завершений/недоступний процес закривається; старі SAS-дані вилучаються зі сторінки, успішне закриття дає стан `stopped`;
- невдале закриття не дозволяє новий запуск, стан лишається `stopping`;
- `stale_comparison` прибирає лише недійсне порівняння, зберігаючи список пристроїв;
- нове порівняння не надсилається, якщо залишилося менш як шість хвилин: п'ять хвилин SAS плюс хвилина на початок обміну;
- сторінка пояснює спільний ліміт і показує приблизний залишок на момент її оновлення; помилки скеровують до відновлення наявного сховища, не створення заміни.

Ні 15-хвилинний ліміт, ні ручне підтвердження довіри, ні перевірки приватності не вимкнено. Rust, секрети, ключі, пристрої, повідомлення, схеми БД та stateless Preview не змінено. Операційний дозвіл — повідомлення користувача `go ahead - work efficient and fast! I need to show the prototype in one hour!` після пояснення несправності. Це не дозвіл оголосити незавершений продукт готовим.

Перевірка: 19 цільових тестів пройдено; повний `npm run check` під Node 24.16.0 і цільовим Node 22.23.2 — 774 тести, build, typecheck й environment policy пройдено на обох. Усі 29 вхідних файлів збірки Rust збігаються з SHA-256 наявного незмінного пакета; перебудова не потрібна. SDD `--before implementation` перевірив 13 документів без помилок. Публікація ще очікується на момент цього запису. Живе підтвердження довіри та доставлена консультація ще не доведені.

## Наступне спостереження, 2026-09-07T11:26:29Z

Коміт `19edf572b6ba44fadff149cb4cdcebf76935aa13` передано в `main`; GoDaddy показав цей коміт із двома станами OK, після чого оператор натиснув публікацію. Старий процес зупинено без видалення даних. Після заміни Published повернув стан `unprepared`; спроба перевірки вже наявного сховища дала `matrix_release_unsafe_path` до запуску Rust. Причина ще не встановлена. Додано лише закриту owner-діагностику метаданих: етап, фіксовану мітку об'єкта, права, відповідність власника, тип та кількість посилань. Довільні імена, повні шляхи, вміст і секрети не відображаються; права не виправляються автоматично, захист не обходиться.

63 цільові тести пройдено. Один повний прогін під Node 22 показав одну невдачу; збережений стислий вивід не містив її імені. Повторний повний прогін із читабельним звітом пройшов усі 775 тестів без змін між прогонами; попередню невдачу не названо виправленою. Типи та SDD-перевірка пройдені. Потрібні публікація діагностики й точна жива причина відмови; демонстрація консультації ще не готова.

## Пропущені права кешу медіа, 2026-09-07T11:38:06Z

Після публікації `1c790377beff271abe5061de1f13face46bc23f9` Chrome Published показав: етап `store`, фіксована мітка `other`, права `644`, звичайний файл належить поточному власнику, одне посилання, не symlink. Вміст не читався. Це підтверджує причину перевірки прав, але ще не встановлює конкретне ім'я живого файлу.

Перевірено локальні джерела зафіксованих `matrix-sdk`/`matrix-sdk-sqlite` 0.18.0: `client/builder/mod.rs::build_store_config` створює також `SqliteMediaStore`, а `media_store.rs` визначає `matrix-sdk-media.sqlite3`. Наш `secure_fresh_store_files` охоплював state, crypto й event-cache, але пропустив media та її WAL/SHM. Із поточною маскою процесу новий SDK-файл може лишитися 0644; це джерельно підтверджений пропуск, конкретний живий файл перевіряється після розширення фіксованих діагностичних міток.

Додано окрему owner/CSRF-дію лише для цих трьох файлів і лише після точного діагностичного результату `store`/media/0644/current-owner/regular/single-link. Дія не запускається автоматично з `prepare`, недоступна Preview чи активному процесу. До зміни перевіряє pinned release, приватні каталоги, обидві встановлені програми, наявний приватний binding і весь store/spool. Невідомий небезпечний об'єкт блокує зміни. Файли відкриваються без слідування symlink, повторно перевіряються inode/owner/link/mode/parent; лише 0644 звужується до 0600 через дескриптор. Байти не читаються й не записуються; після дії повторно перевіряються розмір, mtime, inode та повна сувора інспекція.

Для майбутніх файлів синхронний spawn setup і production sidecar успадковує попередню маску, посилену `0077`; Node повертає свою попередню маску до виходу з синхронного виклику. Пороги довіри, HTTP-приватність, фіксований Rust-пакет та наявний пристрій збережені. 786 тестів пройдено під Node 22.23.2, включно з реальним дочірнім процесом без облікових даних, збереженням байтів/inode та відмовою на links/чужих режимах/небезпечних каталогах/відсутньому binding. Build, typecheck й environment policy пройдені. Публікація й окрема жива дія ще очікуються на момент запису.
