# Chrome isolation follow-up

Recorded 2026-09-07T09:02:37Z. The user explicitly instructed: “for working with Godaddy use chrome”. This supersedes the previous Safari GoDaddy-login handoff; Chrome is the selected GoDaddy browser. Existing Chrome GoDaddy session showed Published `be0f4077aa60f28f61066c6278143caeee8bfe11`, site/infrastructure OK. The consulting-app owner login was completed through its Google button and the already signed-in confirmed owner account; no password, cookie, saved credential or token was read or copied.

Two explicit preparation actions in Chrome returned `matrix_http_isolation_failed` without a browser challenge. The existing page did not expose the failing stage or probe. Separate local, credential-free HTTP status checks returned Published 404 for the two runtime URLs and two deliberately nonexistent private-path examples, Preview 401 for most examples, and Preview 503 for one nonexistent example. These local requests do not prove existing-file privacy or the exact cause of the server-side failure. No protection was bypassed.

The scoped repair adds an owner-only diagnostic consisting of a fixed stage, fixed target/environment labels, numeric HTTP statuses (or no response), and the existing denial result. It exposes no URL, generated filename, response body or exception text, and does not change the pass condition, timeout, browser fallback eligibility or cleanup rules. Regression tests verify projection, redaction and continued denial of credential-bearing child execution after failure.

Local verification: `npm run check` under Node 22.23.2 passed all 762 tests, build, typecheck and environment policy. SDD before-implementation passed all 13 artifacts with no issues/warnings. All 29 native release source inputs still match the existing immutable bundle provenance. No native rebuild is needed or requested. Diagnostics are not yet live at this entry. Matrix device initialization, trust comparison and consultation delivery remain pending; this is not product-release acceptance.

After deploying `666d6dc`, Chrome preparation reached the actual browser challenge. The owner-only diagnostic then recorded all six Published paths denied with 404; Preview returned two 404 and four 401 responses anonymously. The authenticated verifier loaded in Chrome but failed its positive control. Native Chrome DevTools showed a 404 for the temporary `/assets/matrix-isolation-positive-…txt` request; no credentials or response contents were inspected. The parent returned `browser_proof` failure without a cleanup-error code.

Source inspection found a concrete implementation defect: `server.mjs` does not serve arbitrary files under `public/assets`, and neither settings runtime branch had a route for the positive control. The earlier mock-fetch tests assumed that file was served. The correction adds a narrowly matched read-only handler for fresh ordinary positive-control markers, not a general static-file server. It requires owner-safe real ancestors, a regular single-link non-executable file, bounded size, age under three minutes, exact harmless marker syntax and stable file identity. Private Matrix paths, arbitrary assets, symlinks/hardlinks, stale/oversized/malformed files and state-bearing requests remain denied. Preview creates no pool or file. A real loopback HTTP regression now exercises this route with the actual Node server. All 765 tests, build/typecheck/environment policy and the 13-artifact SDD precheck pass. Actual authenticated live proof after this correction is still pending at this entry.

## Live result through 2026-09-07T09:25:24Z

The selected GoDaddy Published dashboard identified `7fc02642b0dc4b547ce512929dffd3529e9eee57`; Preview had identified the same commit before publication. A credential-free query-bearing control request changed from the old 404 to the new handler's deliberate 400 on Published. Authenticated Chrome console performed the identical status-only check on Preview and observed 400, confirming its new handler too. Published `/healthz` remained 200. No native workflow was rerun.

The first corrected authenticated challenge still failed. Chrome DevTools showed 404 for the temporary positive control. A second controlled attempt held the harmless markers without opening the automatic verifier: at about 09:20Z, a read-only Chrome console fetch of the challenge's `positivePath` on Published returned 200. Navigating a temporary Chrome tab to that identical path on authenticated Preview returned the new handler's `Not found.` response. Only the harmless marker filename and HTTP status were projected; no browser credential, cookie, response content from private paths, or production token was extracted. This distinguishes the fixed missing-route bug from the still-unresolved file-visibility/precondition issue. It does not prove whether the cause is separate storage, file ownership, another safe-reader precondition, or runtime configuration. The shared-file assumption is unverified, not a release gate waiver.

The operator closed only the temporary inspection/verifier tabs and explicitly invoked `Скасувати перевірку й прибрати контрольні файли`; Published displayed the stopped state. Cleanup was requested through the owned-canary cleanup path; no broad deletion or existing-state cleanup was performed. No pending browser challenge remains. The public control reader itself expires files older than three minutes, so a later 404 alone must not be labeled filesystem-deletion evidence.

GoDaddy's read-only file-manager UI describes its inventory as Preview files; expanding `public/assets` showed no child filenames. This UI may represent source rather than the runtime mount and is not conclusive filesystem-isolation proof. Current official [Node.js Hosting concepts](https://developer.godaddy.com/en/docs/api-users/concepts/nodejs-hosting-concepts) distinguish preview and publish runtime variants; the [deployment guide](https://www.godaddy.com/en-ca/help/deploy-my-cursor-or-claude-app-with-godaddy-nodejs-hosting-42908) identifies `/public/assets/` for persistence. Neither inspected statement establishes shared writable storage between variants. Do not invent that provider guarantee.

Next bounded task: establish the actual Preview/Published storage visibility and reconcile the setup-only positive-control design with that evidence, retaining exact existing-file production denial checks and authenticated Preview checks. No user credential entry is needed yet. Existing Matrix bot/session/room and all secrets/database schemas remain unchanged. This run does not complete the Matrix connection or product release.

## Visibility correction, 2026-09-07T09:45:37Z

Published and Preview were updated to `a483af21a37d057cf2f768557fef8511c4c2a828` (bounded diagnostic header and preservation of cleanup errors). In authenticated Chrome Preview, a credential-free nonexistent-control request returned `404` with `x-matrix-control-result: missing_file`, distinguishing that result from unsafe ancestors, expiry, and other reader failures. This status-only observation did not itself prove privacy or general storage isolation.

An independent scoped review of architecture §12 found that shared control-file visibility across variants was an extra implementation assumption, not a canonical production requirement. The correction preserves all twelve exact private-path checks. Before announcing a browser challenge and after its report, Published must return the fresh public control over real HTTP with status 200, exact bytes and the `ok` diagnostic. Authenticated Preview accepts only that same exact control or the reader's exact `404`/`missing_file`/`Not found.` response. The latter is labeled `published_control_not_visible_in_preview`, not provider-wide storage isolation. Generic 404s, unsafe/missing ancestors, malformed responses and login/errors still fail. The previously early ENOENT branch now revalidates every ancestor before classifying a file as missing; a deterministic replaced-ancestor regression exercises that race. Version/nonce/origin/owner-session binding, expiry, bounded reads, all private response-body checks, final inode validation, owned-canary cleanup and post-cleanup installation inspection remain mandatory.

Local `npm run check` under Node 22.23.2 passed all 771 tests, build, typecheck and environment policy. Added real-HTTP integration uses distinct Published/Preview roots and confirms no Preview writes; negative cases reject invalid Published controls both before and after browser proof. The independent reviewer found no concrete blocker in the scoped implementation/test diff. The SDD pre-implementation checker passed all 13 artifacts. The existing native release is unchanged and will not be rebuilt. Live verification of this latest correction remains pending at this entry; no Matrix credentials or device were created.

## Deployment and physical-access pause, 2026-09-07T09:50:17Z

Committed/pushed correction `372fddfc2caf37825316f3bc11db14ab23101e49` to the working branch and `main`. Chrome GoDaddy's selected Preview displayed this exact commit, infrastructure OK and site OK; the operator clicked Publish. Published briefly returned 503 during replacement, then its real public verifier page and content-addressed script returned 200. Script `/assets/matrix-preview-isolation.867a03539fd93dbd533918a456d76ff58bec2548366488d3faaf80ee9ba5b9d8.js` contains the corrected visibility branch. This is live deployment evidence, not private-path proof.

The next native Chrome reload was refused with “The Mac is locked and automatic unlock could not unlock it.” The operator requested manual unlock. No new preparation challenge, credential, device, trust confirmation or consultation was started after this deployment. Resume in the existing Chrome owner tab: reload `/operations/matrix`, prepare, open its authenticated Preview verifier, and inspect the final Published result before the credential handoff. The prior over-strong shared-file assumption is fixed; the remaining immediate blocker is physical access to the locked Mac. Do not substitute anonymous Preview401, local tests or a deployment status for this pending live check.

## Actual privacy proof passed, recorded 2026-09-07T10:00:51Z

After the user replied `unlocked`, native Chrome reloaded the existing authenticated Published `/operations/matrix` page. The operator clicked `1. Перевірити програми й приватність каталогів`; Published reached `awaiting_preview` and stated its private paths were denied. The operator clicked `Перевірити авторизований Preview`. In the newly opened, authenticated Preview verifier, the native accessibility result read: `Перевірка Preview пройдена. Результат передано Published.` Returning to the original Published tab showed the final prepared state: `Обидві програми перевірено. Приватні каталоги підготовлено; перевірка HTTP-доступу пройдена.` It explicitly reported that the Published control was not visible in Preview and identified the evidence as server-checked Published plus browser-checked Preview, not server attestation of Preview.

This is the actual deployed `372fddfc2caf37825316f3bc11db14ab23101e49` path, not a mock or localhost result. Its prepared return requires both exact Published positive-control checks, all twelve private-path results with body checks, challenge/session/version binding, final inode checks, owned-canary cleanup and post-cleanup inspection. The observed visibility is only `published_control_not_visible_in_preview` for this challenge, not a provider-wide storage claim. Raw private response bodies, browser cookies and platform tokens were not retrieved or recorded. The runtime's credential-readiness latch is one-use and expires after five minutes; saving secrets/restarting requires a fresh check before native provisioning.

Chrome was then positioned at this same app's Settings → Secrets → Published (`Опублікувати`) → `Вставити файл .env`, with an empty input. The actual UI states that imported variables are appended as new draft rows. Only variable names were projected, not secret values; `MATRIX_SETUP_MODE` already exists and the working setup route confirms provision mode. The local manual helper therefore gained an explicit `--setup-mode-already-configured` flag to omit that existing row. It produces thirteen new variables while retaining the original fourteen-variable behavior without the flag. All sixteen synthetic helper tests passed, with no real Matrix login or clipboard access. No secret was entered or saved, no new bot device was created, and no device trust or consultation was attempted. Next is the user-only hidden-password/local-clipboard handoff, followed by a fresh privacy check and native device verification. The existing Safari bot session must remain signed in.

## Native setup reached manual device comparison, 2026-09-07T10:59:30Z

The user replied `saved` after the credential handoff. GoDaddy displayed Published restarted. The operator renewed the consulting app's owner login through its existing Google account (no password/token extraction). The first fresh privacy attempt stopped on two anonymous Preview 503 responses; all six Published paths returned 404. The exact attempt cleaned up without a cleanup error. Loading the existing authenticated Preview showed its stateless runtime-ready response; a bounded retry then passed the full browser-assisted check and cleanup.

The first fresh initialization action returned `matrix_configuration_incomplete` before spawning. A names-only inspection of Published's saved form found all required Matrix names except `MATRIX_PROTOCOL_VERSION`. No saved values were read; why that single row was absent is not established. Entering bare `1` triggered the provider UI's `secrets.validation.valueNotString`; entering the explicitly quoted string `"1"` enabled Save. The operator saved only this non-secret version setting, applied the pending Published restart, observed `Опубліковану програму перезапущено`, and confirmed the Matrix page had returned to its new-process unprepared state. No access token, passphrase, device ID, account or room was replaced.

A new exact-path Published/Preview privacy run passed after that restart. The subsequent `start_fresh` action accepted the configuration; status returned `verifying` from the pinned native setup process, with the new device's public fingerprint and existing bot/owner devices. This is actual GoDaddy execution of the production Rust component, not just a successful build. At this point the new device's persisted encryption state must be preserved: do not rerun the login helper, reset the store or initialize a replacement device. If setup expires, use the existing-store resume path after a fresh privacy check.

Safari's existing Element user menu confirmed the same bot account. Its existing device ID/fingerprint matched the prior authenticated UI observation. The operator initiated only that exact self-verification request from Chrome, opened Safari's `Start Verification` → emoji comparison, and refreshed Chrome status to `compare`. Both interfaces displayed the comparison and their manual confirmation controls. Neither `They match` nor `Усі символи або числа збігаються` was clicked by the operator. The user was asked to compare all seven emojis and personally confirm both interfaces only if identical. Private cross-signing readiness, bot trust, owner trust, completion and consultation delivery remain unconfirmed. No private account/device identifiers or SAS challenge values are persisted in this receipt. Source code and the immutable native artifact were unchanged in this turn; the SDD pre-implementation checker passed all thirteen artifacts.
