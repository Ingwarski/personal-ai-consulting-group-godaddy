# U-06-C1 Linux candidate verification — 2026-09-09

Status: verified immutable candidate, bundled and pinned in this checkout. **Published cutover and unit acceptance remain unperformed.** Implementation authority remains the existing U-06-C1 receipt; no new product scope or formal plan changes.

## Source-bound evidence

- Repository: `Ingwarski/personal-ai-consulting-group-godaddy`.
- Source/workflow commit: `489cf8f24cca8905cf07b0fced4aed2465f56991`.
- Successful authenticated GitHub Actions run: https://github.com/Ingwarski/personal-ai-consulting-group-godaddy/actions/runs/34277772246 (attempt 1).
- Immutable artifact ID: `10079403505`; archive size: 35,059,558 bytes.
- Archive SHA-256 independently recomputed from the downloaded ZIP and matched against GitHub's artifact metadata: `4eb944dfa5cb0528009590ae95fc826ea1204386625dc5ae75efc13363a8b73f`.
- Manifest SHA-256: `767f64d3dcaf2a88b1979eb38d94f4d8d04236ae417b6f12d36cdea164d14b49`.
- Sidecar SHA-256: `08e414426e03fd0eb36601108126a4ddd570f496cfcd1c538f804a9105354966` (45,114,704 bytes).
- Setup SHA-256: `33332a9514b827cd251ae68fd1a2ca470d5ef2a1921f4a99d7d44f9596937cb3` (39,937,264 bytes).

Independently checked all eight manifest artifact hashes/sizes, all checksum-file entries, all 47 source input hashes against the exact Git commit, native tree identity, source/run identity and the complete pinned builder configuration. Both downloaded executables passed the static stripped ELF validator. The directory contains only the expected ten bundle files. The verified generated bundle replaces the previous tracked bundle; the old bundle remains recoverable in Git. The runtime pin references these actual bytes, not a relabelled previous executable.

## Observed CI results

- Node 22.23.2: full application check passed, **928 tests passed, zero failed**, plus build/typecheck/environment policy.
- Release packaging: **12 passed**.
- Credential-free Linux/musl Rust tests: **115 passed, zero failed, 28 ignored**. Ignored database-dependent cases are not represented as CI passes; real isolated MySQL evidence is recorded separately in `implementation-20260908.md`.
- Two independent clean offline optimized builds passed and produced byte-identical binaries. Compile durations were 37m43s and 40m01s after the initial test compilation; total candidate job 1h27m9s. The truncated live log did not indicate a stalled build.

After installing the bundle and updating the pin, the local full `npm run check` passed again: 928 tests, zero failed/skipped, build/typecheck/environment policy. The actual MySQL release installer and read-only inspector both accepted these exact binaries in an isolated canonical-path temporary application root, without any public-assets directory or DB access. An initial diagnostic invocation used macOS's noncanonical `/var` alias and correctly failed the existing path guard; rerunning with `realpath`, as the installer tests already do, passed. No path guard was weakened. These checks validate installation, not Linux execution on macOS or live MySQL readiness.

## Read-only Published observations and remaining gate

Chrome Google owner sign-in succeeded and redirected to `/operations/runtime`. `/operations/matrix` was accessible and reported setup mode disabled. `/operations/matrix/status` remained blocked by the browser with `ERR_BLOCKED_BY_CLIENT`; its authenticated diagnostic content was not read. Anonymous `Access denied` is not proof of failed owner login. No extension/security settings were changed.

The GoDaddy database viewer listed five existing `personal_consultant_*` tables (state, archives, archive_tombstones, matrix_ingress, matrix_outbox); no four new `pc_matrix_*` tables were present. No row contents, database credentials, or private keys were copied into evidence. The file manager identifies itself as Preview read-only, so its file list is not evidence of Published key presence/absence. Public `/healthz` returned alive on the GoDaddy Node 22 runtime; this does not establish Matrix readiness.

Before a separately authorized live cutover: establish exact Published DB TLS compatibility and original private-store/key availability, then verify an isolated restoration of that actual backup. Missing original keys remain a recovery blocker. Only afterward authorize the exact additive provisioning/import/activation and verify Settings-closed delivery, restart, redeployment and DB-interruption recovery on the same deployed revision/schema/device. No production deployment, schema/secret changes, device reset, Preview statefulness, database wipe, legacy cleanup, domain changes, or HappyPro changes were performed.
