# Product Requirements Document: Personal Consultant

## PI-MATRIX-MYSQL-20260908 — Continuity and storage security correction

Source: the complete current product idea, especially `PI-MATRIX-MYSQL-20260908`. This refines `UC-001`, `UC-002`, `UC-003`, `UC-005`, `FR-052.a`–`FR-052.d`, `NFR-005`–`NFR-010`, `NFR-012` and `NFR-019`, without adding actors, product surfaces or paid dependencies. The existing GoDaddy app and MySQL database must retain every piece of state required to resume the same Matrix identity and accepted work after application files disappear on restart/redeployment. One verified final result must be delivered, not a duplicate consultation. Missing keys remain an explicit recovery blocker unless a valid backup/key source exists; neither migration nor retry grants device-reset authority.

Scoped ASVS reassessment: storage confidentiality/integrity and key inventory (`v5.0.0-11.1.1`, `v5.0.0-11.1.2`, `v5.0.0-11.2.1`, `v5.0.0-11.3.3`, `v5.0.0-14.1.1`, `v5.0.0-14.1.2`); least-privilege storage access and isolated credentials (`v5.0.0-8.1.1`, `v5.0.0-8.2.1`, `v5.0.0-13.3.1`); transactional recovery and exclusive ownership (`v5.0.0-2.3.3`, `v5.0.0-2.3.4`); authenticated encrypted database transport (`v5.0.0-12.3.1`). These existing obligations now explicitly cover Rust's narrowly scoped database access, encrypted Matrix state, pending-event journal and checkpoint—not only Node settings/archive. Other chapter mappings and the no-added-MFA exception remain unchanged; this is not a fresh full security audit or compliance claim.

Allowed outcome: a properly authorized runtime commits protected state atomically, resumes matching identity and accepted work, and rejects obsolete concurrent writers. Denied outcomes: plaintext keys/message bodies in database or logs; agent access to database credentials; Preview using Published state; invalid certificate fallback; stale-worker writes; checkpoint advancement that loses an unacknowledged event; partial migration presented as readiness; silent empty-store initialization for an existing device. Normal database outages retry without erasing state.

For `JOB-003`/`UC-003`, folder-specific setup and timed Preview comparison are removed only after the replacement no longer depends on private durable files. Genuine Matrix device verification, owner access, clear failure/recovery guidance and authorized temporary-file handling remain. `FR-052.a`–`FR-052.d` acceptance requires the actual Published flow with Settings closed, after restart and redeployment, with unchanged identity and no lost accepted messages; local storage tests alone cannot pass it.

- Статус: базова версія V1
- Основне джерело продуктового наміру: `docs/product-idea.md`
- Джерельний SHA-256: `9691980b7283afd2902f197c13dfdf05997b974a8e87784b68050383f7c0efe3`
- Дата формування: 16.08.2026
- Дата повторної валідації: 06.09.2026
- `owner_invocation_id`: `prd-critic-router-20260906`
- Покриття ID: `JOB-001`–`JOB-005` → `UC-001`–`UC-006`; `US-001`–`US-029`; `FR-001`–`FR-046`; `NFR-001`–`NFR-019`; `AC-001`–`AC-016`

## 1. Проблема

Власнику потрібен приватний бізнес-консультант і коуч, доступний через один звичний консультаційний канал на Mac, iPhone, Samsung Flip7 і Windows PC. Чинний браузерний спосіб роботи не дає потрібного досвіду: міжагентна взаємодія схожа на приховану видачу доручень, оновлення не завжди з'являються в одному живому чаті, технічні журнали та сирий Markdown заважають читанню, а процес і фінальний висновок надто повільні та перевантажені.

Проблема не зводиться до доставки однієї відповіді від ШІ. Користувач хоче бачити справжній консиліум окремих агентів-спеціалістів: їхнє дослідження, адресовані заперечення, критику та покращення рекомендації. Водночас результат має залишатися практичним, стислим, конфіденційним і керованим користувачем. Для зміни параметрів майбутніх консультацій потрібна одна вузька адаптивна поверхня, яка не перетворює браузер на другий чат, архів або дашборд.

## 2. Рішення та цінність

`Personal Consultant` — приватний консультант для одного власника в захищеній Element/Matrix-кімнаті. Він приймає текст, зображення або PDF, визначає, чи потрібна пряма відповідь або консиліум, ситуаційно залучає Codex-агентів-спеціалістів і окремого агента-критика вибраного провайдера, показує їхню фактичну дискусію наживо та завершує її одним синтезованим рішенням. Щоденна консультація лишається в Element; єдиний веб-виняток — `Налаштування власника` для параметрів майбутніх сесій. Codex і Claude Code працюють лише через OAuth чинних платних підписок цього власника; Google-вхід до Settings за `PI-AUTH-20260905` є окремою межею доступу й не замінює AI OAuth. API-key, PAYG та автоматичне придбання usage credits не є резервним режимом.

Ціннісна пропозиція:

- одна консультаційна точка входу в Element на всіх потрібних пристроях;
- реальна, видима й дослівна міжагентна співпраця замість імітації;
- професійне рішення з перевіреними фактами, критикою та чіткими межами впевненості;
- не більше трьох практичних наступних дій;
- контроль користувача над сесією, конфіденційними даними, зовнішніми діями, архівом і видимими витратами.
- безпечне керування дозволеними моделями, глибиною міркування та оркестраційною швидкістю майбутніх консультацій без послаблення обов'язкових інваріантів.

## 3. Межі продукту та модель роботи

### 3.1. Актори

| Актор | Роль у продукті |
|---|---|
| Власник | Єдиний користувач і єдиний одержувач результатів V1, джерело запиту, власник платних підписок і остаточний носій рішення та дозволу на високоризикові або зовнішні дії |
| Головний консультант | Приймає запит, визначає режим роботи, добирає агентів, реєструє повідомлення, керує дискусією та відповідає за фінальний синтез |
| Codex-агент-спеціаліст | Виконує окрему предметну частину, застосовує доречні навички та джерела, формує й захищає професійну позицію |
| Критик (Claude Code або Codex) | Незалежно перевіряє ключову пропозицію, докази, альтернативні пояснення, ризики й умови провалу |
| Зовнішні провайдери | `matrix.org`, GoDaddy, MySQL, OpenAI та Anthropic забезпечують транспорт, розміщення, зберігання й агентські можливості; Google підтверджує особу для входу Власника. Вони не є користувачами продукту |

V1 є строго приватною одноосібною автоматизацією, а не SaaS. Використовуються лише чинні платні підписки власника, план і правила яких дозволяють цей приватний сценарій; вони не застосовуються для обслуговування клієнтів, працівників чи інших третіх осіб, не передаються їм і не перепродаються. Якщо правила або eligibility плану більше не дозволяють сценарій, залежний запуск блокується до окремого перегляду автентифікації, ліцензування й білінгу.

### 3.2. Робочі режими

- **Пряма відповідь:** для короткого фактичного або очевидно простого запиту; зайві агенти не запускаються.
- **Консиліум:** для запиту, що перетинає кілька сфер, має високу ціну помилки, потребує незалежної критики або прямо позначений користувачем як консиліум.
- **Очікування дозволу:** лише коли потрібно продовжити роботу понад стандартний час, обробити особливо чутливий документ, виконати зовнішню дію або перейти іншу підтверджену межу повноважень.

### 3.3. Модель сесії

- Одночасно існує не більше однієї активної сесії.
- Нове звичайне повідомлення під час активної сесії є уточненням до неї.
- `Стоп` припиняє активну роботу.
- `Нова задача` закриває поточну сесію та створює нову.
- Завершена сесія стає незмінним архівним записом.

### 3.4. Межі повноважень

- Агенти аналізують, досліджують і рекомендують.
- Користувач залишається остаточним носієм рішення.
- Надсилання повідомлень третім особам, публікації, покупки, подання документів, кадрові рішення, зміни репозиторіїв або інших зовнішніх систем потребують окремого явного дозволу.
- Особисті коучингові питання про страхи, переконання або внутрішні конфлікти потребують попередньої згоди.
- Юридичні, податкові, медичні, інвестиційні та інші високоризикові висновки позначають професійні межі й потребу перевірки профільним фахівцем.

### 3.5. Модель підписок і автентифікації

- Codex використовує один захищений ChatGPT OAuth-стан власника з керованим штатним оновленням. Головний консультант і спеціалісти залишаються окремими реальними agent sessions/threads у межах account runtime; для цього не клонуються `auth.json`, OAuth-cache або інші credential stores.
- Для Claude-маршруту Критик використовує subscription OAuth/setup-token власника; для Codex-маршруту — окремий thread у тому самому керованому ChatGPT OAuth runtime. `OPENAI_API_KEY`, `ANTHROPIC_API_KEY` та інші API/PAYG credentials або provider-auth variables, здатні ввімкнути платний fallback, у runtime відсутні.
- Прострочений або відкликаний OAuth, вичерпана квота, невідомий auth mode чи неможливість підтвердити право плану зупиняють залежного агента. Система не переходить автоматично на API, PAYG або usage credits і не імітує результат.
- Повторна автентифікація відбувається поза Matrix-кімнатою й агентським контекстом через захищений provider browser/device flow або інший підтверджений out-of-band канал. Токени, setup-token і recovery material не надсилаються в чат.

### 3.6. Налаштування власника

- `Налаштування власника` — єдина вузька responsive web surface V1. Вона змінює лише параметри майбутніх консультацій і не містить чату, консиліуму, архіву, execution status або іншого дашборду.
- Доступ має лише Власник після Google-входу, перевірки точної дозволеної ідентичності й створення чинної підписаної захищеної локальної сесії за `PI-AUTH-20260905`. Застосунок не додає власного другого фактора. Резервний локальний пароль, OTP, magic link, інший провайдер входу, публічна реєстрація та додаткові права на дані Google не підтримуються; власні перевірки безпеки Google-акаунта не скасовуються.
- Google-вхід до Settings відокремлений від subscription OAuth Codex і Claude Code: він не замінює їх, не об'єднує credential state і не надає доступу до AI OAuth-секретів.
- Поверхня містить рівно три групи: `Codex-агенти`, `Критик`, `Швидкість консиліуму`. Інших груп або per-agent/per-session-unit overrides у V1 немає.
- `Codex-агенти` містять власні typed allowlisted selectors моделі та її рівня міркування; `Критик` — окремі selectors моделі та її provider-specific effort. Зміна значень одного провайдера не змінює значення другого; довільного вводу моделі немає.
- Підтримка окремо перевіряється за capability map відповідної обраної моделі без спільної шкали та без мовчазного downgrade. Однакові назви рівнів не означають однакової інтенсивності між провайдерами.
- Моделі показуються з точними підтвердженими назвами та версіями. Claude впорядковується `Opus → Sonnet → Haiku`, а підтверджені версії всередині сімейства — від новіших до старіших; сімейний alias не замінює доступні конкретні версії. Згадка моделі власником сама по собі не є доказом її доступності в поточному runtime.
- `Швидкість консиліуму` має лише `швидко / збалансовано / ретельно` і керує оркестрацією. Це не Claude Fast Mode, не API/PAYG fallback і не купівля usage credits.
- Увесь набір зберігається атомарно лише після повної валідації. Власник може повернути стандартні значення, скинути незбережені зміни та бачить фактично застосовані значення.
- Несумісна конфігурація fail-closed блокує нову сесію з конкретним поясненням. Активна сесія зберігає незмінний effective snapshot; нове збереження стосується лише наступних сесій.
- Налаштування не можуть вимкнути або послабити призначеного критика, реальну A2A-взаємодію, Matrix E2EE, дослівну видимість фактичних реплік, правила безпеки, приватності чи зовнішніх дозволів.

## 4. Основний робочий процес

### 4.1. Консультація в Element

1. Власник надсилає текст, зображення або PDF в одну приватну invite-only Matrix-кімнату з увімкненим E2EE через Element.
2. Система перевіряє точну дозволену пару `room_id` і `owner_mxid`, стан перевірки Matrix-пристроїв і потрібну згоду на обробку даних.
3. Не пізніше ніж через 5 секунд система підтверджує отримання запиту.
4. Головний консультант визначає режим: пряма відповідь або консиліум.
5. Перед модельним викликом система підтверджує subscription OAuth і доступну квоту без API/PAYG credentials; за невдачі залежна робота зупиняється з правдивим статусом і без fallback.
6. Для консиліуму головний консультант ситуаційно добирає 2–5 окремих Codex agent sessions/threads і залучає окремий runtime Критика вибраного провайдера.
7. Агенти спочатку формують незалежні позиції, а потім обмінюються адресованими повідомленнями через протокол A2A.
8. Кожне фактично надіслане агентське доручення, проміжна репліка, критика або виправлення одразу реєструється та повністю відображається в Element у канонічному порядку з роллю й часом.
9. Головний консультант звужує або замінює агента без видимого поступу та прозоро повідомляє про це.
10. Критик перевіряє зібрану пропозицію; учасники коригують її, якщо критика змінює рішення.
11. Головний консультант завершує дискусію, коли подальші репліки не змінюють рішення або потрібна зовнішня перевірка.
12. Фінальний результат надходить короткими самодостатніми повідомленнями: рішення, до трьох дій, ризики й умова перегляду, а за потреби — окрема технічна частина.
13. Сесія закривається й зберігається в зашифрованому незмінному архіві.

На старті кожної нової сесії система валідовує чинний набір Settings і фіксує його effective snapshot до першого залежного модельного виклику. Несумісний набір блокує старт; активна сесія не змінюється після нового збереження.

### 4.2. Зміна параметрів майбутніх консультацій

1. Власник відкриває `Налаштування власника` на desktop або mobile на наявному стандартному GoDaddy hostname, явно починає Google-вхід і після перевірки дозволеної ідентичності повертається до застосунку з окремою захищеною сесією. Скасований, недійсний або сторонній вхід не відкриває налаштувань; повторна спроба починає нову транзакцію входу.
2. Поверхня показує рівно три групи та чинні effective values без доступу до AI OAuth-секретів.
3. Власник змінює незалежні typed allowlisted модель+effort Codex, провайдера Критика та його власні модель+effort і orchestration speed або повертає стандартні значення.
4. Система перевіряє повний набір і зберігає його атомарно; будь-яка помилка залишає попередній набір чинним.
5. Валідне збереження застосовується лише до нової сесії, а активна продовжує працювати зі своїм незмінним snapshot.

### 4.3. Канонічні сценарії використання

Ці `UC-*` нормалізують наявні процеси §4.1–4.2 та вимоги V1. Підстава `JOB-*` у `docs/product-idea.md` має статус `confirmed`, а їхнє формулювання й наведені зв'язки — `source-inferred`; нових продуктових рішень або мотивів користувача тут немає.

| Завдання | Канонічне покриття |
|---|---|
| JOB-001 | UC-001 — пряма відповідь або консиліум |
| JOB-002 | UC-002 — керування активною сесією |
| JOB-003 | UC-003 — передумова захищеного доступу; UC-004 — зміна майбутніх параметрів |
| JOB-004 | UC-005 — архівування, експорт і видалення цілої сесії |
| JOB-005 | UC-006 — фактичні витрати й доступність квот |

**Спільні межі.** У кожному сценарії діють відповідні обов'язки `NFR-005`–`NFR-009`, `NFR-012` та конфігураційна частина `NFR-019` із `Security Requirements`. Matrix-операції додатково виконують `FR-001`, `FR-004`–`FR-006`, `FR-034`–`FR-036`; веб-доступ — `FR-037`–`FR-039` і незалежні `NFR-016.a`–`NFR-016.h`. Ці посилання охоплюють дозволені, заборонені й відновлювальні результати відповідних обов'язків, а не лише успішний шлях. Жодна команда, файл, відповідь провайдера чи агента не розширює повноважень. `NFR-019` у §7.5 визначає відтворюваність capability mapping, а його рядок у `Security Requirements` — безпечну production-конфігурацію; обидві частини залишаються обов'язковими.

#### UC-001 — Отримати консультацію та практичне рішення

**Завдання:** `JOB-001`. **Актори:** основний — Власник; допоміжні — головний консультант, Codex-спеціалісти й Критик для консиліуму, Element/Matrix та провайдери §3.1. **Тригер і мета:** Власник надсилає нову практичну проблему, щоб отримати відповідь або синтезоване рішення й наступні дії. **Передумови:** дозволена кімната й перевірений пристрій; немає іншої активної сесії; до залежної обробки отримано належну згоду, підтверджено Settings, subscription OAuth і квоти.

**Основний шлях:**

1. Власник надсилає текст, зображення або PDF; система перевіряє доступ, тип і допустимість даних, приймає запит, підтверджує його до 5 секунд і фіксує єдину сесію з незмінним effective snapshot.
2. Головний консультант визначає режим за `FR-008`. Для консиліуму запускає 2–5 окремих Codex-спеціалістів і окремого призначеного критика з показом ролей.
3. Агенти формують незалежні первинні позиції за `FR-012`, досліджують потрібні джерела й адресовано обмінюються аргументами через A2A; критик перевіряє пропозицію, учасники відповідають і виправляють її за потреби.
4. Головний консультант реєструє фактичні репліки, а система одразу показує їх повністю в канонічному порядку в тій самій кімнаті за `FR-015`–`FR-019`; дотримується меж швидкості `NFR-002`–`NFR-004`.
5. Коли подальша дискусія не змінює рішення, головний консультант надсилає один синтез: висновок, до трьох визначених дій, ризики й умову перегляду, за потреби — окремий цілісний технічний блок. Система закриває сесію та передає її до `UC-005`.

**Варіанти, відмови й відновлення:** простий запит на кроці 2 отримує пряму відповідь без зайвих агентів і переходить до завершення. Непідтримуваний або небезпечний вміст не запускає аналіз; особливо чутливий документ очікує окремого підтвердження. Неавторизований доступ відхиляється; відновлення Matrix-доступу потребує recovery key і перевірки нового пристрою. Несумісні Settings блокують старт до виправлення через `UC-004`. За недоступного агента, OAuth, квоти або доказів діє `FR-030`; відновлення авторизації відбувається поза чатом, без платного fallback. Відсутність поступу спричиняє видиме звуження чи заміну за `FR-023`; понад 10 хвилин — очікування дозволу за `FR-024`. Уточнення й команди переходять до `UC-002`; збій реєстрації/доставки не видається за підтверджену репліку й відновлюється без дублювання за `NFR-009`–`NFR-010`.

**Післяумови й межі:** результат або чесний неповний/зупинений стан видимий Власнику; фактичний запис збережений у канонічному порядку. Діють спільні межі §4.3 та §3.4–3.5; згода агентів не підміняє доказів, а рекомендація не є дозволом на зовнішню дію.

**Покриття:** `FR-001`, `FR-002`, `FR-003`, `FR-004`, `FR-005`, `FR-006`, `FR-007`, `FR-008`, `FR-009`, `FR-010`, `FR-011`, `FR-012`, `FR-013`, `FR-014`, `FR-015`, `FR-016`, `FR-017`, `FR-018`, `FR-019`, `FR-023`, `FR-024`, `FR-025`, `FR-026`, `FR-027`, `FR-028`, `FR-029`, `FR-030`, `FR-031`, `FR-034`, `FR-035`, `FR-036`, `FR-045`, `FR-046`; `NFR-001`, `NFR-002`, `NFR-003`, `NFR-004`, `NFR-005`, `NFR-006`, `NFR-007`, `NFR-008`, `NFR-009`, `NFR-010`, `NFR-011`, `NFR-012`, `NFR-013`, `NFR-014`, `NFR-015`, `NFR-017`, `NFR-019` (обидві частини); `AC-001`, `AC-002`, `AC-003`, `AC-005`, `AC-006`, `AC-007`, `AC-010`, `AC-011`, `AC-014`, `AC-015`.

#### UC-002 — Уточнити, зупинити або змінити активну задачу

**Завдання:** `JOB-002`. **Актори:** основний — Власник; допоміжні — головний консультант, активні агенти й Element/Matrix. **Тригер і мета:** нове повідомлення, `Стоп` або `Нова задача` спрямовує чи припиняє поточну роботу. **Передумови:** одна активна сесія в дозволеній кімнаті; повноваження відправника перевіряються для кожної події.

**Основний шлях:**

1. Власник надсилає уточнення; система перевіряє подію та приєднує її до поточної сесії без створення паралельної.
2. Головний консультант враховує уточнення в подальших дорученнях; система реєструє й показує фактичні зміни роботи за `FR-014`–`FR-019`.
3. Сесія продовжує `UC-001` із тим самим effective snapshot до завершення або наступної команди.

**Варіанти, відмови й відновлення:** `Стоп` припиняє нові модельні виклики, скасовує виконувані там, де це підтримує провайдер, і не дозволяє пізнім відповідям продовжувати зупинену роботу (`FR-021`). `Нова задача` спочатку закриває попередню сесію через `UC-005`, а потім проходить передумови `UC-001` для нової без перенесення старого активного контексту. Повторна доставка чи одночасні команди не дублюють роботу (`NFR-009`–`NFR-010`); збій закриття/запуску повідомляється правдиво, нова робота не видається за розпочату до перевірки стану. Стороння подія не змінює сесію; відновлення дозволеного доступу виконує `AC-006`.

**Післяумови й межі:** уточнення враховане, робота зупинена або попередню задачу закрито й створено одну нову; ніколи не існують два активні консиліуми. Спільні межі §4.3 діють і для команд; вони не змінюють дозволів, OAuth чи snapshot чинної сесії.

**Покриття:** `FR-007`, `FR-014`, `FR-015`, `FR-016`, `FR-017`, `FR-018`, `FR-019`, `FR-020`, `FR-021`, `FR-022`, `FR-023`, `FR-024`, `FR-030`, `FR-031`, `FR-045`; `NFR-003`, `NFR-007`, `NFR-008`, `NFR-009`, `NFR-010`, `NFR-012`, `NFR-017`, `NFR-019` (обидві частини); `AC-003`, `AC-004`, `AC-006`, `AC-007`, `AC-015`.

#### UC-003 — Увійти через Google та керувати локальною сесією

**Завдання:** `JOB-003`, передумова доступу до `UC-004`, а не окрема продуктова мета. **Актори:** основний — Власник; допоміжні — Google і застосунок у GoDaddy. **Тригер і мета:** Власник явно починає вхід, щоб отримати захищений доступ до своїх налаштувань. **Передумови:** наявний стандартний GoDaddy hostname; належно надані credentials і конфігурація клієнта; визначена точна дозволена Google-ідентичність за `NFR-016.b`. Збережена Console-конфігурація не доводить виконання решти передумов (§12).

**Основний шлях:**

1. Власник починає Google-вхід; система створює свіжу обмежену в часі транзакцію цього браузера за `NFR-016.c`.
2. Власник проходить підтвердження особи в Google; система приймає повернення лише після незалежних перевірок токена, точної ідентичності й транзакції за `NFR-016.a`–`NFR-016.c`.
3. Система створює окрему захищену локальну сесію, припиняє попередню сесію цього браузера й допускає `UC-004`; кожний захищений запит перевіряє за `NFR-016.d`–`NFR-016.f`.
4. Власник виконує видимий вихід або захищене припинення своїх активних сесій; система відкликає відповідний локальний доступ на сервері за `NFR-016.e`.

**Варіанти, відмови й відновлення:** стороння ідентичність, недійсний токен, скасування, збій Google, чужа/прострочена транзакція, replay чи одночасне повернення не створюють доступу; повторна спроба починає нову транзакцію. Повторний вхід змінює локальний ідентифікатор; idle/absolute expiry, відкликання або відключення Власника припиняють доступ і після рестарту. Підроблені запити й перевищення допустимих спроб відхиляються за `NFR-016.f`–`NFR-016.g`; безпечне відновлення доступності не вводить резервного пароля. Локальний вихід не означає виходу з Google, а повторний вхід враховує окремий provider-session lifecycle (`NFR-016.e`).

**Післяумови й межі:** тільки перевірений Власник має чинну локальну сесію, або захищений доступ закритий. Спільні межі §4.3 й `FR-038(a–d)`/`FR-039(a–c)` зберігають точну ідентичність, мінімальні права Google та ізоляцію AI subscription OAuth; вхід, відмова й вихід не змінюють AI OAuth. Застосунок не вимагає власного другого фактора і не видає Google-вхід за доказ MFA (`NFR-016.h`).

**Покриття:** `FR-037`, `FR-038(a–d)`, `FR-039(a–c)`; `NFR-005`, `NFR-006`, `NFR-009`, `NFR-012`, `NFR-016`, `NFR-016.a`, `NFR-016.b`, `NFR-016.c`, `NFR-016.d`, `NFR-016.e`, `NFR-016.f`, `NFR-016.g`, `NFR-016.h`, `NFR-019` (`Security Requirements`); `AC-012(a–f)` з окремими дозволеними, забороненими й відновлювальними результатами.

#### UC-004 — Зберегти параметри майбутніх консультацій

**Завдання:** `JOB-003`. **Актори:** основний — Власник; допоміжні — застосунок, Codex і Claude Code як джерела підтвердженої підтримки моделей. **Тригер і мета:** Власник відкриває налаштування, щоб наступні консультації застосовували його валідний вибір. **Передумови:** чинна сесія з `UC-003`, поточний набір, підтверджені окремі каталоги/capability maps і source-backed defaults.

**Основний шлях:**

1. Власник переглядає фактичні значення рівно трьох груп за `FR-040`; система забезпечує доступність на mobile/desktop за `NFR-018`.
2. Власник незалежно вибирає модель і допустимий їй рівень Codex-агентів, провайдера Критика та його власні модель і рівень, а також пресет швидкості; система показує підтверджені версії й порядок за `FR-041` та перевіряє семантику `FR-042`–`FR-043`.
3. Власник зберігає набір; система перевіряє повноваження, захист запиту, актуальність версії й усі значення та атомарно записує одну версію, показуючи фактично застосовані значення.
4. Під час наступного `UC-001` система повторно перевіряє сумісність і фіксує один незмінний effective snapshot; уже активна консультація продовжує використовувати попередній.

**Варіанти, відмови й відновлення:** скасування незбережених змін не записує даних; повернення defaults проходить повну перевірку й атомарне збереження. Невалідний/невідомий набір, застаріла версія або збій запису залишають попередній набір чинним; Власник виправляє значення чи повторює дію з актуального стану. Непідтримувана capability-комбінація блокує новий запуск до виправлення, без silent downgrade. Прострочена/відкликана сесія або підроблений запит не змінює Settings; доступ відновлюється через `UC-003`.

**Післяумови й межі:** збережено один валідний набір або жодної зміни; активний snapshot незмінний. Спільні межі §4.3, незалежність провайдерів і всі інваріанти `FR-046` діють для save/defaults/reset/speed; пресети не вмикають платного fallback. Інших веб-поверхонь, груп чи перевизначень параметрів немає.

**Покриття:** `FR-037`, `FR-038(c)`, `FR-039(a–c)`, `FR-040`, `FR-041`, `FR-042`, `FR-043`, `FR-044`, `FR-045`, `FR-046`; `NFR-016.d`, `NFR-016.e`, `NFR-016.f`, `NFR-016.g`, `NFR-017`, `NFR-018`, `NFR-019` (обидві частини); `AC-012(c–d, f)`, `AC-013`, `AC-014`, `AC-015`, `AC-016`.

#### UC-005 — Архівувати, експортувати або видалити цілу сесію

**Завдання:** `JOB-004`. **Актори:** основний — Власник; допоміжні — головний консультант-реєстратор, застосунок і GoDaddy/MySQL-сховище. **Тригер і мета:** завершення консультації створює її повний незмінний запис; явна команда Власника керує експортом або остаточним видаленням. **Передумови:** канонічно зареєстровані репліки; для операції Власника — перевірені Matrix-доступ і право на конкретну сесію.

**Основний шлях:**

1. Головний консультант завершує сесію; система фіксує повний текст і порядок підтверджених реплік у зашифрованому безстроковому архіві з перевірюваною незмінністю.
2. Власник явно запитує експорт цілої сесії та підтверджує його; система перевіряє право на запис і цілісність, після чого надає повний експорт.
3. Коли Власник явно запитує й підтверджує остаточне видалення цілої сесії, система видаляє саме цей архівний запис і повідомляє фактичний результат.

**Варіанти, відмови й відновлення:** без підтвердження експорт/видалення не виконуються; редагування чи видалення окремої репліки заборонене. Підміна ID, сторонній доступ, відсутній запис, нечинний key state або невідповідність integrity evidence не повертають захищений/неперевірений вміст. Збій запису, експорту чи видалення не видається за успіх; відновлення перевіряє канонічний стан та не дублює операцію (`NFR-009`–`NFR-012`).

**Післяумови й межі:** завершений запис збережений незмінно; підтверджений експорт охоплює цілу сесію, підтверджене остаточне видалення не лишає її доступною в архіві продукту. Спільні межі §4.3 захищають дані, ключі й точну ціль операції; сценарій не додає веб-архіву та не обіцяє видалення незалежних копій у провайдерів або вже отриманого експорту.

**Покриття:** `FR-001`, `FR-005`, `FR-014`, `FR-015`, `FR-016`, `FR-031`, `FR-032`, `FR-034`, `FR-035`; `NFR-005`, `NFR-006`, `NFR-007`, `NFR-008`, `NFR-009`, `NFR-010`, `NFR-011`, `NFR-012`; `AC-003`, `AC-005`, `AC-006`, `AC-008`.

#### UC-006 — Переглянути фактичні витрати та статус квот

**Завдання:** `JOB-005`. **Актори:** основний — Власник; допоміжні — застосунок, Element/Matrix та провайдери, що надають рахунки/usage/ліміт/reset. **Тригер і мета:** команда `Витрати` дає Власнику фактичну основу для оцінки витрат і доступності подальшої роботи. **Передумови:** дозволений Matrix-доступ; суми й статуси можуть бути частково відсутні, що не перешкоджає чесній відповіді.

**Основний шлях:**

1. Власник надсилає `Витрати`; система перевіряє доступ і приймає команду без створення паралельного консиліуму.
2. Система збирає наявні налаштовані місячні платежі за ChatGPT/Codex і Claude, фактичні інфраструктурні витрати та доступний статус плану `matrix.org` і квот провайдерів.
3. Система показує ці складові окремо в тій самій приватній кімнаті; використання AI в межах підписки позначає як включене, а ненадані суми/статуси — як невідомі.

**Варіанти, відмови й відновлення:** недоступний рахунок чи quota status не підміняється оцінкою; `matrix.org` має значення `0` лише за підтвердженого безкоштовного плану. Вичерпана квота, нечинний OAuth або непідтверджене право плану зупиняють залежну роботу за `FR-030`/§3.5 до відновлення передумов; відомий reset повідомляється, повторна авторизація відбувається поза чатом. Повторна доставка не дублює облік; стороння команда не отримує фінансових даних. Жодна відмова не вмикає API/PAYG чи придбання usage credits.

**Післяумови й межі:** Власник бачить відомі фактичні складові та явно позначену невідомість; команда не змінює білінг, квоти чи повноваження. Спільні межі §4.3 виключають credential material із відповіді; вигаданої вартості токенів/сесії та жорстких бюджетних лімітів немає.

**Покриття:** `FR-001`, `FR-030`, `FR-033`, `FR-034`, `FR-035`, `FR-036`; `NFR-005`, `NFR-006`, `NFR-007`, `NFR-008`, `NFR-009`, `NFR-012`; `AC-006`, `AC-007`, `AC-009`, `AC-010`.

## 5. Користувацькі історії

**US-001.** Як власник, я хочу користуватися консультантом в Element зі своїх основних пристроїв, щоб консультація не залежала від окремого браузерного інтерфейсу.

**US-002.** Як власник, я хочу, щоб лише мій дозволений Matrix-акаунт із перевірених пристроїв у визначеній приватній E2EE-кімнаті запускав систему й отримував її дані, а втрачений пристрій можна було відкликати та доступ — відновити через recovery key, щоб консиліум залишався приватним і керованим.

**US-003.** Як власник, я хочу надсилати текст, зображення та PDF, щоб давати агентам достатній матеріал для практичного аналізу.

**US-004.** Як власник, я хочу один раз погодити звичайну обробку бізнес-даних і окремо підтверджувати лише чутливі документи, щоб зберегти швидкість без втрати контролю.

**US-005.** Як власник, я хочу отримувати пряму відповідь на просте питання без зайвого консиліуму, щоб не витрачати час і кошти.

**US-006.** Як власник, я хочу, щоб головний консультант добирав окремі реальні Codex agent sessions/threads під конкретну проблему в межах мого одного захищеного ChatGPT OAuth-стану, щоб склад консиліуму відповідав задачі без копіювання credential stores.

**US-007.** Як власник, я хочу бачити окремого призначеного критика, авторизованого subscription OAuth обраного провайдера (setup-token лише для Claude), щоб основна пропозиція пройшла незалежну перевірку без API/PAYG fallback.

**US-008.** Як власник, я хочу бачити фактичні адресовані повідомлення між агентами, щоб відрізняти консиліум від одного згенерованого монологу.

**US-009.** Як власник, я хочу бачити кожну репліку повністю, з конкретною роллю та часом, щоб розуміти перебіг роботи без технічного шуму.

**US-010.** Як власник, я хочу отримувати швидкі та регулярні змістовні оновлення, щоб не чекати в невідомості.

**US-011.** Як власник, я хочу уточнювати активну задачу новими повідомленнями, щоб коригувати консиліум без створення паралельних сесій.

**US-012.** Як власник, я хочу зупинити роботу командою `Стоп`, щоб негайно припинити непотрібний аналіз і витрати.

**US-013.** Як власник, я хочу перейти до іншої проблеми командою `Нова задача`, щоб не змішувати контексти.

**US-014.** Як власник, я хочу отримувати один синтезований висновок, щоб не самостійно узгоджувати суперечливі монологи агентів.

**US-015.** Як власник, я хочу отримувати фінальну рекомендацію кількома короткими повідомленнями, щоб швидко побачити рішення, дії та ризики.

**US-016.** Як власник, я хочу отримувати окремий самодостатній технічний блок, коли він доречний, щоб цілком перенести його в Codex.

**US-017.** Як власник, я хочу, щоб факти, мої оцінки, припущення й професійні судження були розділені, щоб бачити реальну якість доказів.

**US-018.** Як власник, я хочу знати про збій, недоступного агента, прострочену авторизацію, вичерпану квоту або брак доказів, щоб система не імітувала неіснуючу роботу й не перемикалася на непогоджений платний fallback.

**US-019.** Як власник, я хочу мати зашифрований незмінний архів сесій, щоб пізніше прочитати дослівний консиліум.

**US-020.** Як власник, я хочу експортувати або видалити цілу сесію лише після підтвердження, щоб контролювати свої дані без прихованого редагування журналу.

**US-021.** Як власник, я хочу бачити командою `Витрати` налаштовані місячні платежі за мої AI-підписки, фактичні інфраструктурні витрати та доступний статус usage/ліміту/reset, щоб розуміти реальну модель витрат без вигаданої token-cost сесії.

**US-022.** Як власник, я хочу отримувати всі нові репліки в тій самій приватній E2EE Matrix-кімнаті в Element, щоб не переходити до окремого інтерфейсу.

**US-023.** Як власник, я хочу окремо дозволяти зовнішні й високоризикові дії, щоб агенти не змінювали реальний світ без мого рішення.

**US-024.** Як власник, я хочу відкривати єдину адаптивну поверхню `Налаштування власника` через свій дозволений Google-акаунт і окрему захищену сесію без власного другого фактора застосунку в MVP, щоб змінювати параметри майбутніх консультацій і залишати щоденну роботу в Element.

**US-025.** Як власник, я хочу окремо вибирати моделі Codex-агентів і призначеного критика з типізованих дозволених списків, щоб не вводити непідтримувану або непогоджену модель.

**US-026.** Як власник, я хочу незалежно задавати рівень міркування Codex і provider-specific effort Claude Code лише зі значень, підтверджених для відповідної обраної моделі, щоб налаштування одного провайдера не змінювали іншого й система не знижувала якість мовчки.

**US-027.** Як власник, я хочу вибирати оркестраційний пресет `швидко / збалансовано / ретельно`, щоб керувати складом, перевірками й темпом консиліуму без платного Fast Mode або PAYG fallback.

**US-028.** Як власник, я хочу зберігати весь валідний набір атомарно, повертати стандартні значення, скасовувати незбережені зміни й бачити фактично застосовані значення, щоб уникати часткової або незрозумілої конфігурації.

**US-029.** Як власник, я хочу, щоб несумісна конфігурація блокувала лише старт нової сесії, а активна сесія зберігала свій незмінний effective snapshot, щоб зміна налаштувань не ламала поточний консиліум і не послаблювала його обов'язкові правила.

## 6. Функціональні вимоги

| ID | Вимога | Пов'язані історії | Джерело |
|---|---|---|---|
| FR-001 | V1 має працювати в одній приватній invite-only Matrix-кімнаті з обов'язковим E2EE, одним Matrix-акаунтом власника й одним Matrix-акаунтом бота. Система приймає робочі запити лише від точної allowlist-пари `room_id` і `owner_mxid` після перевірки Matrix-пристроїв; recovery key і відкликання втраченого або скомпрометованого пристрою мають бути передбачені. Інший акаунт або кімната не може запускати агентів, читати архів чи отримувати захищені відповіді. | US-002, US-022 | PI |
| FR-002 | Перед першою звичайною обробкою система має отримати одноразову згоду на обробку бізнес-даних через `matrix.org`, GoDaddy, OpenAI та Anthropic. | US-004 | PI |
| FR-003 | V1 має приймати текст, зображення й PDF; непідтримуваний тип має отримати зрозуміле повідомлення без запуску аналізу. | US-003 | PI |
| FR-004 | Система має мінімізувати контекст для кожного агента й маскувати непотрібні ідентифікатори перед передаванням. | US-004 | PI, AG |
| FR-005 | Система не має приймати або включати до незмінного журналу паролі, API-ключі, повні карткові чи банківські реквізити, державні ідентифікатори та інші секрети. | US-004 | PI, AG, CO |
| FR-006 | Перед обробкою особливо чутливого документа система має попросити окреме підтвердження. | US-004 | PI |
| FR-007 | Після прийняття запиту система має створити або продовжити єдину активну сесію та підтвердити отримання. | US-001, US-010 | PI |
| FR-008 | Головний консультант має маршрутизувати простий запит у пряму відповідь, а складний, високоризиковий, багатосферний або явно позначений запит — у консиліум. | US-005, US-006 | PI, AG, CO |
| FR-009 | Для консиліуму головний консультант має ситуаційно добирати 2–5 Codex-агентів-спеціалістів, запускати їх як окремі реальні agent sessions/threads і показувати конкретні ролі. Усі вони використовують один захищений ChatGPT OAuth-стан власника з керованим штатним refresh у межах account runtime; клонування `auth.json`, OAuth-cache або інших credential stores для агентів не потрібне й заборонене. | US-006 | PI, CO |
| FR-010 | Консиліум має містити окремого призначеного Критика через явно вибраний Claude Code або Codex. Він має власний runtime/context, перевіряє повні первинні позиції, докази, альтернативи, ризики й умови провалу та надає фактичну A2A-критику. Фінал не приймає замість нього головного, спеціаліста, іншого runtime чи попередню generation. Критик не отримує ширших прав через свій провайдер. | US-007 | PI; PI-CRITIC-20260906 |
| FR-011 | Агенти мають обмінюватися фактичними адресованими повідомленнями через протокол A2A; один монолог, оформлений як кілька ролей, не відповідає вимозі. | US-008 | PI, CO |
| FR-012 | До спільної дискусії агенти мають сформувати незалежні первинні позиції, якщо завдання не вимагає спільної побудови одного артефакту. | US-006, US-008 | CO |
| FR-013 | Спільна дискусія має дозволяти агентам називати згоду, суттєву незгоду, запитання та відповідь конкретному агенту; додатковий раунд потрібен лише за нового факту, що змінює рішення. | US-008 | CO |
| FR-014 | Головний консультант має бути єдиним реєстратором підтверджених агентських повідомлень і єдиним відповідальним за фінальний синтез. | US-008, US-014, US-019 | PI, AG, CO |
| FR-015 | Кожне фактично надіслане агентське доручення, проміжна репліка, критика або виправлення має з'являтися в Element одразу після реєстрації, повністю й у канонічному порядку, без накопичення до фіналу. | US-008, US-009 | PI, CO |
| FR-016 | Тіло фактичного повідомлення не можна скорочувати, виправляти, переказувати, згортати або редагувати після реєстрації; виправлення оформлюється новою реплікою. | US-009, US-019 | PI, CO |
| FR-017 | На початку сесії мають відображатися дата й час старту, а над кожною реплікою — конкретна роль і час `HH:MM`. | US-009 | PI, CO |
| FR-018 | Користувацький чат не має показувати номери повідомлень, секунди, технічні ID, системні інструкції, приховані міркування моделей або журнали інструментів. | US-009 | PI, AG, CO |
| FR-019 | Зміст має використовувати читабельне нативне форматування Element/Matrix, а не показувати сирі Markdown-маркери як код чату. | US-009 | PI, RE |
| FR-020 | Нове звичайне повідомлення користувача під час активної сесії має додаватися як уточнення до тієї самої задачі. | US-011 | PI |
| FR-021 | Після прийняття команди `Стоп` система не має запускати нові модельні виклики цієї сесії, має скасувати виконувані операції там, де це підтримує провайдер, і не має публікувати пізні агентські відповіді як продовження зупиненої роботи. | US-012 | PI |
| FR-022 | Команда `Нова задача` має закривати поточну сесію й починати нову без змішування контекстів. | US-013 | PI |
| FR-023 | Якщо агент не показує змістовного поступу, головний консультант має звузити його завдання або замінити агента й показати цю зміну користувачеві. | US-010, US-018 | PI, AG, CO |
| FR-024 | Якщо стандартний консиліум потребує понад 10 хвилин, головний консультант має до спливу цього строку пояснити причину й попросити дозвіл продовжити. | US-010, US-023 | PI |
| FR-025 | Головний консультант має завершувати дискусію, коли подальші репліки не змінюють рішення або потрібна зовнішня перевірка. | US-014 | PI, CO |
| FR-026 | Фінальна рекомендація має надходити послідовністю коротких повідомлень: головний висновок, до трьох дій, критичні ризики/припущення/умова перегляду та, коли доречно, окрема технічна частина. | US-014, US-015, US-016 | PI, AG |
| FR-027 | Кожна суттєва дія у фінальній рекомендації має визначати результат, відповідального, строк, ознаку виконання, доказ, головний ризик і умову перегляду, коли ці поля доречні до задачі. | US-015 | AG |
| FR-028 | Система має розділяти підтверджені факти, інформацію користувача, припущення, професійні судження та невідоме; актуальні й високоризикові твердження мають спиратися на першоджерела. | US-017 | PI, AG |
| FR-029 | Згода кількох агентів не має подаватися як незалежний доказ; рівень упевненості визначається якістю даних і джерел. | US-017 | AG, CO |
| FR-030 | Збій, недоступний агент, неповний консиліум, прострочений або відкликаний OAuth, вичерпана квота чи брак доказів мають повідомлятися прямо. Залежна робота зупиняється fail-closed; система не створює вигадані репліки чи результати, не переходить на API key/PAYG/usage credits і надає лише безпечну out-of-band інструкцію повторної авторизації без токенів у Matrix. | US-018 | PI, AG, CO |
| FR-031 | Завершена сесія має зберігатися безстроково в зашифрованому архіві з незмінним змістом і порядком повідомлень. | US-019 | PI, CO |
| FR-032 | Користувач має мати можливість експортувати повну сесію або остаточно видалити її цілою лише після явного підтвердження; окремі репліки завершеної сесії не редагуються й не видаляються. | US-020 | PI |
| FR-033 | Команда `Витрати` має окремо показувати налаштовані щомісячні платежі за ChatGPT/Codex і Claude, фактичні витрати GoDaddy, MySQL та іншої інфраструктури, а також поточний статус плану `matrix.org` (`0` лише доки доступний безкоштовний план) і доступний provider-reported usage/ліміт/reset для Codex і Claude. Subscription AI usage конкретної сесії позначається як таке, що входить у підписку; ненадана сума лишається невідомою, а вигадана per-session token-cost, автоматична купівля usage credits і жорсткі грошові ліміти заборонені. | US-021 | PI |
| FR-034 | Усі вихідні репліки мають надходити в ту саму приватну E2EE Matrix-кімнату від одного Matrix-акаунта бота; конкретна роль агента й час `HH:MM` відображаються всередині повідомлення, а окремі Matrix-акаунти агентів чи інша консультаційна поверхня не створюються. | US-009, US-022 | PI |
| FR-035 | Перед будь-якою зовнішньою дією або переходом високоризикової межі система має отримати окремий явний дозвіл власника. | US-023 | PI, AG |
| FR-036 | За замовчуванням система має відповідати природною українською, адаптуватися до мови користувача й використовувати термін `лійка продажів`. | US-001, US-015 | AG, WC |
| FR-037 | V1 має мати рівно одну вузьку responsive web surface `Налаштування власника` лише для параметрів майбутніх консультацій. Вона не містить чату, живого консиліуму, архіву, execution status або іншого дашборду; повсякденна консультація лишається в Element/Matrix. | US-001, US-022, US-024 | PI |
| FR-038 | (a) `Налаштування власника` відкриваються лише після явно розпочатого Google-входу та серверної перевірки точної дозволеної ідентичності Власника. (b) Повернення завершує лише відповідну одноразову транзакцію того самого браузера; скасована, підроблена, повторна, прострочена або стороння відповідь не створює сесію. (c) Захищені сторінки, ресурси й API потребують чинної підписаної локальної сесії; зміни додатково потребують origin/CSRF-перевірки. (d) Зберігаються наявний GoDaddy app і стандартний hostname; доступ Google обмежений підтвердженням особи. Резервний пароль, інший провайдер, реєстрація чи власний другий фактор застосунку не додаються. | US-024 | PI, OI; PI-AUTH-20260905 |
| FR-039 | (a) Google-вхід і локальна Settings-сесія логічно й за секретами відокремлені від subscription OAuth Codex і Claude Code; жоден із цих контурів не замінює інший. (b) Google credentials, токени та дані транзакції входу не передаються AI-агентам, у Matrix, журнали чи архів; клієнт не отримує серверних секретів або AI OAuth tokens/setup-token. (c) Вхід, відмова, вихід і повторний вхід до Settings не змінюють AI OAuth-станів. | US-024 | PI; PI-AUTH-20260905 |
| FR-040 | Рівно три групи: `Codex-агенти` для головного й спеціалістів; `Критик` із явним перемикачем `Claude Code / Codex` і власними model/effort полями кожної гілки; `Швидкість консиліуму`. Показується вибрана гілка, неактивні уподобання зберігаються без виконання. Це єдиний рольовий виняток; довільних per-agent/per-session-unit overrides немає. | US-024–US-028 | PI; PI-CRITIC-20260906 |
| FR-041 | Кожна активна рольова/provider-гілка має typed allowlisted selector без довільного вводу, з точними підтвердженими назвами й версіями. Codex-критик підтримує вибір `GPT-6 Astra` (`gpt-6-astra`), якщо це підтверджує поточний Codex runtime. Claude: `Opus → Sonnet → Haiku`, новіші підтверджені версії першими; alias не підміняє версії. Непідтверджена модель позначається недоступною і не виконується. | US-025 | PI; PI-CRITIC-20260906 |
| FR-042 | Codex-агенти й Критик мають незалежні model/effort значення, навіть коли обидва використовують Codex; зміна одного не змінює іншого. Codex бере підтримку з поточного `model/list`, зокрема `gpt-6-astra` + `xhigh` (`Extra High`) для запитаного Критика; Claude — з підтвердження конкретної моделі підпискою. Між провайдерами немає спільної шкали. `За замовчуванням моделі` не передає explicit effort; недоступні збережені значення не скидаються мовчки. | US-026 | PI; PI-CRITIC-20260906 |
| FR-043 | Група `Швидкість консиліуму` має надавати лише `швидко / збалансовано / ретельно` як orchestration presets для складу й паралельності консиліуму, глибини перевірок та очікуваного темпу. Жоден preset не вмикає Claude Fast Mode, API/PAYG, usage credits або інший платний fallback. | US-027 | PI |
| FR-044 | Save валідовує структуру всіх трьох груп і поточні capabilities лише виконуваних гілок, атомарно зберігає один набір із неактивними уподобаннями та не змінює його за помилки. Неактивні значення не є доказом доступності. Defaults/reset/cancel/effective values явні; старі налаштування зберігають Claude як історичний вибір до нового owner-save. | US-028 | PI; PI-CRITIC-20260906 |
| FR-045 | Перед новою сесією перевіряються Codex-агенти, явно вибраний Критик, їхні точні model/effort та speed preset. Для Codex-критика відсутній, прострочений, помилковий чи завислий Claude не блокує каталог/вибір Settings/запуск і не викликається цим маршрутом. Для Claude-критика Claude має бути готовий. Невідомі або несумісні виконувані значення блокують запуск без fallback чи downgrade. Effective snapshot фіксує вибраний маршрут і не змінюється після save. | US-029 | PI; PI-CRITIC-20260906 |
| FR-046 | Settings/default/reset/speed не можуть вимкнути або послабити обов'язкового призначеного Критика, реальну A2A, Matrix E2EE, дослівність, safety/privacy чи окремі зовнішні дозволи. Провайдер, модель або effort не замінюються автоматично при збої. | US-029 | PI; PI-CRITIC-20260906 |

Позначення джерел: `PI` — `docs/product-idea.md`; `OI` — явна owner-вказівка поточного invocation; `AG` — `AGENTS.md`; `CO` — `CONSILIUM.md`; `WC` — `WORKING_CONTEXT.md`; `RE` — поточні тести й код репозиторію.

## 7. Нефункціональні вимоги

### 7.1. Швидкість

| ID | Вимога | Джерело |
|---|---|---|
| NFR-001 | Підтвердження прийнятого запиту має надійти не пізніше ніж через 5 секунд. | PI |
| NFR-002 | Перша видима репліка агента має надійти не пізніше ніж через 30 секунд після прийняття запиту на консиліум. | PI |
| NFR-003 | Під час активної роботи не має бути понад 60 секунд без змістовної видимої репліки або пояснення очікування. | PI |
| NFR-004 | Стандартний консиліум має завершитися не пізніше ніж через 10 хвилин або перейти в стан очікування дозволу на продовження. | PI |

### 7.2. Безпека та приватність

| ID | Вимога | Джерело |
|---|---|---|
| NFR-005 | Зміст Matrix-подій має бути захищений E2EE між перевіреними Element/Matrix-пристроями власника й бота; `matrix.org` отримує зашифрований зміст, але бачить службові метадані. E2EE завершується на verified Matrix device бота в GoDaddy Node runtime: розшифрований зміст існує в пам'яті runtime і передається OpenAI та, лише для Claude-маршруту, Anthropic через TLS, а архів зберігається в MySQL лише як шифротекст прикладного рівня. | PI |
| NFR-006 | OAuth-стани й setup-token зберігаються лише в захищеному runtime, можуть бути відкликані власником і не можуть міститися в Matrix-повідомленнях, промптах агентів, журналі, експорті, telemetry або клієнтському коді. `OPENAI_API_KEY`, `ANTHROPIC_API_KEY` та інші API/PAYG credentials або provider-auth variables, здатні увімкнути fallback, мають бути відсутні; перед кожним запуском підтверджується subscription OAuth mode. | PI, AG, CO |
| NFR-007 | Запит має виконуватися лише після перевірки Matrix-події, allowlist-пари `room_id` і `owner_mxid` та дозволеного стану пристрою; підроблена подія, інший акаунт, кімната або відкликаний пристрій не можуть запустити агентів. | PI |
| NFR-008 | Дані кожного агента мають бути обмежені мінімально необхідним контекстом. | PI, AG |

### 7.3. Цілісність і прозорість

| ID | Вимога | Джерело |
|---|---|---|
| NFR-009 | Повторна доставка тієї самої вхідної події не має створювати дубль сесії, агентської роботи, видимої репліки або облікованої вартості. | PI, CO |
| NFR-010 | Порядок відображених і заархівованих реплік має відповідати порядку підтвердженої реєстрації. | PI, CO |
| NFR-011 | Закрита сесія має мати перевірювану ознаку незмінності; конкретний механізм визначає архітектура. | PI, CO |
| NFR-012 | Налаштовані місячні платежі підписок, фактичні інфраструктурні витрати, доступний usage/ліміт/reset, збої, auth/quota expiry, заміни агентів і неповні результати мають бути доступні користувачеві без вигаданої per-session token-cost або прихованого «успішного» статусу. | PI, AG |

### 7.4. Сумісність і читабельність

| ID | Вимога | Джерело |
|---|---|---|
| NFR-013 | Основний консультаційний сценарій має працювати через штатні клієнти Element на Mac, iPhone, Samsung Flip7/Android і Windows PC без окремого браузерного consultation UI; вузькі `Налаштування власника` не є частиною цього сценарію. | PI |
| NFR-014 | Формат фінального результату має залишатися читабельним у межах Element/Matrix: короткі блоки, не більше трьох активних пріоритетів і окрема технічна частина лише за потреби. | PI, AG |
| NFR-015 | Відмінності між штатними клієнтами Element не блокують V1, якщо основний сценарій і E2EE залишаються доступними в тій самій приватній кімнаті. | PI |

### 7.5. Безпека, цілісність і доступність Settings

| ID | Вимога | Джерело |
|---|---|---|
| NFR-016 | Google-вхід і кожний захищений Settings-запит виконують незалежні обов'язки `NFR-016.a`–`NFR-016.h` з `Security Requirements`. Невідома конфігурація, недійсна відповідь провайдера, інший акаунт, повторне використання транзакції, нечинна локальна сесія чи відсутній захист зміни від підроблення запиту не дають доступу; відмова не розкриває credentials, дані сесії або AI OAuth. | PI; PI-AUTH-20260905 |
| NFR-017 | Валідний Settings set має зберігатися як одна узгоджена версія, а effective snapshot активної сесії — залишатися незмінним і відтворюваним протягом її lifecycle; частковий write або silent mutation заборонені. | PI |
| NFR-018 | Єдина Settings surface має бути придатною на mobile і desktop, керованою клавіатурою та assistive technology, мати програмно визначені labels, видимий focus, зрозумілі validation errors і не покладатися лише на колір для змісту. | PI, OI |
| NFR-019 | Однакова валідна комбінація settings і capability-map version має давати той самий effective configuration; unsupported mapping або невідомий capability state блокує нову сесію, а не спричиняє silent downgrade. | PI |

## Маршрутизатор Критика — узгодження 06.09.2026

Джерело: `PI-CRITIC-20260906`; чинні `JOB-001/JOB-003`, `UC-001/UC-004`, `FR-010/040/041/042/044/045/046`. Старі описи незалежності провайдерів тепер означають незалежність **рольових налаштувань** плюс окремі provider-specific гілки Критика. Перемикач доступний навіть без Claude-каталогу; Google-вхід не залежить від AI-каталогу. Підтримку Astra/Extra High на GoDaddy ще треба підтвердити, назва в запиті не підміняє перевірку.

`AC-002` перевіряється окремо для Claude і Codex. `AC-013` включає перемикання без зміни головного/спеціалістів, збереження неактивних уподобань та міграцію попередніх записів. `AC-014` включає Codex-критика за відсутнього/простроченого/403/завислого Claude без будь-якого виклику Claude, а також відмову для недоступних Astra/xhigh без підміни. `AC-015` фіксує маршрут, модель і effort активної сесії незалежно від наступних змін. Узгодження агентів одного провайдера не вважається незалежним доказом за `FR-029`.

### Повторна оцінка безпеки маршруту

Це цільова зміна `UC-001/UC-004`, а не повторний повний аудит ASVS. Решта попередніх оцінок і виняток без власного MFA збережені.

- `NFR-006`: Codex-критик використовує один наявний OAuth writer, без копій credential store; Claude секрет потрібен лише Claude-маршруту. Зміна pinned runtime перевіряє походження й сумісність (`v5.0.0-13.1.1`, `v5.0.0-13.3.1`, `v5.0.0-15.2.1`).
- `NFR-007/008`: призначений critic identity, provider, runtime й generation перевіряються сервером; роль із тексту або інший Codex-агент не можуть підтвердити критику. Вхід і model/effort — дані, не authority (`v5.0.0-2.1.1`, `v5.0.0-2.3.1`, `v5.0.0-8.1.1`, `v5.0.0-8.2.1`, `v5.0.0-8.2.2`, `v5.0.0-15.3.1`).
- `NFR-009/010/017/019`: обмежені виклики лише потрібного провайдера, атомарні versioned зміни та міграція, незмінні snapshots, replay/generation fencing і strict schema. Підроблені поля, повтор чи часткова міграція не змінюють повноважень або маршрут (`v5.0.0-2.3.3`, `v5.0.0-2.3.4`, `v5.0.0-15.2.2`, `v5.0.0-15.3.3`).
- Дані сесії не надсилаються Anthropic у Codex-маршруті. `FR-002/004/005`, Google/Matrix-доступ, секрети, архів і зовнішні дозволи не послаблюються. Нових security requirement IDs або виключень немає.

## Security Requirements

Для production web/API-поверхонь цільовим requirements-baseline є **OWASP ASVS 5.0.0 Level 2**. Рівень обрано через приватні бізнес-документи, credential material, зовнішні інтеграції, state-changing Settings і остаточне видалення сесій. Це мапування вимог, а не твердження про тестування, відповідність або сертифікацію.

Переглянуто всі розділи ASVS: V1–V16 застосовуються до GoDaddy Node HTTP/Settings, Matrix ingress і bridge, внутрішніх API/process boundaries, MySQL state/archive та provider integrations у наведеному нижче обсязі. V17 не застосовується, бо V1 не містить WebRTC. Специфічні ризики нативних Element-пристроїв/E2EE lifecycle та instruction-boundary для AI доповнюють ASVS і лишаються обов'язковими за `NFR-007`/`NFR-008`.

| Чинна вимога | Обов'язковий дозволений результат | Заборонений / fail-closed результат | ASVS 5.0.0 |
|---|---|---|---|
| NFR-005 | Лише TLS до зовнішніх провайдерів, vetted cryptography та app-layer ciphertext для архіву; ключі мають окремий lifecycle і не зберігаються разом із шифротекстом. | Plaintext archive, cleartext transport, слабкий алгоритм/ключ або decrypt після втрати авторизованого key state. | `v5.0.0-11.1.1`, `v5.0.0-11.1.2`, `v5.0.0-11.2.1`, `v5.0.0-11.3.3`, `v5.0.0-12.1.1`, `v5.0.0-12.3.1`, `v5.0.0-14.1.1`, `v5.0.0-14.1.2` |
| NFR-006 | Secrets надходять лише через визначений secret store із мінімальними правами, керованими створенням, відкликанням і знищенням; Google credentials та subscription OAuth мають окремі межі доступу. Токени отримують лише компоненти, яким вони потрібні; OAuth-транзакція прив'язана до ініційованого flow. Production dependencies походять із trusted pinned sources та мають inventory/remediation policy. | Secret у source/build/client/log/prompt/export, довгоживучий credential або session token у URL, непогоджений OAuth response, API/PAYG fallback або неперевірена dependency. | `v5.0.0-10.1.1`, `v5.0.0-13.1.1`, `v5.0.0-13.3.1`, `v5.0.0-13.3.2`, `v5.0.0-14.2.1`, `v5.0.0-14.2.3`, `v5.0.0-15.1.1`, `v5.0.0-15.1.2`, `v5.0.0-15.2.1`, `v5.0.0-15.2.3` |
| NFR-007 | Server-side authorization перевіряє Matrix event, exact `room_id`/`owner_mxid`, device state і право на конкретний record/action при кожному запиті. | Client-side-only check, spoofed event, object-ID tampering, revoked device або unauthorized room/account запускає роботу чи повертає дані. | `v5.0.0-2.3.1`, `v5.0.0-8.1.1`, `v5.0.0-8.2.1`, `v5.0.0-8.2.2`, `v5.0.0-8.3.1` |
| NFR-008 | Matrix/provider/file input канонікалізується один раз, валідовується на trusted boundary, contextually encodes output і передається до SQL/process/network APIs лише через безпечні параметризовані інтерфейси; agent treats external content as data, not authority. Дозволені image/PDF мають збіг extension/MIME/magic, size/unpacked limits і контрольоване місце зберігання. | Injection/SSRF/path traversal, небезпечний або надмірний файл, raw untrusted output чи вкладена інструкція розширює agent authority або запускає модель/процес. | `v5.0.0-1.1.1`, `v5.0.0-1.2.4`, `v5.0.0-1.3.5`, `v5.0.0-1.3.6`, `v5.0.0-1.5.2`, `v5.0.0-2.1.1`, `v5.0.0-2.2.1`, `v5.0.0-5.1.1`, `v5.0.0-5.2.1`, `v5.0.0-5.2.2`, `v5.0.0-5.3.2`, `v5.0.0-15.3.1` |
| NFR-009 | Idempotency keys, rate/concurrency bounds, request/body/file/time limits і resource cleanup обмежують повтори та виснаження ресурсів. | Replay, duplicate event, oversized/slow request або retry storm створює повторну дію, необмежену роботу чи неконтрольовану вартість. | `v5.0.0-2.1.3`, `v5.0.0-2.3.2`, `v5.0.0-2.3.4`, `v5.0.0-2.4.1`, `v5.0.0-15.1.3`, `v5.0.0-15.2.2` |
| NFR-010 | Persisted registration визначає один канонічний порядок; publish/archive transitions перевіряють current state і завершуються транзакційно або з recovery state. | Partial/reordered write або concurrent update видається за підтверджену канонічну репліку. | `v5.0.0-2.3.3`, `v5.0.0-2.3.4`, `v5.0.0-15.3.3` |
| NFR-011 | Незмінність закритої сесії підтверджується collision-resistant integrity evidence, яке перевіряється перед export/read. | Змінений архів або evidence mismatch повертається як валідна завершена сесія. | `v5.0.0-11.4.3`, `v5.0.0-15.3.3` |
| NFR-012 | Security events містять when/where/who/what, захищені від injection/unauthorized access, мають synchronized time, correlation і alert/response path; user-facing errors generic, а server-side detail не містить secrets. | Credential/PII у логах, log injection, silent security failure, success masking або stack/internal detail у відповіді. | `v5.0.0-16.1.1`, `v5.0.0-16.2.1`, `v5.0.0-16.2.2`, `v5.0.0-16.2.5`, `v5.0.0-16.3.1`, `v5.0.0-16.3.2`, `v5.0.0-16.3.3`, `v5.0.0-16.3.4`, `v5.0.0-16.4.1`, `v5.0.0-16.4.2`, `v5.0.0-16.5.1`, `v5.0.0-16.5.2`, `v5.0.0-16.5.3` |
| NFR-016 | Дозволена Google-ідентичність і захищена локальна сесія перевіряються за пунктами нижче; продуктове рішення про власний другий фактор визначене `PI-AUTH-20260905`. | Інший акаунт, підроблення, replay, fixation, обхід перевірок або кешована захищена відповідь дають доступ; Google-вхід видається за підтверджену MFA. | Окреме мапування `NFR-016.a`–`NFR-016.h` нижче |
| NFR-017 | Settings write перевіряє authorization, current version і повний schema set та commit-ить один atomic state; failure лишає попередню версію без side effects. | Partial write, lost update, stale overwrite або inconsistent snapshot стає effective configuration. | `v5.0.0-2.3.3`, `v5.0.0-2.3.4`, `v5.0.0-15.3.3` |
| NFR-019 | Production конфігурація fail closed за missing/unknown values, мінімізує exposed services/methods/headers, не довіряє `Host`/forwarded headers без allowlist і не дозволяє preview ділити stateful credentials/schema без окремого isolation proof. | Default/insecure config, unsupported capability, host-header injection, debug/admin surface або shared preview state запускає production session. | `v5.0.0-2.1.2`, `v5.0.0-2.2.3`, `v5.0.0-13.2.2`, `v5.0.0-13.2.4`, `v5.0.0-13.2.5`, `v5.0.0-15.3.3`, `v5.0.0-15.3.5` |

### NFR-016 — Обов'язки Google-входу та сесії

| Пункт | Незалежно перевірюваний обов'язок | ASVS 5.0.0 |
|---|---|---|
| NFR-016.a | Сервер приймає лише автентичний Google ID Token для потрібного клієнта й призначення: перевіряє підпис, дозволений алгоритм, довірене джерело ключів, issuer, audience та строки чинності. Metadata іншого issuer, довільне джерело ключів, непідписаний токен або access token замість ID Token відхиляються. | `v5.0.0-6.8.2`, `v5.0.0-9.1.1`, `v5.0.0-9.1.2`, `v5.0.0-9.1.3`, `v5.0.0-9.2.1`, `v5.0.0-9.2.2`, `v5.0.0-9.2.3`, `v5.0.0-10.5.3`, `v5.0.0-10.5.4` |
| NFR-016.b | Ідентичність Власника прив'язана до стабільного Google `sub` у просторі довіреного issuer і точної дозволеної підтвердженої email-адреси. Введена адреса, display name, підказка вибору акаунта або перший успішний сторонній вхід не створюють право Власника. | `v5.0.0-10.5.2` |
| NFR-016.c | Лише явна дія Власника починає вхід. Відповідь, код і nonce прив'язані до однієї непередбачуваної, обмеженої в часі транзакції того самого браузера; її повторне чи одночасне використання не створює іншу сесію. Скасування й помилки не відкривають захищені дані, а повторна спроба використовує нову транзакцію. Протокольне повернення з Google не підмінює origin/CSRF-захист наступних змін у застосунку. | `v5.0.0-10.1.2`, `v5.0.0-10.2.1`, `v5.0.0-10.5.1`, `v5.0.0-11.5.1`, `v5.0.0-7.6.2` |
| NFR-016.d | Кожний захищений запит перевіряє сесію на сервері. Успішна автентифікація й повторний вхід створюють новий динамічний ідентифікатор сесії та припиняють попередню сесію цього браузера; статичний credential або Google-токен не служить сесією застосунку. Якщо застосовано reference token, він унікальний, криптографічно випадковий і має щонайменше 128 біт ентропії. | `v5.0.0-7.2.1`, `v5.0.0-7.2.2`, `v5.0.0-7.2.3`, `v5.0.0-7.2.4` |
| NFR-016.e | Архітектура до реалізації визначає idle/absolute строки, число паралельних сесій, поведінку за досягнення межі та окремий lifecycle Google-сесії й сесії застосунку. Вихід, expiry, відключення дозволеного власника чи відкликання сесій припиняють доступ на сервері, включно після рестарту. Власник має видимий вихід і захищений спосіб припинити свої активні сесії; локальний вихід не видається за вихід із Google, а залежність повторного входу від provider-session документується. | `v5.0.0-7.1.1`, `v5.0.0-7.1.2`, `v5.0.0-7.1.3`, `v5.0.0-7.3.1`, `v5.0.0-7.3.2`, `v5.0.0-7.4.1`, `v5.0.0-7.4.2`, `v5.0.0-7.4.4`, `v5.0.0-7.4.5`, `v5.0.0-7.6.1` |
| NFR-016.f | Cookies мають `Secure`, `HttpOnly`, прив'язку до одного hostname та `SameSite`, відповідний призначенню транзакції чи сесії. Захищені відповіді не кешуються; browser storage не містить Google/AI credentials, крім дозволеного session token застосунку. Origin/CSRF-перевірка змін, HSTS, CSP із захистом від embedding, правильний Content-Type, nosniff і referrer policy не дозволяють підробити запит або витекти даним входу. | `v5.0.0-3.3.1`, `v5.0.0-3.3.2`, `v5.0.0-3.3.3`, `v5.0.0-3.3.4`, `v5.0.0-3.4.1`, `v5.0.0-3.4.3`, `v5.0.0-3.4.4`, `v5.0.0-3.4.5`, `v5.0.0-3.4.6`, `v5.0.0-3.5.1`, `v5.0.0-4.1.1`, `v5.0.0-14.2.2`, `v5.0.0-14.3.2`, `v5.0.0-14.3.3` |
| NFR-016.g | Документовані обмеження частоти, автоматизованих спроб і ресурсів захищають створення та завершення входу без можливості зловмисно заблокувати Власника назавжди. Зміна браузерної транзакції чи рестарт не обходять обмеження; безпечне відновлення доступності й відмова не потребують резервного пароля. Події входу, відмови й відкликання мають очищений від секретів доказ за `NFR-012`. | `v5.0.0-6.1.1`, `v5.0.0-6.3.1` |
| NFR-016.h | Застосунок виконує рішення про відсутність власного другого фактора за винятком нижче. За відсутності перевірених даних Google про силу чи свіжість автентифікації припускається мінімальна, однофакторна сила; сам факт Google-входу не засвідчує MFA. Невиконаний захист сесії чи токена не виправдовується цим винятком. | `v5.0.0-6.3.3`, `v5.0.0-6.8.4` |

**Визначений виняток `v5.0.0-6.3.3`.** Поточне пряме рішення Власника `PI-AUTH-20260905` — Google-вхід без власного другого фактора застосунку для цього MVP. Обґрунтування обмежене приватним використанням одним дозволеним Власником, вузькою поверхнею налаштувань і збереженням провайдерського входу та його власних перевірок; це не підстава знижувати цільовий Level 2 для решти вимог. Парольний механізм застосунку й запасний спосіб входу не повертаються.

Залишковий ризик: фішинг, компрометація або викрадення чинної Google-сесії дозволеного Власника можуть дати зловмиснику підтверджену Google-ідентичність; викрадена локальна сесія також залишається ризиком до відкликання чи завершення її строку. Вхід через провайдера та його можливі додаткові перевірки не гарантують захисту від цих сценаріїв. Обов'язкові компенсувальні вимоги — вся сукупність `NFR-016.a`–`NFR-016.g`, точна owner-авторизація `FR-038`, ізоляція секретів `FR-039`/`NFR-006`, обмеження зловживань `NFR-009`, журналювання/реагування `NFR-012` і безпечна конфігурація `NFR-019`. Власник переглядає цей виняток після підозри на компрометацію, зміни правил/можливостей Google, розширення користувачів, прав або поверхонь, а також перед переходом за межі цього MVP; до відновлення порушених компенсувальних вимог захищений доступ закритий.

Це визначене продуктове рішення з документованим ризиком і вимогами до компенсації, а не нове питання до Власника, доказ реалізованої MFA або повної ASVS-відповідності. Архітектура визначає механізми, QA — конкретні перевірки; достатність фактичної реалізації та весь застосовний набір контролів ще потребують доказів перед release.

**Застосовність при переході до Google.** `v5.0.0-6.2.8` і `v5.0.0-6.2.9` більше не мапуються на власний пароль Settings, бо цей спосіб входу вилучений; правила пароля й відновлення Google-акаунта належать Google. V6.5–V6.6 не створюють у застосунку MFA/OTP-функцій. V6.8.1 і V10.2.2 про кілька IdP, SAML-контроль V6.8.3 та OIDC back-channel logout V10.5.5 не застосовані до підтвердженого єдиного Google-входу без цих протоколів. Застосунок є OIDC-клієнтом, а не OAuth resource server, authorization server або OpenID Provider: V10.3–V10.4, V10.6–V10.7 не приписуються йому як реалізовані серверні контролі. Збереження точної адреси повернення й мінімальних декларацій прав у Google Console підтверджене (§12); набрання чинності конфігурацією, фактичні запитані права й поведінка consent залишаються перевірюваними передумовами інтеграції, а не доказом provider compliance. Решта попереднього мапування збережена; auth-корекція не засвідчує новий повний аудит усіх контролів V1–V17, а поточне додавання `UC-*` не змінює її обов'язків.

## 8. Підтверджені продуктові обмеження реалізації

- Користувацький канал V1 — Element із протоколом Matrix: одна приватна invite-only E2EE-кімната на публічному `matrix.org` із поточним безкоштовним планом.
- У кімнаті є один Matrix-акаунт власника та один Matrix-акаунт бота; доступ обмежений allowlist-парою `room_id` і `owner_mxid`, а ролі агентів показуються всередині повідомлень бота.
- Серверна частина розміщується в наявному GoDaddy Node.js app на стандартному GoDaddy hostname й не залежить у production від локального Mac; власний VPS або власний Matrix homeserver у V1 не використовуються. Settings state, session registry і зашифрований архів використовують GoDaddy/MySQL-еквіваленти, причому архівні дані зберігаються лише як шифротекст прикладного рівня.
- Головний консультант і спеціалісти працюють як окремі реальні Codex agent sessions/threads через один захищений ChatGPT OAuth-стан платної підписки власника; клоновані `auth.json` або окремі OAuth-cache не є умовою їхньої окремості й не використовуються.
- Критик працює як окремий runtime Критика вибраного провайдера через відповідний subscription OAuth платної підписки власника.
- У runtime відсутні `OPENAI_API_KEY`, `ANTHROPIC_API_KEY` та інші API/PAYG credentials, здатні ввімкнути fallback. Вичерпана квота або недійсний OAuth зупиняє залежну роботу; повторна авторизація відбувається безпечно поза Matrix і без передавання credential material агентам.
- Реальна міжагентна комунікація використовує протокол A2A.
- V1 є одноосібною приватною не-SaaS системою з однією дозволеною Matrix-кімнатою й однією активною сесією; підписки власника не обслуговують працівників, клієнтів чи інших третіх осіб.
- Повсякденний консультаційний досвід лишається в Element/Matrix; єдина дозволена web surface — вузькі responsive `Налаштування власника` без чату, архіву, live status або дашборду.
- Settings access виконує `FR-038`–`FR-039` і `NFR-016.a`–`NFR-016.h`: лише Google-вхід Власника на наявному стандартному GoDaddy hostname, окрема захищена локальна сесія й захист змін від підроблення. Власний другий фактор застосунку та резервний пароль не додаються; Codex/Claude subscription OAuth лишається окремим.
- Settings schema має рівно три групи: `Codex-агенти` з власними typed allowlisted моделлю й рівнем міркування; `Критик` з власними typed allowlisted моделлю й provider-specific effort; `Швидкість консиліуму` з `швидко / збалансовано / ретельно`. Шкали провайдерів незалежні, а підтримка перевіряється для конкретної моделі. Capability validation, точні підтверджені версії й порядок моделей, no-silent-downgrade, atomic save, defaults/reset, effective-value display і immutable active-session snapshot є обов'язковими.
- Жодний Settings path не може змінювати critic/A2A/E2EE/verbatim/safety/privacy/permission invariants або активувати Claude Fast Mode, API/PAYG чи usage credits.
- Користувач обрав змістовий і UX-напрям Candidate B «Дослівний консиліум»: усі фактично надіслані агентські доручення, проміжні репліки, критика й виправлення видно наживо повністю; приховані міркування, системні промпти та журнали інструментів до цього переліку не належать.
- Продукт не створює власного звуку повідомлень; сповіщення визначають штатні налаштування Element та операційної системи.
- Команда `Витрати` розділяє налаштовані щомісячні платежі підписок, фактичну інфраструктуру та доступний статус usage/ліміту/reset; вона не приписує підписному використанню вигадану token-cost сесії.
- Точні allowlisted моделі, capability-map source/version, provider-specific reasoning mapping, defaults, persistence/session-signing mechanisms, версія A2A, захищене сховище OAuth-станів, lifecycle refresh/reauth, MySQL schema, керування ключами, черги й повторні спроби є власністю `docs/architecture.md`, але не можуть змінити продуктові Settings/OAuth межі.
- Stateful preview дозволений лише після окремого доказу ізольованих schema, credentials і data boundary; без такого доказу preview лишається stateless. Ця міграція не дозволяє видаляти legacy Cloudflare/Google ресурси, секрети, бази чи rollback-артефакти без перевіреного recovery/restore evidence, точного переліку цілей і окремого підтвердження безпосередньо перед дією.
- `DAS Forge 4` є лише pattern evidence для typed allowlists, validated atomic save, defaults/reset і effective-value display. Його ширший settings scope, сім груп, sound/motion, free-text model input і per-unit overrides не є scope `Personal Consultant`.
- Наявний локальний live-chat доводить лише можливість зберігати дослівне тіло, приховувати технічні ID і перетворювати Markdown. Він не доводить готовність Matrix E2EE, `matrix.org`, GoDaddy runtime, A2A, архіву або агентського циклу й не є цільовим UI.

## 9. Рішення щодо перевірки продукту

- Найвищий прийнятний доказ готовності — наскрізний тест через реальну приватну E2EE Matrix-кімнату на `matrix.org`, перевірені Matrix-пристрої, штатні Element-клієнти, розгорнуту серверну частину та фактичних агентів.
- Модульні й інтеграційні тести потрібні для швидкого зворотного зв'язку, але не замінюють реального Element/Matrix-сценарію.
- Скріншот, статична документація, згенерований HTML, синтетична Matrix-подія або локальний browser-preview окремо не доводять готовність V1.
- Перевірка має відрізняти фактичне повідомлення агента від прихованих міркувань, технічних логів і синтетичних тестових даних.
- Кожна перевірка незмінності має порівнювати відображений, експортований і заархівований порядок та повний текст підтверджених реплік.
- Перевірка приватності має включати інший `owner_mxid`, інший `room_id`, неперевірений або відкликаний пристрій, підроблену й повторно доставлену Matrix-подію, секрет у повідомленні та чутливий документ без підтвердження.
- Перевірка E2EE має підтвердити реальне шифрування подій між перевіреними Matrix-пристроями й окремо зафіксувати, що розшифрований зміст обробляють GoDaddy Node runtime, OpenAI та, лише для Claude-маршруту, Anthropic через визначену довірчу межу.
- Перевірка швидкості має вимірювати чотири окремі межі: 5 секунд, 30 секунд, 60 секунд і 10 хвилин.
- Перевірка agent runtime має довести окремі реальні Codex sessions/threads через один керований ChatGPT OAuth-стан без клонів credential store, окремий runtime Критика вибраного провайдера через відповідний subscription OAuth та відсутність `OPENAI_API_KEY`, `ANTHROPIC_API_KEY` й інших PAYG fallback credentials.
- Негативні auth/quota-сценарії мають довести fail-closed поведінку за expiry, revocation, quota exhaustion і невідомого auth mode: жодного модельного fallback, нової фактичної agent reply чи прихованої покупки usage credits; повторна авторизація відбувається out-of-band.
- Перевірка приватної моделі має підтвердити, що тільки власник ініціює запити й отримує результати; доступ клієнта, працівника або іншої третьої особи не підтримується. Зміна правил провайдера, що забороняє цей сценарій, блокує запуск до перегляду.
- Перевірка `Витрати` має зіставити налаштовані subscription fees і фактичні infrastructure invoices/usage з показаними значеннями, зберегти unavailable як невідоме та підтвердити відсутність вигаданої per-session token-cost.
- Наскрізна перевірка Settings має пройти реальний Google-вхід у зовнішньому браузері на наявному стандартному GoDaddy hostname: дозволений Власник повертається до захищеної поверхні з окремою локальною сесією, а інший акаунт чи скасований вхід не отримують даних. Прийнята конфігурація Google, локальний mock або HTTP 200 окремо не доводять такого результату. Daily consultation при цьому лишається в Element.
- Негативні та відновлювальні acceptance-результати мають покрити кожний пункт `NFR-016.a`–`NFR-016.h`: неправильні issuer/audience/підпис/строк/ідентичність, відсутню або чужу браузерну транзакцію, replay і одночасне завершення, скасування чи збій Google, підроблену/прострочену/відкликану сесію, підроблену зміну Settings, throttling і відновлення після рестарту. Не допускаються витоки Google/AI credentials, прихований парольний fallback або зміни Codex/Claude OAuth-станів. Конкретні процедури й механізми перевіряють downstream-власники; ця вимога не позначає їх виконаними.
- Contract tests Settings schema мають довести рівно три групи `Codex-агенти`, `Критик`, `Швидкість консиліуму`, незалежні model/effort controls усередині кожної provider group, точні підтверджені версії й порядок Claude `Opus → Sonnet → Haiku` з новішими версіями першими, відсутність free-text model input та extra/per-agent/per-unit overrides.
- Capability-map tests мають охопити підтримувані й непідтримувані пари кожної дозволеної моделі з її власним provider-specific набором рівнів, семантику `За замовчуванням моделі` без explicit effort, unknown/stale capability state і відсутність silent downgrade. Зміна моделі чи рівня Codex не змінює Claude Code, і навпаки; спільна шкала або універсальний mapping між ними не допускаються.
- Speed tests мають довести детерміновану orchestration semantics пресетів `швидко / збалансовано / ретельно` та відсутність Claude Fast Mode, API/PAYG, usage-credit або іншого платного fallback у кожному пресеті.
- Save/reset tests мають довести атомарність: валідний повний set зберігається, будь-яке невалідне поле не змінює жодного persisted value, reset/defaults відновлюють source-backed set, cancel лишає persisted set незмінним, а surface показує фактичні effective values.
- Session tests мають довести, що нова сесія отримує одну validated immutable effective snapshot, активна сесія не змінюється після нового save/reset, а incompatible або unknown config блокує лише старт нової сесії з конкретною причиною.
- Responsive/accessibility tests мають охопити representative mobile і desktop widths, keyboard-only navigation, assistive-technology names/relationships, focus order/visibility, validation/error announcements і plain-text meaning без color-only state.
- Негативні invariant tests мають довести, що жоден model/depth/speed/default/reset path не вимикає critic, A2A, E2EE, verbatim publication, safety/privacy або external-action permission gates.
- Детальні повторювані DoD/eval-гейти належать `docs/dod-evals.md`, а покроковий QA — `docs/qa-checklist.md`; вони перевіряють цей PRD і не створюють нових продуктових вимог.

## 10. Мінімальні наскрізні acceptance-сценарії

### AC-001. Проста пряма відповідь

**Дано:** дозволена E2EE Matrix-кімната, перевірений пристрій власника й чинна одноразова згода.
**Коли:** власник надсилає просте текстове питання.
**Тоді:** система підтверджує запит до 5 секунд, не запускає зайвий консиліум і повертає читабельну пряму відповідь.

### AC-002. Повний консиліум

**Дано:** дозволена Matrix-кімната, складна багатосферна проблема, чинні subscription OAuth власника та доступні квоти Codex-агентів і вибраного провайдера Критика.
**Коли:** власник надсилає запит на консиліум.
**Тоді:** головний консультант добирає 2–5 доречних окремих Codex agent sessions/threads через один керований ChatGPT OAuth-стан без клонованих auth stores, залучає окремий runtime Критика вибраного провайдера через відповідний subscription OAuth, показує першу агентську репліку до 30 секунд, реєструє фактичну A2A-дискусію повністю та видає синтез до 10 хвилин.

### AC-003. Видимість і незмінність чату

**Дано:** активний консиліум із кількома адресованими репліками.
**Коли:** репліки з'являються в Element і сесія закривається.
**Тоді:** користувач бачить дату старту, ролі й `HH:MM`, не бачить технічних ID або сирого Markdown, а архів та експорт містять ті самі повні тіла й порядок повідомлень.

### AC-004. Уточнення, зупинка й нова задача

**Дано:** одна активна сесія.
**Коли:** власник надсилає уточнення, потім `Стоп`, а після цього `Нова задача`.
**Тоді:** уточнення потрапляє в поточний контекст, подальша робота першої сесії припиняється, а нова сесія не успадковує її робочий контекст як активну задачу.

### AC-005. Зображення, PDF і чутливі дані

**Дано:** дозволена Matrix-кімната й перевірений пристрій власника.
**Коли:** власник надсилає звичайне зображення, звичайний PDF, особливо чутливий документ і повідомлення із секретом.
**Тоді:** підтримувані звичайні вкладення обробляються, чутливий документ очікує окремого підтвердження, а секрет не передається агентам і не потрапляє до незмінного журналу.

### AC-006. Сторонній запит і життєвий цикл пристрою

**Дано:** `owner_mxid` або `room_id` поза allowlist, неперевірений пристрій чи перевірений пристрій, який власник після втрати відкликав.
**Коли:** такий відправник або пристрій намагається запустити консультацію чи отримати архів, а власник окремо відновлює доступ на новому пристрої через recovery key і перевіряє його.
**Тоді:** сторонній, неперевірений або відкликаний доступ не запускає агентів і не повертає захищені дані, а новий перевірений пристрій власника працює в дозволеній E2EE-кімнаті.

### AC-007. Прозорий збій і перевищення часу

**Дано:** агент недоступний, subscription OAuth прострочений/відкликаний, quota вичерпано або до завершення стандартного консиліуму потрібно понад 10 хвилин.
**Коли:** система досягає відповідної межі.
**Тоді:** вона прямо називає збій або причину затримки, не вигадує репліку, не переходить на API key/PAYG/usage credits, за потреби дає безпечну out-of-band вказівку повторної авторизації та просить дозвіл продовжити, якщо потрібен додатковий час.

### AC-008. Архів, експорт і видалення

**Дано:** завершена сесія.
**Коли:** власник запитує експорт, намагається видалити окрему репліку, а потім підтверджує видалення цілої сесії.
**Тоді:** експорт відтворює повну сесію, окрема репліка не змінюється, а повна сесія видаляється лише після явного підтвердження.

### AC-009. Витрати

**Дано:** налаштовані місячні платежі ChatGPT/Codex і Claude, фактичні інфраструктурні витрати та активна або завершена сесія з підписними модельними викликами.
**Коли:** власник надсилає `Витрати`.
**Тоді:** система окремо показує налаштовані subscription fees, фактичні infrastructure spend та доступний provider-reported usage/ліміт/reset, позначає AI usage сесії як включене в підписку, недоступне — як невідоме, не вигадує per-session token charge і не купує usage credits автоматично.

### AC-010. Підтримувані клієнти Element

**Дано:** приватна E2EE Matrix-кімната налаштована в підтримуваному клієнті Element, а пристрої перевірені.
**Коли:** надходить нова репліка агента.
**Тоді:** нова репліка з'являється в тій самій приватній кімнаті на Mac, iPhone, Samsung Flip7/Android і Windows PC без окремого консультаційного інтерфейсу.

### AC-011. Фінальний формат і межі дій

**Дано:** консиліум сформував рекомендацію, що містить доречну технічну дію або зовнішній крок.
**Коли:** головний консультант завершує сесію.
**Тоді:** користувач отримує рішення, до трьох дій, ризик і умову перегляду короткими повідомленнями; технічна частина відокремлена, а зовнішня дія не виконується без окремого дозволу.

### AC-012. Google-вхід до Налаштувань власника

**Дано:** наявний стандартний GoDaddy hostname, налаштований Google-клієнт і точна дозволена ідентичність Власника; конфігурація та готовність інтеграції мають бути підтверджені окремо, цей сценарій не стверджує, що вони вже існують.
**Коли:** Власник явно починає Google-вхід у браузері й повертається до застосунку; окремо виконуються сторонній, скасований, підроблений, повторний, одночасний і прострочений вхід, а також вихід, повторна автентифікація та завершення строку сесії.
**Тоді:** (a) тільки дозволена Google-ідентичність у відповідній свіжій транзакції створює окрему локальну сесію й відкриває Settings; (b) усі відмови за `NFR-016.a`–`NFR-016.c` не повертають захищених даних, повторна спроба є новою транзакцією; (c) сесії виконують rotation, idle/absolute expiry та server-side revocation за `NFR-016.d`–`NFR-016.e`, а рестарт не скасовує їхніх обмежень; (d) запити й відповіді виконують `NFR-016.f`–`NFR-016.g` без витоку credentials або прихованого password fallback; (e) застосунок не вимагає власного другого фактора й не видає Google-вхід за доказ MFA; (f) Codex/Claude subscription OAuth не відкривається й не змінюється, а консультаційний UX лишається в Element. Окремий дозволений і заборонений результат потрібен для кожного пункту; один успішний login не закриває весь сценарій.

### AC-013. Три групи, атомарне збереження й повернення значень

**Дано:** чинний Settings set, source-backed defaults, дозволені моделі та одна невалідна зміна.
**Коли:** Власник переглядає effective values, зберігає валідний повний set, пробує невалідний set, скасовує незбережені зміни та повертає defaults.
**Тоді:** surface містить рівно `Codex-агенти`, `Критик`, `Швидкість консиліуму`; перші дві групи мають незалежні модель і рівень міркування відповідного провайдера. Валідний set зберігається атомарно, невалідна спроба не змінює жодного persisted value, cancel не записує зміни, defaults відновлюються як один set, а показані effective values відповідають фактично застосованим. Моделі мають точні підтверджені версії; Claude показано в порядку `Opus → Sonnet → Haiku` з новішими підтвердженими версіями сімейства першими.

### AC-014. Capability validation і семантика швидкості

**Дано:** окремі typed allowlists Codex/Claude з підтвердженими назвами й версіями, capability map власних рівнів кожної моделі, supported, unsupported та unknown комбінації й три speed presets.
**Коли:** Власник змінює модель або рівень одного провайдера, окремо повторює дію для другого та запускає нову сесію з кожною комбінацією.
**Тоді:** для кожної моделі застосовується лише її підтверджений рівень, а `За замовчуванням моделі` не передає explicit effort; значення іншого провайдера не змінюються. Unsupported або unknown комбінація блокується без silent downgrade чи перенесення рівня між провайдерами; `швидко / збалансовано / ретельно` змінюють лише orchestration behavior і не вмикають Claude Fast Mode, API/PAYG або usage credits; critic/A2A/E2EE/verbatim/safety/privacy/permission invariants зберігаються.

### AC-015. Immutable effective snapshot сесії

**Дано:** активна сесія зі snapshot A та інший валідний Settings set B.
**Коли:** Власник зберігає B під час активної сесії, а після її завершення запускає нову сесію; окремо він намагається стартувати з incompatible set C.
**Тоді:** активна сесія до кінця використовує A, наступна сесія отримує один immutable snapshot B, а C fail closed блокує старт до виправлення без часткового застосування.

### AC-016. Responsive й доступна Settings surface

**Дано:** `Налаштування власника` на representative mobile і desktop widths та доступ через keyboard/assistive technology.
**Коли:** Власник проходить усі три групи, читає effective values, отримує validation error і виконує save/cancel/reset.
**Тоді:** content і controls лишаються читабельними й operable без горизонтально прихованої критичної дії, мають програмні labels, логічний focus, доступне повідомлення про помилку та plain-text meaning без залежності лише від кольору.

## 11. Поза межами V1

- публічний чатбот, реєстрація інших користувачів, ролі команди або багатокористувацький SaaS;
- використання підписок власника для клієнтів, працівників або інших третіх осіб, спільний доступ, перепродаж чи будь-яка SaaS/комерційна модель на цих credential;
- будь-яка web surface, крім вузьких responsive `Налаштувань власника`; зокрема браузерний чат, live-preview, архів, live execution status або повсякденний дашборд;
- локальний owner password чи його резервне використання, OTP, magic link, інший IdP замість Google, власний другий фактор застосунку, публічна реєстрація, додаткові права на дані Google або Settings access без чинної локальної сесії;
- free-text model input, settings group поза трьома підтвердженими, sound/motion settings, per-agent чи per-session-unit override;
- WhatsApp, Meta WhatsApp Cloud API, Telegram, Slack, iMessage або сторонній конструктор чатботів;
- власний VPS, самостійне адміністрування Matrix homeserver або локальний Mac як production-сервер;
- окремі Matrix-акаунти для кожної ролі агента;
- власний звук повідомлень поверх нативних сповіщень Element;
- голосові повідомлення, аудіо, відео й інші не підтверджені типи вкладень;
- кілька паралельних консиліумів у тій самій Matrix-кімнаті;
- жорсткі ліміти вартості сесії або місяця;
- `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, API/PAYG-білінг, автоматичні usage credits або будь-який прихований fallback із subscription OAuth;
- Claude Fast Mode або інший платний speed mode, замаскований під orchestration preset;
- клонування `auth.json`, окремі копії OAuth-cache чи інші дубльовані credential stores для створення окремих Codex-агентів;
- автоматичні зовнішні дії без окремого дозволу;
- показ прихованих внутрішніх міркувань моделей, системних промптів або технічних логів;
- монетизація, білінг користувачів, доступ клієнтів або інша комерційна модель продукту;
- детальна архітектура, дизайн екранів, wireframes, реалізаційні юніти й порядок розробки.

## 12. Відкладені рішення та відкриті питання

Попереднє питання про password-only/MFA замінене прямим рішенням `PI-AUTH-20260905`: Google-вхід без власного другого фактора застосунку в цьому MVP. Його ризик, компенсувальні вимоги й умова перегляду визначені в `Security Requirements`; повторної відповіді Власника не потрібно. Google Console прийняла й зберегла точну адресу повернення `https://wy2v0putg6.c35.airoapp.ai/auth/google/callback` у наявному клієнті проєкту `personal-ai-consulting-group`, дозволений домен `airoapp.ai` і лише декларації `openid` та `https://www.googleapis.com/auth/userinfo.email`. Це не підтверджує володіння доменом чи живий вхід: захищене надання credentials не завершене, а за спостереженням `2026-09-05T03:15:30Z` живий `/auth/google/start` повертав HTTP 404. Набрання чинності конфігурацією, обмін кодом і створення локальної сесії Власника залишаються неперевіреними; нового клієнта/секрету не створено, код і deployment не змінено. Зовнішні зміни потребують свого чинного дозволу. Ця специфікація не підтверджує реалізовану MFA або повну ASVS-відповідність. Інші рішення відкладено до відповідних downstream-власників:

- точні моделі Codex і Claude;
- typed allowlist contents, capability-map source/version, provider-specific reasoning mapping і source-backed defaults для трьох Settings groups;
- точний Google OIDC flow, безпечна прив'язка дозволеної ідентичності, транзакція входу, session signing/expiry/revocation, CSRF, throttling, storage schema та persistence protocol; вони не можуть додати інший login method, парольний fallback, власний другий фактор застосунку, новий домен чи права Google або порушити owner-only/atomic save/effective snapshot;
- версію та конкретну реалізацію протоколу A2A;
- захищене сховище одного ChatGPT OAuth-стану, Claude subscription setup-token і конкретний out-of-band flow повторної авторизації; ці рішення не можуть додати API/PAYG fallback або передавання токенів у Matrix/agent context;
- сховище, формат архіву, керування ключами, черги, повторні спроби та відновлення після збою;
- точні грошові ліміти після накопичення фактичної статистики `Витрати`; вони не входять у V1 і не змінюють subscription-cost semantics;
- можливе додавання голосових, відео, інших файлів і паралельних сесій після перевірки V1;

Жоден із цих пунктів не може мовчки змінити підтверджену межу V1.

## PI-CONSENSUS-20260908 — Персональні ролі, делегування й автономний Matrix

Джерело — однойменне уточнення в `docs/product-idea.md`; поведінка уточнює UC-001 (JOB-001), UC-002 (JOB-002) і збереження запису UC-005 (JOB-004). У межах наведених пунктів замінює попередні FR-009–014/025/034/036 та реалізаційну фіксацію одного раунду; решта їхніх зобов'язань чинна. Нової поверхні, провайдера чи права не додає.

| Зобов'язання | Спостережувана вимога |
|---|---|
| FR-047.a | У UC-001/UC-002 кожна роль, адресат і позначення Head Consultant/Critic мають сталу англійську назву; автоматичне службове повідомлення не видається за модельну репліку. |
| FR-047.b | Імена мають різні читабельні кольори за підтвердженої підтримки клієнта; там, де цього не можна забезпечити, перед іменем є доречний сталий емодзі. Текстова назва обов'язкова, колір не є єдиним носієм ідентичності. |
| FR-048.a | UC-001 визначає мову з першого змістовного повідомлення задачі; явний вибір мови має пріоритет. За нерозпізнаваного вводу ставить одне уточнення без запуску зайвих спеціалістів. |
| FR-048.b | Мова сесії передається всім дорученням, спеціалістам, Critic, фіналу й службовим повідомленням UC-001/UC-002 та зберігається при відновленні. Цитати, документи й короткі команди її не змінюють; явне прохання Власника — змінює для наступних реплік, не переписуючи історію UC-005. |
| FR-049.a | У UC-001 ситуаційний пул розширено рівно сімома ролями з PI-CONSENSUS-20260908; зберігаються 2–5 потрібних спеціалістів і чинні обмеження чисельності пресетів. Не запускати весь пул автоматично. |
| FR-049.b | Нові ролі є ШІ-коучингом/освітою в професійних межах AGENTS.md, не клінічною, ліцензованою психотерапевтичною чи інвестиційною послугою. Не ставити діагнозів, не призначати лікування, не гарантувати прибуток, не збирати медичні записи/секрети; особисті коучингові питання потребують згоди. За кризи зупинити звичайний консенсус і надати доречну людську допомогу, без удаваного контакту зі службою. |
| FR-050.a | Head Consultant у UC-001 формує структуроване розкладання запиту: ціль, відмінні фахові підзадачі, виконавці, очікувані результати, доречні факти, обмеження й залежності; вибрані підзадачі разом покривають запит, а дублювання має явне обґрунтування. |
| FR-050.b | Кожен спеціаліст отримує саме своє конкретне зареєстроване доручення. Спільний вступ публікується один раз; повні відмінні доручення й відповіді видно в чаті. Однаковий шаблон усім не задовольняє вимогу. |
| FR-051.a | Critic у UC-001 адресно допомагає кожному спеціалісту покращити поточну пропозицію: суттєве зауваження, причина/доказ і умова його закриття; спеціаліст відповідає виправленням або обґрунтованою незгодою. Згода дозволяє припинити зайві раунди. До першої незалежної позиції чужі висновки не передаються. |
| FR-051.b | Не більше семи фактичних повідомлень Critic на одного спеціаліста за одну задачу, включно з явним схваленням. Потрібні стійкі лічильники й ідемпотентність: перезапуск, retry, Stop/Continue або нова generation тієї самої задачі не обнуляють бюджет. |
| FR-051.c | Консенсус потребує явних підтверджень Head Consultant, усіх вибраних спеціалістів і призначеного Critic щодо однакової поточної спільної пропозиції та актуальних фахових позицій. Зміна змісту, даних або висновку скасовує залежну згоду. Фінальний текст не може додати нове несхвалене рішення. Мовчання, помилка, рольова назва чи старий receipt не приймаються. |
| FR-051.d | Якщо ліміт вичерпано до згоди, повернути підсумок нерозв'язаних розбіжностей, відсутніх доказів і безпечного наступного кроку з явною позначкою «консенсус не досягнуто» мовою сесії. Не називати це узгодженим фіналом. UC-002 Stop, десятихвилинний дозвіл продовження, квоти й заборона платного fallback мають пріоритет. |
| FR-052.a | Після первинного налаштування UC-001/UC-002 працюють від повідомлення в дозволеній Matrix-кімнаті без відвідування Settings, її cookie або локального комп'ютера. Початкове приймання, синхронізація й відновлення запускаються службою самостійно. |
| FR-052.b | Тимчасові мережеві/I/O збої мають автоматичне обмежене повторення з паузами; порушення довіри, пошкодження/втрата сховища, відкликаний доступ відрізняються й безпечно блокують залежну роботу. Відновлення не створює нового пристрою, не скидає ключів/історії й не ігнорує ізоляцію Preview. |
| FR-052.c | Після простою, перезапуску й повторної публікації одна допустима Matrix-подія запускає не більше однієї консультації. Зберігаються курсор, підтверджена черга й захищений наявний crypto store; дублікати/чужі/непідтверджені події не запускають моделей. |
| FR-052.d | Якщо використано HTTP-сповіщення Matrix, воно є лише сигналом для перевірки справжніх подій, а не авторизованою командою. Захист від підробки, повторів, витоку секретів і виснаження ресурсів обов'язковий; приймання сигналу не відкриває Settings, не змінює довіри й не обходить перевірку Власника/кімнати/пристрою. Нового платного або локального сервісу немає. |

**Уточнений шлях UC-001:** (1) прийняти й перевірити подію незалежно від веб-входу; (2) зафіксувати мову та режим; (3) Head Consultant визначає склад і різні підзадачі; (4) незалежні первинні позиції; (5) адресовані конструктивні критика/відповіді; (6) спільна пропозиція й явні підтвердження актуального змісту; (7) узгоджений результат або чесне завершення без консенсусу; (8) UC-005 зберігає повний запис. UC-002 коригує задачу з інвалідацією застарілих погоджень; Stop/продовження не дозволяють перевищити лічильник. Склад, версія протоколу, мова, прогрес і лічильники відновлюються без фальшивих повторів.

**Приймання:** приклади англійською й українською та явний вибір іншої мови; цитата іншою мовою; ситуаційний добір кожної нової ролі без заяви про людську кваліфікацію; різні доручення замість шаблону; ранній консенсус; суттєве виправлення; незгода після сьомого повідомлення; новий факт після схвалення; збій/перезапуск без скидання лічильника; Matrix-повідомлення після простою й після публікації при закритих Settings. Це визначені сценарії, ще не виконані тести.

**Безпекове уточнення:** зберігається ASVS 5.0.0 L2 як ціль вимог, не сертифікація. NFR-005/006/009/011 охоплюють додаткову чутливість особистих тем, мінімізацію/згоду й заборонені дані; NFR-007/008 — незмінні ідентичності й недовірені повідомлення/HTML; NFR-010/017/019 — стійкі ліміти, атомарність і один виконавець; NFR-012 — діагностику без секретів і безпечні класи відновлення. Цільово перечитані каталожні v5.0.0-1.3.6, 2.3.1–2.3.4, 8.2.2, 14.1.1, 14.2.1, 16.5.1, 16.5.3; попередня оцінка інших розділів збережена, не названа новим аудитом. Клінічні/кризові межі FR-049.b є додатковим продуктово-безпековим правилом, не вигаданим ASVS control. Налаштування, OAuth та право доступу не послаблюються новими рольовими назвами або сигналом пробудження.

## 13. Джерела та покриття

| Джерело | Що підтверджує | Використання в PRD |
|---|---|---|
| `docs/product-idea.md` | Актуальний намір, Element/Matrix, Candidate B, V1, GoDaddy Node hosting, Google-вхід за `PI-AUTH-20260905` без власного другого фактора застосунку, GoDaddy/MySQL equivalents, subscription OAuth, private/non-SaaS boundary, дані, витрати, команди, незалежні provider groups і orchestration speed, точні підтверджені версії/порядок моделей і validation/save/reset/effective/snapshot semantics | Основне джерело всіх продуктових меж і acceptance-сценаріїв, включно з `US-024`–`US-029`, `FR-037`–`FR-046`, `NFR-016`–`NFR-019`, `AC-012`–`AC-016` |
| Поточне пряме рішення `PI-AUTH-20260905`; product-idea invocation `6c04877e-6bb8-48ce-aa78-92be9b8e4fdd`; попередній PRD invocation `1e0f7892-70e6-42e2-9da6-f58e26d7023c` | Google-вхід лише дозволеного Власника, без власного другого фактора застосунку, резервного пароля, додаткових користувачів/прав і зміни стандартного GoDaddy hostname. Час поточного повідомлення не реконструюється. | Auth-частини §2–4, `US-024`, `FR-038`–`FR-039`, `NFR-006`, `NFR-016.a`–`NFR-016.h`, §8–9, `AC-012`, exclusions і визначений ASVS-виняток; історичні auth-вимоги нижче збережені як історія |
| Поточне узгодження invocation `02e7ca71-8b98-4fe7-9193-946486986952`; `docs/product-idea.md` SHA-256 `852758d725831ebb435da8ffd9a548a6ea1ad70fbab13261885a4793a12ad14e`; попередній `docs/prd.md` SHA-256 `2d783713f3ef232c02f150996037d20635554c686f467d179b3a8f36a49573c7` | П'ять нормалізованих `JOB-*` та вже визначені актори, вимоги, відмови й acceptance-результати; продуктові рішення й обов'язки безпеки не змінені | Шість `UC-*` у §4.3 із повним покриттям `JOB-001`–`JOB-005`; downstream-документи не використано як джерело вимог |
| `forge/google-owner-login-investigation-20260905.md`: `### Authorized existing-project update — 5 September 2026, 03:15 UTC`, SHA-256 `8d6dcc66755e80e5fb67920b31e0b45c6d21a5c0ae17c698a910dcb813ac1676` | Збережені callback, authorized domain і декларації прав наявного Google-клієнта; незавершене надання credentials, незмінені код/deployment і HTTP 404 живого start-маршруту на зазначений час | Актуалізація меж доказів `UC-003`, `Security Requirements` і §12; не доказ чинності інтеграції, успішного входу або реалізації |
| `forge/google-owner-login-investigation-20260905.md`: `## What actually happened` SHA-256 `64ea68c2eb22ad325eb4e64cce0a7831b889679d88ac71d01fa1cd7bfbd5189b`; `## Reproduced application defect` SHA-256 `8c2ef1c7764acc1de4bb3cdbf1abff6a01b4fbd38f72b98f86e54a2674f2a1cc`; `## What Google's current documentation establishes` SHA-256 `bff74667e47f11fd5afa0c88821d37d70787f9b67a8f2dacc070ac40738fc96d` | Історична заміна прямого Google OIDC паролем, помилкова категорична оцінка домену та відтворювана залежність старого адаптера від proxy-origin. Це evidence/research, а не підтверджений продуктовий намір чи доказ живого Google-входу. | Передумови інтеграції й межі доказів `FR-038`, `NFR-016`, §9 та `AC-012`; пізніші браузерні доповнення звіту цими фрагментами не охоплено |
| Пакет OWASP ASVS 5.0.0, pinned JSON SHA-256 `bcdbec214d70abcfad9284a31d4f9e5134305831d628aad3aa85d7e26626cb35` | Точні прочитані контролі V6–V10, V13–V14, V16 і чинні browser-controls для auth-уточнення; оцінка застосовності назв усіх V1–V17 | Окремі auth-обов'язки та документоване послаблення `v5.0.0-6.3.3`; збережені не пов'язані з auth mappings не видаються за повторний повний аудит |
| Виправлення користувача 02.09.2026 та product-idea invocation `e116a6de-4e8c-4b0e-b93c-ea77002a4144`; PRD invocation `676b12e7-1d7a-4e5c-831a-bb88c1493e50` | Вказівка «розділи двох провайдерів і розділи їх налаштування», уточнення порядку Claude та окремих версій моделей. Первісне групування `Моделі / Глибина міркування / Швидкість` і спільну шкалу від 16.08.2026 скасовано цим виправленням; історичне рішення не є чинною вимогою. Згадка моделі не доводить її runtime availability. | Вузьке узгодження §3.6, `US-026`, `FR-040`–`FR-043`, §8–9, `AC-013`–`AC-014` із поточним продуктовим наміром; решта меж, ID й оцінка `Security Requirements` збережені без зміни |
| Owner-вказівка invocation `4f5ce7bd-6f8a-47d3-be6a-6e2a422928e6` | Bounded PRD revalidation after the GoDaddy product-idea reconciliation, stable existing IDs, ASVS 5.0.0 Level 2 review and no test-completion claim | Hosting/auth/storage reconciliation, `Security Requirements`, validation scope |
| Owner-вказівка invocation `29a3dde9-6a4a-4f28-87be-4aa558d73ec8` | Bounded product-idea reconciliation to GoDaddy Node, local owner password and GoDaddy/MySQL equivalents without destructive cleanup | Історично спожитий намір; password-частину замінено `PI-AUTH-20260905`, решта меж збережена |
| Owner-вказівка invocation `81baa3f2-39b5-45d2-8c1c-238ebd66cdbb` | Попередня bounded PRD update для Google/JWT access; hosting/access частина скасована новішою owner-вказівкою, responsive/accessibility і stable traceability збережені | Історичне джерело `NFR-018` і testing evidence boundary, не чинна auth вимога |
| `AGENTS.md` | Роль консультанта й коуча, маршрутизацію простих/складних запитів, дисципліну правди, формат дій і межі дозволів | FR-008, FR-027–FR-030, FR-035–FR-036 |
| `CONSILIUM.md` | Реальний мультиагентний склад, незалежний перший прохід, дискусію, реєстратора, незмінність і синтез | FR-009–FR-018, FR-023, FR-025, FR-029–FR-031 |
| `WORKING_CONTEXT.md` | Підтверджену персональну модель, професійну українську та термін `лійка продажів` | FR-036 і термінологія |
| `README.md` | Початкове позиціонування та наявність попереднього browser-preview | Контекст проблеми; surface-рішення замінене актуальнішим `docs/product-idea.md` |
| Поточний код і `tests/consilium-chat.test.mjs` | Збереження дослівного тіла, очищення ролі від технічних ID і безпечне форматування Markdown | Лише evidence limits та RE-похідна FR-019; не доказ Element/Matrix V1 |
| `DAS Forge 4` | Pattern evidence для typed allowlists, validated atomic save, defaults/reset та effective values | Не є product scope, UI baseline або джерелом додаткових settings groups/features |


Цільове уточнення 06.09.2026: `PI-CRITIC-20260906` замінює лише Claude-only рольову прив'язку на явний перемикач Claude/Codex; історичні рішення вище не відновлюють скасовану обов'язковість провайдера.
