# User Journey

- Продукт: `Personal Consultant`
- Версія шляху: V1
- Дата актуалізації: 06.09.2026
- Робоча мова: `uk`
- owner_invocation_id: `user-journey-critic-router-20260906`

## Source References

Поточне узгодження 06.09.2026 стосується лише PI-CRITIC-20260906; його межі визначено в розділі «Маршрутизатор Критика — узгодження 06.09.2026», поточні спожиті фрагменти та hashes — у manifest. Збережені нижче таблиці джерел і датовані спостереження попередніх переглядів є історією, а не новим full-source, runtime або test evidence.

Порядок джерел відповідає `docs/guardrails.md`: актуальне продуктове рішення має перевагу, `docs/product-idea.md` визначає намір V1, `docs/prd.md` — нормалізовану поведінку, `docs/project-context.md` — підтверджений контекст, а `docs/canonical-terms.md` — мову без зміни поведінки.

- Успадковані посилання `PI` на `docs/product-idea.md` з попередньої версії цього документа збережено для незмінених етапів; під час цього узгодження їхню поведінку звірено з чинним PRD, а не використано як нове окреме джерело.
- `docs/prd.md`: розділи 1–5; `JOB-001`–`JOB-005` через мапування §4.3; `UC-001`–`UC-006`; `US-001`–`US-029`; `FR-001`–`FR-046`; `NFR-001`–`NFR-019`, зокрема `NFR-016.a`–`NFR-016.h`; розділи 8–12; `AC-001`–`AC-016`.
- `docs/project-context.md`: «Цільові користувачі», «Основні проблеми користувача», «Бажані результати користувача», «Цільові платформи», «Основні сценарії», «Межі V1», «Поза межами V1», «Обмеження», «Припущення», «Ризики» та «Відкриті питання».
- `docs/canonical-terms.md`: `Власник`, `Запит`, `Сесія`, `Консиліум`, `Дослівний консиліум`, `Підтверджена репліка агента`, `Фінальна рекомендація`, `Технічна частина`, `Приватна Matrix-кімната`, `Дозволена Matrix-пара`, `Перевірений Matrix-пристрій`, Matrix/data/archive terms, агентські OAuth-терміни, `Налаштування власника`, `Google-вхід Власника`, `Дозволена Google-ідентичність`, `Локальна сесія Власника`, `GoDaddy Node runtime`, `MySQL-сховище`, `Stateless Preview`, `Гейт незворотного очищення`, `Codex-агенти`, `Критик`, `Міркування Codex`, `Міркування Claude Code`, `Швидкість консиліуму`, `Пресет швидкості`, `Фактичні налаштування сесії`; дії `Стоп`, `Нова задача`, `Витрати`, експорт і видалення; продуктові стани та канонічні назви потоків.
- `docs/guardrails.md`: `Source Of Truth Order`, `AI Autonomy Boundaries`, `Forbidden Changes`, `Scope Boundaries`, `When To Ask`, `When To Stop`, `Verification Rules`, `Evidence Requirements` та `Open Questions`.
- Узгоджена upstream-межа: наявний GoDaddy Node.js application і MySQL є чинними runtime/storage; Google підтверджує особу єдиного Власника, після чого застосунок створює окрему локальну сесію; власної MFA застосунку й резервного пароля немає. Використовується штатний GoDaddy hostname без перенесення домену. Cloudflare Worker/DO/R2/Access допустимі лише як historical superseded context. HappyPro не входить до journey, а destructive legacy cleanup лишається за окремим JIT-дозволом.
- Успадковані посилання `RE` на `README.md` з попередньої версії стосуються лише початкового позиціонування, принципу «результат → діагностика → рішення → дія → перевірка» та мінімального контексту першої консультації. README не перечитано як нове джерело; історичний browser-preview не використано як поверхню V1.

Скорочення трасування нижче: `PI` — `docs/product-idea.md`; `PRD` — `docs/prd.md`; `PC` — `docs/project-context.md`; `CT` — `docs/canonical-terms.md`; `GR` — `docs/guardrails.md`; `RE` — `README.md`.

Джерела цього узгодження, SHA-256: `docs/prd.md` — `a45aa866bd0591ba778b8ddf1528f9789fd954eef86dd2d8109c8db3f0ed23cc`; `docs/project-context.md` — `458da1092f8ac6b10fca6aadc33fab6b9b56aef650fff23e814fe84cb15735a4`; `docs/canonical-terms.md` — `dd4a7ef9ee403f941b649247b14eafff044af717d54fb8731b0d2d26665848d3`; `docs/guardrails.md` — `6b88dcd634b03203f9f8dde93bd4e4abdba3fe57205b5f3bb776106b39bd999b`. Попередній `docs/user-journey.md` — `dda85ac0e9a82152aa0c7b121c624341e2628381fa8d1aff2ffe7fe4b85b0e77`, invocation `6b63b10a-0d12-4280-9535-4aa115266fcb`.

Поточне пряме підтвердження Власника визначило єдиний дозволений Google-акаунт; точне значення лишається захищеною deployment-конфігурацією й не наводиться в репозиторії. Попередня невизначеність вибору акаунта для цього шляху закрита; сам факт відповіді не доводить налаштованої прив'язки чи живого доступу за `NFR-016.b`. За §12 PRD збережені параметри Google Console не дорівнюють успішному входу: credentials ще не були завершені, а спостереження `2026-09-05T03:15:30Z` зафіксувало HTTP 404 живого початку входу. Цей документ не стверджує нової перевірки або deployment.

## Primary User

`Власник` — основний і єдиний користувач V1, джерело запиту, остаточний носій рішення та дозволу. Окремих клієнтських, командних чи адміністративних ролей у V1 немає. Власник користується штатним Element на Mac, iPhone, Samsung Flip7/Android або Windows PC. `[PI: «Основний користувач і проблема», «Межі V1»; PRD: §3.1, US-001–US-002; PC: §3, §7; CT: «Власник»]`

## Named Protagonist

`Власник` — рольова назва протагоніста. Особисте ім'я джерела не підтверджують, тому воно не вигадується. `[PC: §3; CT: «Власник»; GR: Source Of Truth Order]`

## User Goal

Власник хоче в одній приватній invite-only E2EE Matrix-кімнаті в Element перетворити практичну проблему на рішення: для простого запиту — швидко отримати пряму відповідь, для складного або високоризикового — побачити справжню дискусію окремих агентів і отримати одну синтезовану фінальну рекомендацію з не більш як трьома наступними діями, ризиками та умовою перегляду. `[PI: «Результат для користувача», «Принципи продукту»; PRD: §2, US-005–US-017, US-022; PC: §2, §6]`

Окрема рідкісна ціль — за потреби безпечно змінити лише `Codex-агенти`, `Критик` та `Швидкість консиліуму` майбутніх консультацій у responsive `Налаштуваннях власника`, переконатися, що набір сумісний і застосовуватиметься лише до наступної сесії, а потім повернутися до Element. `[PRD: US-024–US-029, FR-037–FR-046; PC: §6–8; CT: «Налаштування власника»]`

Мотивація — ухвалити краще практичне рішення й перейти до перевірюваної дії, не витрачаючи увагу на технічні журнали, приховану імітацію консиліуму або самостійне узгодження суперечливих агентських висновків. `[PI: «Основний користувач і проблема»; PRD: §1–2; PC: §2, §5]`

## Starting Context

- Власник має бізнесове, фінансове, управлінське, підприємницьке, продуктове, маркетингове, продажне або інше практичне питання й відкриває одну `Приватну Matrix-кімнату` в штатному Element на підтримуваному пристрої. Фізичне місце користування джерела не визначають. `[PI: «Основний користувач і проблема», «Основний сценарій»; PRD: §1, §4, US-001, US-022; PC: §3, §7]`
- Під час першого використання Власник проходить нативні Element/Matrix-кроки запрошення до кімнати, відновлення за recovery key за потреби та перевірки пристрою; окремого продуктового onboarding-екрана джерела не визначають. У кімнаті є лише один Matrix-акаунт Власника й один Matrix-акаунт бота на `matrix.org`. `[PRD: US-002, FR-001, NFR-013; GR: Scope Boundaries, When To Stop]`
- Запит може бути текстом, зображенням або PDF і приймається лише з точної `Дозволеної Matrix-пари` `room_id` + `owner_mxid` та перевіреного, не відкликаного Matrix-пристрою. Одночасно може існувати не більше однієї `Активної сесії`. `[PRD: §3.3, FR-001, FR-003, NFR-007; PC: §8–9; CT: «Запит», «Дозволена Matrix-пара», «Активна сесія»]`
- На першому використанні ще може не бути `Згоди на обробку даних`; під час подальшого використання одноразова згода вже чинна, але не замінює окремого підтвердження для `Особливо чутливого документа`. `[PI: «Правила даних і доступу»; PRD: FR-002, FR-006, AC-005; CT: відповідні терміни]`
- До робочого запуску Власник поза Matrix один раз завершує захищену provider-managed авторизацію: Codex отримує один авторитетний ChatGPT OAuth-стан зі штатним refresh, а Claude-гілка Критика — subscription OAuth token від `claude setup-token`. Це інфраструктурна передумова, а не окрема продуктова поверхня; token, setup-token, `auth.json`, refresh state, URL або code повторної авторизації ніколи не надсилаються в кімнату. `[PI: «Правила даних і доступу»; PRD: §3.5, NFR-006; CT: «Codex OAuth-стан», «Claude Code OAuth token»; GR: When To Ask]`
- Для Власника важливі швидкість, конфіденційність, видимий реальний поступ, якість доказів, контроль витрат і право зупинити або змінити задачу. `[PI: «Основний користувач і проблема», «Стандарт швидкості»; PRD: US-010–US-013, US-017–US-021; PC: §5–6]`
- Якщо Власник вирішує змінити параметри наступної консультації, він виходить із щоденного Element-потоку до єдиної вузької responsive web surface `Налаштування власника` на штатному GoDaddy hostname. Явно початий Google-вхід повертає його в тій самій вкладці до застосунку; лише перевірена дозволена ідентичність створює окрему чинну локальну сесію. Реєстрація, резервний пароль, власний другий фактор застосунку чи інший провайдер входу не є альтернативними гілками. `[PRD: UC-003, FR-038, NFR-016.a–NFR-016.h; GR: Scope Boundaries, Forbidden Changes]`

## Stakes And Constraints

- Бізнес-дані проходять через `matrix.org`, GoDaddy hosting, OpenAI та Anthropic, тому перша звичайна обробка потребує одноразової згоди, а контекст агентів має бути мінімізовано. Matrix E2EE захищає зміст між перевіреними пристроями, але `matrix.org` бачить службові метадані; E2EE завершується на verified Matrix device бота в GoDaddy Node.js runtime, де розшифрований зміст передається OpenAI та, лише для Claude-маршруту, Anthropic через TLS. `[PRD: FR-002, FR-004, NFR-005, NFR-008; GR: AI Autonomy Boundaries, Scope Boundaries]`
- Кімната має залишатися invite-only з E2EE, рівно двома joined-учасниками без інших pending invites, history visibility `joined`, без public address/listing, guests, bridges і widgets. Публічний `matrix.org` не надає Власнику server-level контролю federation, тому V1 не покладається на `m.federate: false`. `[GR: Forbidden Changes, When To Stop]`
- Production працює на `matrix.org` і в наявному GoDaddy Node.js application з MySQL на штатному hostname, а не на Cloudflare Worker/DO/R2, платному Matrix-hosting, власному Matrix homeserver чи локальному Mac. Це hosting-рішення, не перенесення домену. Репозиторій HappyPro не є частиною шляху й не змінюється; destructive cleanup старих ресурсів не є користувацькою дією та потребує окремого JIT-дозволу. Продукт не створює власного звуку; сповіщення визначають штатні налаштування Element та операційної системи. `[GR: Forbidden Changes, Scope Boundaries]`
- `Секрет` не приймається, не передається агентам і не входить до незмінного журналу, архіву чи експорту. `[PRD: FR-005, AC-005; CT: «Секрет»; GR: Forbidden Changes, When To Stop]`
- Власник має відрізняти реальний `Консиліум` від одного монологу, оформленого як кілька ролей; видимість кількох ролей сама по собі не є доказом. `[PRD: FR-011–FR-016, NFR-010–NFR-012; GR: Evidence Requirements]`
- Стандарт часу має чотири окремі межі: підтвердження до 5 секунд, перша видима агентська репліка до 30 секунд, не більш як 60 секунд без змістовного оновлення або пояснення очікування, фінальна рекомендація стандартного консиліуму до 10 хвилин або запит дозволу продовжити. `[PI: «Стандарт швидкості»; PRD: NFR-001–NFR-004; GR: Verification Rules]`
- Єдиний дозволений режим роботи агентів — `Subscription OAuth` платних підписок Власника. Один захищений `Codex OAuth-стан` не об'єднує агентів: Головний консультант і кожен спеціаліст працюють в окремій реальній Codex-сесії/треді. Claude-гілка Критика має окремий subscription OAuth/setup-token. За недійсного auth mode, expiry, revocation, refresh failure або вичерпаної квоти залежна робота fail closed; API key, PAYG і usage credits не є резервом. `[PI: «Модель використання підписок і витрат»; PRD: §3.5, FR-009–FR-010, FR-030; CT: відповідні внутрішні терміни; GR: When To Stop]`
- `Облік витрат` не є token-cost сесії. V1 показує налаштовані місячні платежі ChatGPT/Codex і Claude, фактичні інфраструктурні витрати та доступний provider-reported usage/ліміт/reset; підписне AI-використання позначається як `входить у підписку`, а недоступне значення — як `невідомо`. Непідтверджений жорсткий грошовий ліміт не вводиться. `[PI: «Модель використання підписок і витрат»; PRD: FR-033, AC-009; CT: «Облік витрат»]`
- V1 є приватною одноосібною не-SaaS системою. Запити ініціює й результати отримує лише Власник; його підписки не обслуговують клієнтів, працівників або інших третіх осіб. Зміна eligibility чи правил провайдера блокує залежний запуск до окремого перегляду. `[PI: «Модель використання підписок і витрат»; PRD: §3.1, §8; GR: Scope Boundaries, When To Stop]`
- Агенти аналізують і рекомендують. Зовнішня або високоризикова дія, а також особисті коучингові запитання про внутрішні конфлікти потребують окремого явного дозволу Власника. `[PRD: §3.4, FR-035; GR: When To Ask]`
- `Налаштування власника` не є чатом, архівом, live status або щоденним дашбордом і не показують Google/локальні session secrets або жодних Codex/Claude OAuth token, setup-token, `auth.json`, refresh state чи reauth material. Google-сесія, `Локальна сесія Власника` й `Subscription OAuth` мають незалежні життєві цикли; вхід, відмова, вихід і повторний вхід до налаштувань не змінюють AI OAuth. Окремо дозволене захищене runtime-налаштування підписок не заборонене цією межею, але не передає секрети через значення Settings, клієнт або дані локальної сесії. `[PRD: UC-003, FR-039, NFR-006, NFR-016.d–NFR-016.f; GR: Forbidden Changes, When To Stop, Verification Rules]`
- Settings мають рівно три групи. `Codex-агенти` та `Критик` мають незалежні typed model+effort allowlists з provider-specific capability validation без silent downgrade; `Швидкість консиліуму` має лише `швидко / збалансовано / ретельно`. Жодний пресет не є Claude Fast Mode, API/PAYG або usage credits і не послаблює critic/A2A/E2EE/verbatim/research/safety/privacy/permission інваріанти. `[PRD: FR-040–FR-046; GR: Forbidden Changes, Scope Boundaries]`

## Journey Overview

Нативне запрошення до `Приватної Matrix-кімнати` й перевірка пристрою → exact allowlist `room_id` + `owner_mxid` і room invariants → перша згода або перевірка чинної згоди → текст / зображення / PDF → перевірка меж даних → підтвердження прийняття до 5 секунд → вибір `Прямої відповіді` або `Дослівного консиліуму` → передзапускова перевірка subscription auth mode і квоти потрібних провайдерів → окремі реальні агентські сесії або видимий fail-closed стан → фінальна рекомендація чи чесний неповний результат → завершення та `Архів сесій` → повний експорт або підтверджене видалення цілої сесії.

Під час `Активної сесії` Власник може уточнити задачу, отримати `Облік витрат`, зупинити роботу командою `Стоп` або закрити поточну сесію й почати іншу командою `Нова задача`. Ці дії не створюють паралельних сесій. `[PRD: §3.3–4, FR-020–FR-022, FR-033; CT: §4–6]`

Окремий короткий шлях налаштувань запускається лише за потреби: Element → `Налаштування власника` на штатному GoDaddy hostname → явний Google-вхід і повернення в тій самій вкладці → окрема `Локальна сесія Власника` → перегляд чинних і стандартних значень → зміна трьох дозволених груп із перевіркою сумісності → атомарне збереження або повернення стандартного набору → підтвердження «лише для наступної сесії» → повернення до Element. Скасування чи збій входу не відкривають налаштувань; повторна спроба є новим входом. Сам Google-вхід доступний до готовності модельних каталогів і AI-підписок; непідтверджені параметри блокують збереження й нову консультацію, а не вхід. Цей шлях не замінює й не перериває основну консультаційну подорож. `[PRD: UC-003, UC-004, AC-012–AC-016; GR: Scope Boundaries, Verification Rules, Source Access Failures]`

Покриття підтверджених завдань і сценаріїв нижче посилається на визначення PRD, а не створює нових вимог або дублює їхні повні шляхи:

| Завдання | Сценарій PRD | Покриття цього шляху |
|---|---|---|
| `JOB-001` | `UC-001` | Етапи 1–6A/6B, 8–9: пряма відповідь або справжній консиліум |
| `JOB-002` | `UC-002` | Етап 7: уточнення, `Стоп`, `Нова задача` |
| `JOB-003` | `UC-003`, `UC-004` | S1 і S5: доступ та локальна сесія; S2–S5: майбутні параметри |
| `JOB-004` | `UC-005` | Етап 10: архів, повний експорт, підтверджене видалення |
| `JOB-005` | `UC-006` | Етап 7: `Витрати`; етапи 5 і 8: квоти та відновлення |

## Journey Stages

### 1. Перше приєднання, пристрій і перевірка доступу

**Дія Власника.** Власник у штатному Element приймає запрошення до єдиної `Приватної Matrix-кімнати`. Якщо це новий або замінений пристрій, Власник відновлює доступ за recovery key і перевіряє пристрій засобами Element/Matrix; втрачений або скомпрометований пристрій відкликає. Journey не додає власного onboarding UI. `[PRD: US-001–US-002, FR-001, AC-006, AC-010; CT: «Перевірений Matrix-пристрій», «Recovery key»]`

**Очікувана відповідь.** До запуску агентів або повернення захищених даних система перевіряє Matrix-подію, точну allowlist-пару `room_id` і `owner_mxid`, E2EE, статус перевіреного й не відкликаного пристрою та room invariants. У кімнаті залишається одна identity Власника й одна identity бота; ролі агентів не стають окремими Matrix-акаунтами. `[PRD: FR-001, NFR-007, AC-006; GR: Scope Boundaries, When To Stop]`

**Розгалуження доступу й recovery.** Інший `room_id`, інший `owner_mxid`, підроблена подія, неперевірений або відкликаний пристрій завершують шлях без консультації й доступу до архіву та не змінюють активну сесію. Новий пристрій може продовжити лише після recovery та перевірки; втрачений пристрій — лише після відкликання, не як активний клієнт. `[PRD: US-002, AC-006; GR: When To Stop, Verification Rules]`

**Розгалуження room invariants.** Якщо `matrix.org` не підтверджено; E2EE вимкнено або не підтверджено; кімната не invite-only; joined-membership не дорівнює Власник + бот; є інший pending invite; history visibility не `joined`; є public address/listing; дозволено guests; присутній bridge або widget; пристрій не verified — робочі повідомлення не обробляються до відновлення інваріантів. `[GR: Forbidden Changes, When To Stop]`

Після успішної перевірки система переходить до перевірки згоди.

### 2. Перше використання і згода

**Дія Власника.** Під час першого використання Власник явно надає або не надає одноразову `Згоду на обробку даних` через `matrix.org`, GoDaddy hosting, OpenAI та Anthropic. Пояснення прямо відділяє Matrix E2EE від подальшої серверної обробки: `matrix.org` бачить метадані, а розшифрований у GoDaddy Node.js runtime зміст передається AI-провайдерам через TLS. `[PRD: FR-002, NFR-005; CT: «Згода на обробку даних», «Matrix E2EE»; GR: Scope Boundaries]`

**Розгалуження.** Після згоди звичайна обробка бізнес-даних може тривати. Без згоди система не починає таку обробку; загальна згода не поширюється на особливо чутливі документи або зовнішні дії. `[PRD: §3.2, FR-002, FR-006, FR-035; GR: When To Ask, When To Stop]`

### 3. Надсилання тексту, зображення або PDF і межа даних

**Дія Власника.** Власник надсилає текст, зображення або PDF із практичною проблемою та потрібним контекстом. `[PI: «Основний сценарій»; PRD: US-003, FR-003; CT: «Надіслати запит»]`

**Очікувана відповідь.** Підтримуваний звичайний матеріал переходить до аналізу; непотрібні ідентифікатори маскуються, а кожен агент згодом отримує лише мінімально потрібний контекст. `[PRD: FR-003–FR-004; GR: AI Autonomy Boundaries]`

**Розгалуження.**

- Непідтримуваний тип отримує зрозуміле повідомлення без запуску аналізу. `[PRD: FR-003]`
- Якщо виявлено `Секрет`, його зупиняють до передачі агентам і до реєстрації, не повторюючи секрет у відповіді чи архіві. `[PRD: FR-005; GR: When To Stop]`
- Перед обробкою `Особливо чутливого документа` система переходить в `Очікування дозволу` й просить окреме підтвердження. За невизначеної класифікації застосовується ця безпечна межа. Без підтвердження відповідну обробку не продовжують. `[PRD: FR-006, AC-005; CT: «Очікування дозволу»; GR: When To Ask, When To Stop]`

### 4. Прийняття запиту і початок сесії

**Очікувана відповідь.** Після прийняття дозволеної Matrix-події система створює або продовжує єдину `Активну сесію`, показує дату й час її старту та не пізніше ніж через 5 секунд підтверджує отримання. Повторна доставка тієї самої Matrix-події не повинна дублювати сесію, роботу, видиму репліку чи витрати. `[PRD: §3.3, FR-007, FR-017, NFR-001, NFR-009; PI: «Досвід живого чату»]`

**Відчуття Власника.** Запит не зник у невідомості: роботу прийнято, а поточний контекст однозначно відокремлений від інших задач. `[PC: §5–6; PRD: US-010, US-013]`

### 5. Вибір режиму та передзапускова auth/quota-перевірка

**Рішення Головного консультанта.** Простий фактичний або очевидно короткий запит переходить у `Пряму відповідь`. Складний, багатосферний, високоризиковий, такий, що потребує незалежної критики, або прямо позначений Власником запит переходить у `Консиліум`. `[PRD: §3.2, FR-008; CT: «Пряма відповідь», «Консиліум»]`

**Критерій довіри.** Зайві агенти не запускаються для демонстрації процесу; водночас складний запит не подається як консиліум, якщо окремих агентів фактично не скликано. `[PRD: US-005, FR-008, FR-030; GR: Forbidden Changes]`

**Передзапускова перевірка.** До кожного модельного виклику, а для консиліуму — до запуску будь-якого залежного агента, система підтверджує дозволений `Subscription OAuth` mode і доступну квоту потрібного провайдера. Для прямої відповіді потрібна чинна Codex-авторизація. Для повного консиліуму до оголошення успішно скликаного складу мають бути готові один захищений Codex OAuth-стан для окремих реальних Codex-сесій/тредів і окремий Claude Code OAuth token лише для Claude-маршруту. Наявність `OPENAI_API_KEY`, `ANTHROPIC_API_KEY` або іншого API/PAYG credential є blocker, а не fallback. `[PRD: §3.5, §4.5–4.6, FR-009–FR-010, FR-030, NFR-006; GR: When To Stop]`

**Розгалуження auth/quota.** Якщо auth mode невідомий, OAuth прострочений або відкликаний, managed refresh не вдався, Claude setup-token недійсний, квота вичерпана чи private single-owner eligibility не підтверджена, залежний модельний виклик не запускається. Власник отримує причину, provider-reported reset time, якщо він доступний, і безпечну вказівку завершити повторну авторизацію поза Matrix. Повідомлення ніколи не просить надіслати в кімнату token, setup-token, `auth.json`, refresh state, URL/code або screenshot із credential material. Після out-of-band відновлення шлях повертається лише через свіжу auth-mode/quota перевірку; автоматичного переходу на API, PAYG чи usage credits немає. `[PRD: US-018, FR-030, AC-007; CT: «Fail-closed auth/quota»; GR: When To Ask, When To Stop]`

### 6A. Гілка прямої відповіді

**Очікувана відповідь.** Після успішної Codex auth/quota-перевірки Власник отримує читабельну пряму відповідь без зайвого консиліуму, додаткових агентів і технічного шуму. Рівень упевненості відповідає доказам; актуальні чи високоризикові твердження спираються на першоджерела або прямо позначають обмеження. Якщо перевірка не проходить, відповідь не імітується: гілка переходить до видимого fail-closed стану етапу 8. `[PRD: AC-001, AC-007, FR-028–FR-030, FR-036; GR: Verification Rules]`

**Завершення гілки.** Практична цінність виникає, коли відповідь дає Власнику достатнє рішення або визначає наступну перевірку без невиправданих часу й витрат. Після завершення сесія переходить до етапу архіву й контролю даних. `[PI: «Результат для користувача», «Принципи продукту»; PRD: US-005, §4]`

### 6B. Гілка повного дослівного консиліуму Candidate B

**Очікувана відповідь.** Лише після успішної передзапускової перевірки Codex-агентів і вибраного Критика Головний консультант показує ситуаційно підібрані конкретні ролі 2–5 окремих Codex-агентів-спеціалістів і залучає окремого агента-критика у власному runtime (Claude Code-процес або Codex thread). Один захищений ChatGPT OAuth-стан використовується без клонування credential store, але Головний консультант і кожен Codex-агент-спеціаліст мають окрему реальну сесію/тред, роль, завдання й робочий контекст. Перша видима агентська репліка має надійти не пізніше ніж через 30 секунд після прийняття запиту на консиліум. `[PRD: US-006–US-007, FR-009–FR-010, NFR-002, AC-002; CT: «Codex OAuth-стан», «Реальна Codex-сесія/тред», «Claude Code OAuth token»]`

**Робота агентів.** Спеціалісти спочатку формують незалежні позиції, а потім обмінюються фактичними адресованими повідомленнями через A2A; критик перевіряє ключову пропозицію, докази, альтернативи, ризики й умови провалу. `[PRD: FR-011–FR-014; PC: §8]`

**Видимість.** За напрямом Candidate B «Дослівний консиліум» кожне фактично надіслане агентське доручення, проміжна репліка, критика або виправлення одразу з'являється в тій самій Element/Matrix-кімнаті повністю й у канонічному порядку, без редагування, переказу, приховування або пакетування до фіналу. Усі повідомлення надходять від одного Matrix-акаунта бота; всередині повідомлення видно конкретну предметну роль і час `HH:MM`. Виправлення надходить новою реплікою. Системні інструкції, приховані міркування, журнали інструментів, технічні ID, номери, секунди й сирі Markdown-маркери не показуються. `[PRD: US-008–US-009, US-022, FR-014–FR-019, FR-034, AC-003; CT: «Дослівний консиліум», «Підтверджена репліка агента»]`

**Сповіщення.** Нові репліки використовують лише нативні сповіщення й звук Element та операційної системи, якщо їх увімкнув Власник. Продукт не створює власного звуку. `[PI: «Досвід живого чату»; PRD: §8; GR: Forbidden Changes]`

**Темп.** Під час активної роботи не минає понад 60 секунд без змістовної видимої репліки або пояснення очікування. Якщо агент не показує поступу, Головний консультант звужує його завдання або замінює агента й повідомляє про це Власнику. `[PRD: FR-023, NFR-003; PI: «Стандарт швидкості»]`

### 7. Втручання Власника під час активної сесії

- **Уточнення.** Нове звичайне повідомлення додається до контексту поточної задачі; паралельна сесія не створюється. `[PRD: FR-020, AC-004; CT: «Уточнити задачу»]`
- **`Витрати`.** Система окремо показує налаштовані місячні платежі ChatGPT/Codex і Claude, фактичні витрати GoDaddy Node.js/MySQL та іншої чинної інфраструктури, а також поточний статус `matrix.org` (`0` лише доки доступний безкоштовний план) і доступний provider-reported usage/ліміт/reset. Subscription AI usage поточної сесії позначається як `входить у підписку`; показник, якого провайдер не надав, — як `невідомо`. Команда не вигадує per-session token charge, не купує usage credits і не встановлює жорсткого грошового ліміту. `[PRD: US-021, FR-033, AC-009; CT: «Витрати», «Облік витрат»; GR: Scope Boundaries]`
- **`Стоп`.** Система не запускає нових модельних викликів цієї сесії, скасовує поточні операції там, де це підтримує провайдер, і не публікує пізні агентські відповіді як продовження зупиненої роботи. Сесія стає `Зупиненою сесією`. `[PRD: FR-021, AC-004; CT: «Стоп», «Зупинена сесія»]`
- **`Нова задача`.** Система закриває поточну сесію, створює нову й не переносить попередній робочий контекст як активну задачу. До вирішення словникового питання повідомлення має бути буквальним: «поточну сесію закрито, нову створено». `[PRD: FR-022, AC-004; CT: «Нова задача», Open Vocabulary Questions; GR: Open Questions]`
- **Окремий дозвіл на дію.** Якщо рекомендація переходить у конкретну зовнішню або високоризикову дію, система називає дію, ціль, отримувача або систему й істотний наслідок та чекає окремого явного дозволу. Мовчання, загальна згода на дані або дозвіл на іншу дію не підходять. `[PRD: US-023, FR-035; GR: When To Ask, When To Stop]`

### 8. Часова межа, збій або неповний результат

**Межа 10 хвилин.** Стандартний консиліум завершується фінальною рекомендацією не пізніше ніж через 10 хвилин. Якщо потрібен довший аналіз, Головний консультант до спливу цього часу називає причину й просить окремий дозвіл продовжити. Без дозволу робота не продовжується; з дозволом сесія повертається до активної роботи з видимими оновленнями. `[PRD: FR-024, NFR-004, AC-007; CT: «Очікування дозволу»; GR: When To Ask, When To Stop]`

**Збій.** Недоступний агент, провайдер, неможливість скликати справжній консиліум або інша втрачена частина роботи показуються як `Збій або неповний результат`. Система не вигадує реплік чи успіху, називає наслідок і доступну наступну дію. `[PRD: FR-023, FR-030, NFR-012, AC-007; CT: «Збій або неповний результат»]`

**Збій authorization/quota.** Невдалий preflight не запускає залежних агентів. Якщо OAuth expires/revoked або квота вичерпується вже під час активної роботи, нові залежні виклики припиняються; уже підтверджені репліки залишаються дослівними, а незавершена гілка позначається як неповна. Codex і Claude Code мають окремі статуси: недоступність одного не маскується успіхом іншого й не дає права назвати повний консиліум завершеним. Власник отримує лише безпечну out-of-band reauth/reset вказівку без credential material у Matrix; відновлення потребує свіжого preflight. `[PRD: US-018, FR-030, NFR-006, NFR-012, AC-007; GR: When To Stop, Verification Rules]`

**Межа приватного використання.** Запит від клієнта, працівника або іншої третьої особи, передавання їй результату чи використання для неї підписок Власника зупиняє залежну роботу. Цей шлях не перетворює V1 на SaaS і не пропонує API/PAYG як обхід; потрібне окреме продуктове, ліцензійне та білінгове рішення поза V1. `[PRD: §3.1, §8, §11; GR: Forbidden Changes, When To Stop]`

**Брак доказів.** Головний консультант відділяє підтверджені факти, інформацію Власника, припущення, професійні судження й невідоме. Якщо доказів недостатньо або потрібна зовнішня перевірка, система не робить упевненого висновку, а називає перевірку, здатну змінити рішення. Згода агентів не подається як незалежний доказ. `[PRD: FR-025, FR-028–FR-030; GR: When To Stop, Verification Rules]`

### 9. Фінальна рекомендація

**Climax у дії.** Головний консультант припиняє дискусію, коли наступні репліки вже не змінюють рішення або потрібна зовнішня перевірка, синтезує суперечності й бере відповідальність за один результат. Власник не мусить сам обирати між сирими монологами агентів. `[PI: «Фінальна рекомендація»; PRD: FR-014, FR-025–FR-026]`

**Отриманий результат.** `Фінальна рекомендація` надходить послідовністю коротких самодостатніх повідомлень:

1. головний висновок або рішення;
2. не більше трьох конкретних наступних дій;
3. критичні ризики, припущення та умова перегляду;
4. окрема самодостатня `Технічна частина`, лише коли вона доречна й може бути цілком перенесена в Codex.

Для суттєвої дії, коли це доречно до задачі, визначаються результат, відповідальний, строк, ознака виконання, доказ, головний ризик і умова перегляду. Зовнішня або високоризикова дія залишається рекомендацією до отримання окремого явного дозволу. `[PRD: FR-026–FR-028, FR-035, AC-011; CT: «Фінальна рекомендація», «Технічна частина»]`

### 10. Завершення, архів, експорт і видалення

**Звичайне завершення.** Після завершення робочий запис переходить до зашифрованого безстрокового `Архіву сесій` із незмінним змістом і канонічним порядком підтверджених повідомлень. Сесію не можна називати успішно завершеною чи коректно заархівованою, якщо повноту, порядок, недублювання або незмінність не підтверджено. `[PRD: FR-031, NFR-010–NFR-011; CT: «Завершена сесія», «Архів сесій»; GR: When To Stop]`

**Експорт.** На запит Власник отримує повну сесію зі збереженням змісту й порядку; експорт не змінює архівний запис. `[PRD: FR-032, AC-008; CT: «Експортувати сесію»]`

**Видалення.** Окрему репліку завершеної сесії не можна редагувати або видалити. Цілу сесію можна остаточно видалити лише після явної команди Власника та повторного явного підтвердження. Якщо підтвердження не надано, видалення не відбувається. `[PI: «Правила даних і доступу»; PRD: FR-032, AC-008; GR: When To Ask]`

### Окремий короткий шлях: Налаштування власника

Цей шлях виникає епізодично, лише коли Власник хоче змінити параметри майбутньої консультації. Він не додає браузерний консультаційний сценарій, не показує перебіг консиліуму й завершується поверненням до Element. `[PRD: US-024, FR-037; PC: §7–8; CT: «Налаштування власника»]`

#### S1. Перехід, Google-вхід і локальна сесія

**Дія Власника.** Власник за потреби переходить з Element до responsive `Налаштувань власника` на mobile або desktop та явно починає `Google-вхід Власника`. Він підтверджує особу в Google й повертається до застосунку в тій самій вкладці. Застосунок не приймає пароль Google, не вимагає власного другого фактора й не пропонує резервного пароля; перевірки самого Google не скасовуються. Готовність модельних каталогів та AI-підписок не є передумовою цього входу. `[PRD: UC-003, FR-037, FR-038, NFR-016.c, NFR-016.h, NFR-018, AC-012, AC-016; GR: Scope Boundaries, Source Access Failures]`

**Перевірка доступу.** Лише серверне підтвердження токена, точної дозволеної ідентичності й свіжої транзакції цього браузера створює окрему захищену локальну сесію. Підтверджений Власником акаунт прив'язується за `NFR-016.b`, а не за display name, Console-акаунтом або першим стороннім входом. Кожний захищений запит потребує чинної сесії; підроблені запити, стороння ідентичність, недійсна чи відкликана сесія, replay або обхід входу не відкривають і не змінюють Settings. Деталі перевірки та секрети не показуються. `[PRD: UC-003, FR-038, NFR-016.a–NFR-016.g, AC-012; GR: When To Stop, Verification Rules]`

**Скасування, помилка й повторна спроба.** Скасування Власником, збій Google, чужа або прострочена транзакція чи повторне повернення не створюють доступу. Після повернення Власник може явно почати свіжу спробу або залишити веб-шлях і повернутися до Element; обмеження спроб не обходяться новою транзакцією. Успішний повторний вхід створює новий локальний ідентифікатор і припиняє попередню сесію цього браузера. Якщо доступ сплив або відкликаний під час редагування, збереження й повернення стандартних значень не виконуються; після повторного Google-входу Власник знову перевіряє чинний набір. `[PRD: UC-003, NFR-016.c–NFR-016.g, AC-012; GR: When To Stop]`

**Межа довіри.** Google-вхід, відмова, локальний вихід і повторний вхід не відкривають, не змінюють і не замінюють Codex або Claude Code `Subscription OAuth`. Google credentials, секрети локальної сесії та жодний AI OAuth credential, setup-token, refresh state, `auth.json` або reauth material не відображаються в Settings і не потрапляють у його значення, клієнтські дані або дані сесії; дозволений захищений session token застосунку не є Google/AI credential. Окремо дозволене захищене runtime-налаштування підписок залишається окремою операцією, а не побічним ефектом входу. `[PRD: FR-039, NFR-006, NFR-016.d–NFR-016.f; GR: Forbidden Changes, When To Stop, Verification Rules]`

#### S2. Перегляд чинного й стандартного наборів

**Очікувана відповідь.** Після входу Власник бачить чинні effective values для наступної сесії та source-backed standard values рівно для трьох груп: `Codex-агенти` (модель + міркування), `Критик` (провайдер + власні модель + міркування) та `Швидкість консиліуму`. Непідтверджений каталог або capability map не видається за готовий вибір: до його готовності неперевірений набір не зберігається, але локальний доступ не втрачається лише з цієї причини. Якщо консультація вже активна, текст прямо пояснює, що її незмінні `Фактичні налаштування сесії` не зміняться; це пояснення не перетворює Settings на live-status дашборд. `[PRD: UC-004, US-025–US-029, FR-040–FR-045; CT: відповідні терміни; GR: Source Access Failures]`

#### S3. Зміна та inline compatibility validation

**Дія Власника.** Власник змінює одну чи кілька з рівно трьох дозволених груп або готує повернення всього набору до standard values. Для Codex і Claude Code доступні незалежні підтверджені списки з точними назвами й версіями моделей, без довільного вводу; Claude впорядковано `Opus → Sonnet → Haiku`, версії сімейства — від новіших до старіших. Кожний рівень міркування належить лише своїй моделі; спільної шкали чи автоматичного перенесення між провайдерами немає. Зміна Codex-агентів не змінює Критика, навіть коли він теж Codex; перемикання Критика не переносить effort між його гілками; `За замовчуванням моделі` не передає явного effort. Для швидкості доступні `швидко / збалансовано / ретельно`. `[PRD: UC-004, US-025–US-028, FR-040–FR-044, AC-013–AC-014; CT: «Міркування Codex», «Міркування Claude Code»]`

**Очікувана відповідь.** Під час зміни Власник отримує inline compatibility validation за актуальною capability map: підтримувана комбінація готова до збереження, а unsupported, unknown або stale model/depth mapping має конкретне зрозуміле пояснення без silent downgrade. `Швидкість` перевіряється лише як orchestration preset; навіть `швидко` не є Fast Mode, API/PAYG або usage-credit режимом і не може послабити критика, A2A, Matrix E2EE, дослівність, потрібне дослідження, safety, privacy чи permission gates. `[PRD: US-026–US-027, FR-042–FR-043, FR-046, NFR-019; AC-014; GR: When To Stop]`

#### S4. Атомарне save, cancel або повернення стандартних значень

**Валідний шлях.** Save однією MySQL-транзакцією атомарно зберігає весь повністю валідний набір. Cancel відкидає незбережені зміни. Повернення standard values проходить ту саму свіжу validation і застосовується як один узгоджений набір, а не як часткові записи. Після успіху показані effective values відповідають фактично збереженим. `[PRD: US-028, FR-044, NFR-017; AC-013; GR: Verification Rules]`

**Невалідний шлях.** Якщо будь-яке поле несумісне, capability map stale/unknown або atomic write не підтверджено, не зберігається жодне поле; попередній валідний набір залишається effective. Такий набір не створює `Фактичні налаштування сесії` й не запускає нову сесію. Після capability drift уже збережений set повторно перевіряється на старті наступної сесії та fail closed блокує її до сумісної зміни; активна сесія продовжується зі своїм незмінним snapshot. `[PRD: US-029, FR-044–FR-045, NFR-017, NFR-019; AC-013–AC-015; GR: When To Stop]`

#### S5. Момент успіху й повернення до Element

**Момент успіху налаштувань.** Власник бачить правдивий збережений effective set і чітке повідомлення, що зміна застосовується лише до наступної сесії. Активна сесія, якщо вона є, не перезапускається й не мутує. Власник повертається до Element; під час наступного запиту система ще раз валідовує повний набір і лише тоді фіксує його як незмінні `Фактичні налаштування сесії`. `[PRD: UC-004, US-028–US-029, FR-044–FR-045; AC-015]`

**Завершення локального доступу.** Власник має видимий вихід і захищений спосіб припинити свої активні локальні сесії. Вихід, відкликання, idle/absolute expiry чи відключення дозволеного Власника припиняють захищений доступ на сервері й не скасовуються рестартом. Локальний вихід не означає виходу з Google: Google може зберегти власну сесію та використати її за наступного явного входу, але застосунок знову перевіряє ідентичність і створює нову локальну сесію. AI-підписки від цього не змінюються. Точні строки й межі паралельних локальних сесій визначає архітектура за `NFR-016.e`; цей шлях їх не вигадує. `[PRD: UC-003, FR-039, NFR-016.d–NFR-016.e, AC-012]`

## Climax Beat

Для основного складного сценарію кульмінація настає, коли після видимої реальної дискусії, незалежної критики й коригування пропозиції Головний консультант перетворює суперечності на одну `Фінальну рекомендацію`: конкретне рішення, до трьох дій і чітку умову перегляду. Саме тут видимий процес стає практичною зміною рішення або дії Власника. `[PI: «Результат для користувача», «Фінальна рекомендація», «Принципи продукту»; PRD: FR-010–FR-016, FR-025–FR-027]`

Для простого сценарію відповідний момент цінності — своєчасна пряма відповідь без невиправданого консиліуму. `[PRD: US-005, AC-001]`

Окремий момент успіху налаштувань не замінює основну кульмінацію: це коротке підтвердження, що сумісний набір атомарно збережено лише для наступної сесії, активна сесія не змінилася, а Власник може повернутися до Element. `[PRD: US-028–US-029, AC-013–AC-015]`

## Decision Points

| Рішення | Хто вирішує | Варіанти й наслідок | Джерело |
|---|---|---|---|
| Чи підтверджено кімнату, подію, exact allowlist і пристрій? | Система за Matrix-подією та room/device state | Так — продовжити; інший `room_id`/`owner_mxid`, підроблена подія, неперевірений або відкликаний пристрій — не запускати агентів і не повертати захищені дані | `PRD US-002, FR-001, NFR-007, AC-006`; `GR When To Stop` |
| Чи цілісні room invariants? | Система | `matrix.org` + E2EE + invite-only + рівно Власник/бот + no pending invites + history `joined` + no public address/listing + no guests/bridges/widgets + verified devices — продовжити; порушення — зупинити | `GR Forbidden Changes, When To Stop` |
| Чи можна довіряти новому пристрою? | Власник через нативні Matrix recovery, verification і revocation | Recovery + перевірка — продовжити; неперевірений або відкликаний стан — не обробляти | `PRD US-002, AC-006`; `GR Verification Rules` |
| Чи є згода на звичайну обробку? | Власник | Так — продовжити; ні — не починати звичайну обробку | `PRD FR-002`; `GR When To Ask` |
| Чи підтримується вхід і чи безпечний він для обробки? | Система в межах політик | Текст/зображення/PDF — продовжити; інший тип — відмова без аналізу; `Секрет` — зупинити до реєстрації | `PRD FR-003–FR-005, AC-005`; `GR When To Stop` |
| Чи є документ особливо чутливим або невизначеним? | Система запитує, Власник підтверджує | Підтверджено — обробити; не підтверджено — не обробляти відповідний документ | `PRD FR-006`; `GR When To Ask` |
| Який режим потрібен? | Головний консультант | Простий запит — `Пряма відповідь`; складний, багатосферний, високоризиковий, критичний або явно позначений — `Консиліум` | `PRD FR-008`; `CT Product States` |
| Чи готові дозволений auth mode і квота потрібних провайдерів? | Система до кожного model call і до запуску залежних агентів | Subscription OAuth + доступна квота — продовжити; unknown/expired/revoked/refresh failure/invalid setup-token/quota exhausted або API/PAYG credential — fail closed без fallback | `PRD §3.5, FR-009–FR-010, FR-030, NFR-006`; `GR When To Stop` |
| Чи завершив Власник потрібну повторну авторизацію? | Власник поза Matrix; система повторно перевіряє | Provider-managed out-of-band flow завершено й новий preflight пройдено — відновити залежну роботу; credential/code у Matrix або невдалий preflight — не запускати | `PRD FR-030, AC-007`; `GR When To Ask, When To Stop` |
| Чи лишається сценарій приватним single-owner? | Система за доступом і актуальною eligibility; матеріальну зміну вирішує Власник | Лише Власник — продовжити; клієнт, працівник, третя особа або непідтверджена provider eligibility — зупинити до окремого перегляду | `PRD §3.1, §8`; `GR Scope Boundaries, When To Stop` |
| Чи продовжувати понад 10 хвилин? | Власник після пояснення Головного консультанта | Так — продовжити активну роботу; ні або дозволу немає — не продовжувати | `PRD FR-024, NFR-004`; `GR When To Stop` |
| Чи змінює нова репліка активну задачу? | Власник через зміст або точну команду | Звичайне повідомлення — уточнення; `Стоп` — зупинка; `Нова задача` — закриття поточної та створення нової сесії | `PRD FR-020–FR-022, AC-004` |
| Чи готові докази для висновку? | Головний консультант | Достатні — синтезувати; недостатні — назвати невідоме й наступну перевірку | `PRD FR-025, FR-028–FR-030`; `GR Verification Rules` |
| Що робити із завершеною сесією? | Власник | Зберігати в архіві; експортувати повністю; видалити цілою лише після підтвердження | `PRD FR-031–FR-032, AC-008` |
| Чи підтверджено доступ до `Налаштувань власника`? | Система перевіряє Google-ідентичність, браузерну транзакцію й окрему локальну сесію | Чинна сесія дозволеного Власника — відкрити; скасування, збій, стороння ідентичність, недійсна транзакція, підроблена/прострочена/відкликана сесія або обхід — відмовити; повторна спроба починає свіжий Google-вхід без резервного пароля | `PRD UC-003, NFR-016.a–NFR-016.h, AC-012`; `GR When To Stop, Verification Rules` |
| Чи сумісний повний Settings set? | Система за актуальною capability map | Supported — дозволити atomic save/reset; unsupported, unknown або stale — не зберігати й не запускати нову сесію, без silent downgrade | `PRD US-025–US-029, FR-041–FR-045, NFR-019`; `GR When To Stop` |
| Коли застосувати збережену зміну? | Система на межі нової сесії | Лише наступна сесія після повторної validation отримує новий immutable snapshot; активна сесія не змінюється | `PRD US-029, FR-045, NFR-017, AC-015` |

## Friction And Risks

| Фрикція або страх Власника | Як шлях відповідає | Залишковий ризик |
|---|---|---|
| Консиліум може бути імітацією одного монологу | Показуються лише фактичні, окремо надіслані й зареєстровані адресовані репліки; неможливість скликання називається прямо | Видима роль без доказу окремого агента все ще може створити хибне відчуття реальності; готовність потребує наскрізного доказу |
| Довге очікування без розуміння поступу | Межі 5/30/60 секунд і 10 хвилин; видима заміна агента або пояснення затримки | Відмови зовнішніх провайдерів можуть порушити межі; це має перейти в прозорий частковий стан |
| Перевантаження довгими неузгодженими текстами | Репліки залишаються повними для правдивості, але Головний консультант завершує їх короткою синтезованою рекомендацією | Дослівний живий перебіг може бути об'ємним; окремого скороченого режиму V1 не підтверджено |
| Розкриття конфіденційних даних або секрету | Одноразова згода, окреме підтвердження чутливого документа, мінімізація контексту й зупинка секрету до журналювання | Помилка класифікації чи маскування може передати зайві дані зовнішньому провайдеру |
| Витік OAuth або непомітний PAYG fallback | Credential material не входить до Matrix, A2A, промптів, журналу чи архіву; preflight перевіряє subscription auth mode і квоту, а помилка fail closed | Компрометація захищеного credential store або зміна provider behavior лишаються ризиком; відновлення потребує відкликання й out-of-band reauth |
| Втрата контролю над дією, контекстом або витратами | Окремі дозволи, одна активна сесія, `Стоп`, `Нова задача`, `Витрати`, експорт і підтверджене видалення | Немає жорсткого грошового ліміту; monthly fee або provider usage/reset може бути недоступним і має лишатися `невідомо` |
| Вичерпання subscription quota під час консиліуму | Codex-агенти й вибраний Критик перевіряються окремо до запуску та перед новими викликами; залежна гілка зупиняється без API/PAYG fallback | Частина підтверджених реплік може вже існувати; такий результат чесно позначається неповним, а не повним консиліумом |
| Використання приватних підписок для третьої особи | Exact single-owner доступ і private non-SaaS межа перевіряються до залежного запуску | Зміна користувацької моделі або правил провайдера потребує нового рішення про auth, licensing і billing поза V1 |
| Хибна впевненість через згоду агентів | Факти, припущення, судження й невідоме розділяються; актуальні та високоризикові твердження перевіряються за першоджерелами | Спільні сліпі зони ШІ залишаються; зовнішні дані або профільний фахівець можуть бути необхідні |
| Сторонній доступ або втрачений пристрій | Exact allowlist `room_id` + `owner_mxid`, verified device, recovery і revocation передують обробці | Компрометація recovery key або запізніле відкликання залишають ризик; до відновлення довіри робота зупиняється |
| Хибне відчуття наскрізної конфіденційності до AI | Перша згода чесно пояснює метадані homeserver і завершення E2EE на боті в GoDaddy runtime | Розшифрований зміст обробляють GoDaddy runtime, OpenAI та, лише для Claude-маршруту, Anthropic; мінімізація контексту залишається необхідною |
| Порушення повноти або порядку сесії | Element-відображення, архів та експорт мають зберігати повне тіло й канонічний порядок без дублів | Повторна доставка чи конкуренція повідомлень може зіпсувати порядок; без доказу сесія не вважається коректно завершеною |
| Сторонній або прострочений Settings-доступ | Точна Google-ідентичність, серверна перевірка окремої локальної сесії, строки й відкликання включно після рестарту; обхід не дає доступу | Фішинг або викрадення Google/локальної сесії лишаються ризиком; Google-вхід не доводить MFA й не повинен відкривати AI OAuth credentials (`NFR-016.a`–`NFR-016.h`) |
| Несумісна конфігурація або capability drift | Inline validation, validate-before-save, atomic write й повторна перевірка до старту нової сесії | Нова сесія може бути заблокована до оновлення набору; активна сесія має лишитися незмінною |
| `Швидкість` помилково сприймається як платний або менш безпечний режим | Значення описані лише як orchestration presets; Fast Mode, PAYG і послаблення інваріантів заборонені | Реалізація потребує доказу, що `швидко` не обходить критика, A2A, дослівність чи safety/privacy gates |

## Failure Path

1. **Немає підтвердженого Matrix-доступу.** Інший `room_id` або `owner_mxid`, підроблена подія, неперевірений чи відкликаний пристрій не запускають агентів, не повертають захищених даних і не змінюють активну сесію Власника. `[PRD: US-002, AC-006; GR: When To Stop]`
2. **Порушено room invariants.** Якщо не підтверджено `matrix.org`, E2EE або invite-only; joined-склад не дорівнює Власник + бот; є інший pending invite; history visibility не `joined`; є public address/listing; дозволено guests; є bridge чи widget; пристрій не verified — робочі запити не обробляються до відновлення інваріантів. `[GR: When To Stop]`
3. **Recovery або revocation не завершено.** Новий пристрій без відновлених ключів і перевірки не працює; втрачений або скомпрометований пристрій має бути відкликаний. Якщо Matrix E2EE-ключі, verified state чи безпечне recovery/revocation не працюють, нові робочі повідомлення не розшифровуються й не обробляються. `[PRD: US-002, AC-006; GR: When To Stop]`
4. **Немає першої згоди.** Звичайна обробка бізнес-даних через `matrix.org`, GoDaddy hosting, OpenAI та Anthropic не починається. `[PRD: FR-002; GR: When To Stop]`
5. **Непідтримуваний вхід.** Система пояснює обмеження й не запускає аналіз. `[PRD: FR-003]`
6. **Виявлено секрет.** Секрет зупиняється до агентів і журналу та не повторюється у відповіді. `[PRD: FR-005; GR: When To Stop]`
7. **Немає підтвердження для чутливого документа.** Обробка цього документа залишається в `Очікуванні дозволу` й не продовжується. `[PRD: FR-006; GR: When To Stop]`
8. **Передзапускова auth/quota-перевірка не пройдена.** Невідомий auth mode, OAuth expiry/revocation, refresh failure, invalid Claude setup-token, quota exhaustion або заборонений API/PAYG credential зупиняють залежний виклик до запуску агента. Система називає безпечний статус і не створює agent reply. `[PRD: US-018, FR-030, AC-007; GR: When To Stop]`
9. **Auth/quota втрачено під час роботи.** Нові залежні виклики зупиняються, вже підтверджені репліки не редагуються, а результат позначається неповним. Для відновлення Власник проходить provider-managed reauth поза Matrix або чекає reset квоти; потім система свіжо перевіряє auth mode й quota. Жодного API/PAYG fallback немає. `[PRD: FR-030, NFR-006, NFR-012; GR: Verification Rules]`
10. **Credential material з'явився в Matrix або agent context.** Система не повторює значення, припиняє залежну обробку й вимагає відкликання та безпечної out-of-band повторної авторизації до відновлення. `[GR: Forbidden Changes, When To Stop]`
11. **Порушено private single-owner межу.** Запит або результат для клієнта, працівника чи іншої третьої особи не використовує підписки Власника й не запускає agent work; V1 не стає SaaS або shared service. `[PRD: §3.1, §11; GR: When To Stop]`
12. **Агент не дає поступу.** Головний консультант звужує завдання або замінює агента й показує це Власнику. `[PRD: FR-023]`
13. **Агент або справжній консиліум недоступні.** Система не імітує роботу, а називає `Збій або неповний результат`, його наслідок і доступну наступну дію. `[PRD: FR-030; GR: When To Stop]`
14. **Минуло до 60 секунд без репліки.** Власник отримує змістовне оновлення або пояснення очікування; мовчазний активний стан не відповідає V1. `[PRD: NFR-003]`
15. **Потрібно понад 10 хвилин.** До спливу межі Головний консультант пояснює причину й просить дозвіл; без нього не продовжує. `[PRD: FR-024, AC-007]`
16. **Доказів недостатньо.** Невідоме позначається, упевнена рекомендація зупиняється, а Власник отримує перевірку, яка може змінити рішення. `[PRD: FR-028–FR-030; GR: When To Stop]`
17. **Немає окремого дозволу на зовнішню або високоризикову дію.** Консультація може завершитися рекомендацією, але відповідна дія не виконується. `[PRD: US-023, FR-035; GR: When To Stop]`
18. **Власник надсилає `Стоп`.** Подальша робота припиняється, а пізні агентські відповіді не публікуються як продовження. `[PRD: FR-021]`
19. **Не доведено цілісність запису.** Сесію не називають успішно завершеною або коректно заархівованою; статус залишається частковим із наступною перевіркою. `[GR: When To Stop, Evidence Requirements]`
20. **Google-вхід або локальну сесію не підтверджено.** Скасування, збій Google, стороння ідентичність, недійсний токен, чужа/прострочена транзакція, replay чи одночасне повернення не створюють доступу. Відсутня, підроблена, прострочена або відкликана локальна сесія й обхід захищеного запиту не відкривають і не змінюють Settings. Відмова не розкриває Google/локальні/AI секрети; Власник може повернутися до Element або явно почати свіжий Google-вхід, без власної MFA застосунку чи резервного пароля. `[PRD: UC-003, NFR-016.a–NFR-016.h, AC-012; GR: When To Stop, Verification Rules]`
21. **Локальний доступ сплив або відкликаний під час редагування.** Save/reset не виконуються; незбережені зміни не стають effective. Вихід, idle/absolute expiry, відкликання чи відключення дозволеного Власника залишають доступ закритим і після рестарту. Повторний Google-вхід, якщо Власник досі дозволений, знову перевіряє ідентичність і замінює локальну сесію; після повернення Власник перевіряє чинний набір. Чинна Google-сесія не відновлює локальний доступ сама собою; AI subscription OAuth не показується й не змінюється. `[PRD: UC-003, FR-039, NFR-016.d–NFR-016.e, AC-012; GR: When To Stop]`
22. **Конфігурація несумісна.** Unsupported model/depth pair, невідомий model slug, заборонений speed effect або інша невідповідність показуються inline; жодне поле не зберігається, і нова сесія з таким набором не стартує. `[PRD: US-025–US-029, FR-041–FR-046, AC-013–AC-015]`
23. **Capability drift.** Якщо allowlist або capability map змінилися, stale/unknown state не спричиняє silent downgrade. Наступна сесія fail closed блокується до свіжої сумісної validation; попередній persisted set і активний snapshot не мутують. `[PRD: FR-042, FR-045, NFR-019; GR: When To Stop]`
24. **Atomic save/reset не підтверджено.** Не застосовується жодна частина набору; Власник бачить попередні effective values і категорію помилки без секретних деталей. `[PRD: US-028, FR-044, NFR-017; GR: When To Stop]`
25. **Зміна зроблена під час активної сесії.** Це не помилка активної консультації: вона продовжується з незмінними `Фактичними налаштуваннями сесії`; збережений валідний set може бути застосований лише після завершення та успішного preflight нової сесії. `[PRD: US-029, FR-045, AC-015]`

## Exit Points

- **До аналізу:** непідтверджена `Дозволена Matrix-пара`, пристрій або room invariants, відсутня згода, непідтримуваний тип, зупинений `Секрет`, непройдений subscription auth/quota preflight або порушена private single-owner межа завершують відповідну гілку без запуску залежних агентів. `[PRD: FR-001–FR-006, FR-030; GR: When To Stop]`
- **У стані дозволу:** чутливий документ або продовження понад 10 хвилин не рухаються далі без явного рішення Власника. `[PRD: FR-006, FR-024]`
- **Після прямої відповіді:** Власник отримує достатній результат без консиліуму, а завершена сесія переходить до архіву. `[PRD: AC-001, FR-031]`
- **Після фінальної рекомендації:** стандартний консиліум завершується одним синтезом і переходить до архіву. `[PRD: FR-025–FR-031]`
- **Через `Стоп`:** активна робота припиняється як `Зупинена сесія`. `[CT: «Стоп», «Зупинена сесія»]`
- **Через `Нова задача`:** поточну сесію закрито, нову створено без змішування активних контекстів. `[PRD: FR-022]`
- **Через збій або брак доказів:** Власник отримує чесний частковий результат і наступну можливу перевірку замість вигаданого успіху. `[PRD: FR-030; GR: Evidence Requirements]`
- **Через auth/quota failure:** залежна робота залишається зупиненою до provider-reported reset або захищеної out-of-band reauth і наступного успішного preflight; credential material у Matrix не приймається, API/PAYG fallback не пропонується. `[PRD: FR-030, AC-007; GR: When To Stop]`
- **Після експорту:** архівний запис не змінюється. Після підтвердженого видалення ціла сесія остаточно видаляється. `[PRD: FR-032; CT: «Експортувати сесію», «Видалити сесію»]`
- **До відкриття Settings:** скасування, збій або непідтверджений Google-вхід, недійсна/прострочена/відкликана локальна сесія чи обхід захищеного запиту завершують спробу без доступу та без зміни налаштувань або AI OAuth. Власник повертається до Element або явно починає нову спробу з новою браузерною транзакцією. `[PRD: UC-003, NFR-016.a–NFR-016.g, AC-012; GR: When To Stop, Verification Rules]`
- **Через локальний вихід або відкликання:** відповідні локальні сесії припиняються на сервері, включно після рестарту; Google-сесія та AI OAuth залишаються окремими. `[PRD: UC-003, FR-039, NFR-016.e]`
- **Під час Settings:** cancel повертає до попереднього effective set; incompatible set, capability drift або write failure завершують спробу без часткового збереження й без старту нової сесії. `[PRD: FR-042–FR-045, AC-013–AC-015]`
- **Після Settings success:** Власник бачить, що валідна зміна стосується лише наступної сесії, і повертається до Element. Активна сесія лишається незмінною. `[PRD: US-029, AC-015]`

## Success State

### Пряма відповідь

Власник із перевіреного, не відкликаного пристрою в дозволеній приватній E2EE Matrix-кімнаті отримав підтвердження прийняття до 5 секунд і після успішної Codex subscription auth/quota-перевірки — читабельну відповідь на простий запит без зайвих агентів, технічного шуму або псевдовпевненості. Результат достатній для рішення чи наступної перевірки, а завершена сесія має коректний архівний запис. Якщо auth/quota недоступні, успіхом є чесний fail-closed статус без модельного виклику та PAYG fallback, а не вигадана відповідь. `[PRD: US-001–US-005, US-018, US-022, AC-001, AC-007, FR-007–FR-008, FR-028–FR-031]`

### Повний консиліум

Власник у тій самій приватній E2EE Matrix-кімнаті після успішної передзапускової перевірки subscription auth/quota Codex-агентів і вибраного Критика побачив доречний склад: Головний консультант і 2–5 спеціалістів працюють в окремих реальних Codex-сесіях/тредах через один захищений OAuth-стан, а критик — в окремому Claude Code-процесі або Codex thread за вибраним маршрутом через відповідний subscription OAuth. Перша агентська репліка надійшла до 30 секунд; усі фактично надіслані агентські доручення, проміжні репліки, критика й виправлення з'явилися наживо, повністю, від одного Matrix-акаунта бота з ролями й часом `HH:MM`. Власник не залишався понад 60 секунд без змістовного оновлення або пояснення, а до 10 хвилин отримав фінальну рекомендацію або явно дозволив продовження. Фінальна рекомендація містить один висновок, до трьох дій, ризики й умову перегляду; технічна частина відокремлена лише за потреби. Збій, auth/quota failure, неповноту та межі доказів не приховано; API/PAYG fallback відсутній. Завершена сесія зашифрована, незмінна, повністю експортується й видаляється лише цілою після підтвердження. `[PI: «Критерій готовності V1»; PRD: US-006–US-023, AC-002–AC-003, AC-007–AC-008, AC-011; GR: Verification Rules]`

### Налаштування власника

Власник за потреби пройшов Google-вхід із поверненням у тій самій вкладці, отримав окрему чинну `Локальну сесію Власника` й відкрив єдині responsive налаштування на штатному GoDaddy hostname. За готових підтверджених каталогів він побачив чинні та стандартні значення, змінив лише `Codex-агенти`, `Критик` та `Швидкість консиліуму`, перевірив сумісність незалежних моделей і рівнів та атомарно зберіг або повернув валідний повний набір. Точні підтверджені версії й порядок Claude збережені; секрети Google, локальної сесії та AI OAuth не з'явилися в Settings. Власна MFA застосунку, резервний пароль, Fast Mode/PAYG або послаблення safeguards не додані. Власник отримав правдиве підтвердження «лише для наступної сесії» та повернувся до Element; активна консультація не змінилася. Окремий успіх доступу не залежить від готовності каталогів: за їх відсутності вхід можливий, але неперевірений набір не зберігається й нова консультація не запускається. Вихід/відкликання припиняють локальний доступ, не видаються за Google logout і не змінюють AI-підписок. `[PRD: UC-003, UC-004, US-024–US-029, AC-012–AC-016; GR: Verification Rules, Source Access Failures]`

Практичний критерій цінності в усіх описаних гілках — зміна рішення або дії Власника, а не кількість тексту, агентів чи фреймворків. `[PI: «Результат для користувача», «Принципи продукту»; PC: §2]`

## User Story Traceability

| Story | Journey coverage |
|---|---|
| `US-001` | Starting Context; stages 1, 4, 6A; Success State |
| `US-002` | Starting Context; stage 1 access/recovery/revocation; Failure Path 1–3 |
| `US-003` | Stage 3 supported text/image/PDF path |
| `US-004` | Stages 2–3 consent, minimization and sensitive-document branch |
| `US-005` | Stage 5 routing; stage 6A direct response |
| `US-006` | Stages 5 and 6B: one Codex OAuth state, separate real sessions/threads |
| `US-007` | Stages 5 and 6B: separate designated Claude/Codex critic through the selected subscription route |
| `US-008` | Stage 6B actual addressed A2A discussion |
| `US-009` | Stage 6B full verbatim role + `HH:MM` messages without technical noise |
| `US-010` | Stages 4, 6B and 8: 5/30/60-second and progress paths |
| `US-011` | Stage 7 clarification within the one active session |
| `US-012` | Stage 7 `Стоп`; Failure Path 18 |
| `US-013` | Stage 7 `Нова задача`; Exit Points |
| `US-014` | Stage 9 single synthesized recommendation |
| `US-015` | Stage 9 short final sequence with at most three actions |
| `US-016` | Stage 9 separate self-contained technical part when relevant |
| `US-017` | Stage 8 evidence boundary; stage 9 risks/assumptions |
| `US-018` | Stages 5 and 8 fail-closed auth/quota/agent/evidence paths; Failure Path 8–16 |
| `US-019` | Stage 10 encrypted immutable archive |
| `US-020` | Stage 10 full export and confirmed whole-session deletion |
| `US-021` | Stage 7 corrected `Витрати` semantics and cost uncertainty |
| `US-022` | All working stages stay in the same private E2EE Matrix room in Element |
| `US-023` | Stages 7–9 explicit permission for external/high-risk action and >10-minute continuation |
| `US-024` | Settings S1, S5: Google-вхід у тій самій вкладці, скасування/повторна спроба, окрема локальна сесія, вихід/строки/відкликання; повернення до Element; `UC-003`, `AC-012` |
| `US-025` | Settings S2–S3: незалежні дозволені моделі Codex і Claude Code, точні підтверджені версії та порядок Claude |
| `US-026` | Settings S3: незалежні рівні конкретної моделі кожного провайдера, `За замовчуванням моделі` без явного effort, без спільної шкали, перенесення чи silent downgrade |
| `US-027` | Settings S3: `швидко / збалансовано / ретельно` orchestration only, never Fast Mode/PAYG or weaker safeguards |
| `US-028` | Settings S2–S4: effective/default values, cancel, validated atomic save and return to standard values |
| `US-029` | Settings S4–S5: incompatible set blocks only a new session; active session snapshot remains unchanged |

## Confirmed Facts And Constraints

- V1 має одного `Власника`, одну `Дозволену Matrix-пару`, одну `Приватну Matrix-кімнату` й не більше однієї `Активної сесії`.
- Єдиний користувацький канал — штатний Element на Mac, iPhone, Samsung Flip7/Android і Windows PC; окремого browser-chat, live-preview або повсякденної панелі немає.
- Кімната `matrix.org` invite-only з E2EE; у ній рівно один Matrix-акаунт Власника й один Matrix-акаунт бота, без інших pending invites, public address/listing, guests, bridges чи widgets, з history visibility `joined`. Exact allowlist — `room_id` + `owner_mxid`; пристрої перевіряються, новий відновлюється за recovery key, а втрачений або скомпрометований відкликається.
- Matrix E2EE завершується на verified Matrix device бота в GoDaddy Node.js runtime; `matrix.org` бачить метадані, а розшифрований зміст передається OpenAI та, лише для Claude-маршруту, Anthropic через TLS. Production використовує наявний GoDaddy Node.js application і MySQL на штатному hostname, не Cloudflare Worker/DO/R2 і не перенесення домену; платний Matrix-hosting, власний Matrix homeserver та локальний Mac не використовуються.
- Підтримувані входи — текст, зображення та PDF.
- Перше використання потребує одноразової `Згоди на обробку даних`; `Особливо чутливий документ` потребує окремого підтвердження; `Секрет` не приймається й не журналюється.
- Головний консультант обирає між `Прямою відповіддю` і `Консиліумом`. Повний консиліум — це 2–5 окремих Codex-агентів-спеціалістів та окремий Критик із фактичними адресованими повідомленнями через A2A.
- Єдиний дозволений AI auth mode — subscription OAuth платних підписок одного Власника: один захищений Codex OAuth-стан зі штатним refresh для Головного консультанта й 2–5 спеціалістів, які все одно працюють в окремих реальних Codex-сесіях/тредах; окремий Claude Code OAuth token від `claude setup-token` — лише для Claude-маршруту.
- До кожного модельного виклику та запуску залежного агента система перевіряє auth mode і квоту. Expiry, revocation, refresh failure, invalid setup-token, quota exhaustion, заборонений API/PAYG credential або непідтверджена private single-owner eligibility зупиняють залежну роботу без fallback.
- Reauth виконує Власник лише поза Matrix через захищений provider-managed flow; OAuth/setup-token/`auth.json`/refresh state/reauth URL або code не потрапляють до кімнати, A2A, промптів, журналу, архіву чи експорту.
- V1 є приватною не-SaaS системою одного Власника. Його підписки не використовуються для клієнтів, працівників або інших третіх осіб.
- Головний консультант є єдиним реєстратором підтверджених агентських повідомлень і єдиним відповідальним за фінальну рекомендацію.
- Candidate B «Дослівний консиліум» показує в Element усі фактично надіслані агентські доручення, проміжні репліки, критику й виправлення наживо, повністю та без пакетування до фіналу. Один Matrix-акаунт бота показує всередині кожного повідомлення конкретну роль і час `HH:MM`, але не технічні ID, номери, секунди, приховані міркування, журнали інструментів або сирий Markdown.
- Часові межі — 5 секунд / 30 секунд / 60 секунд / 10 хвилин; продовження понад 10 хвилин потребує окремого дозволу.
- Точні команди керування — `Стоп`, `Нова задача`, `Витрати`.
- `Витрати` показують налаштовані місячні платежі AI-підписок, фактичні інфраструктурні витрати та доступний provider-reported usage/ліміт/reset. Included subscription usage не отримує вигаданої per-session token-cost; недоступне позначається як `невідомо`.
- Завершена сесія зберігається зашифровано й безстроково, не допускає редагування окремих реплік, повністю експортується або остаточно видаляється цілою після підтвердження.
- Агенти аналізують і рекомендують; зовнішні та високоризикові дії потребують окремого явного дозволу Власника.
- Продукт не створює власного звуку й покладається на нативні сповіщення Element та операційної системи.
- Повсякденна консультація лишається в Element. Єдиний web-виняток — рідкісні responsive `Налаштування власника` без чату, архіву, live execution status або дашборду; після зміни Власник повертається до Element.
- Settings access використовує лише Google-вхід дозволеного Власника й окрему захищену локальну сесію. Скасування, збій, недійсна ідентичність/транзакція/сесія або обхід не дають доступу; власної MFA застосунку, резервного пароля, іншого IdP чи реєстрації немає. Єдиний дозволений акаунт підтверджений Власником; точна прив'язка лишається захищеною конфігурацією, не текстом репозиторію.
- Google-сесія, локальна сесія й `Subscription OAuth` незалежні. Локальний вихід/expiry/відкликання припиняють доступ і після рестарту, але не означають виходу з Google; вхід, відмова, вихід і повторний вхід не змінюють AI OAuth. Settings не показують секретів; окремо дозволене захищене runtime-налаштування підписок не перетворюється на побічний ефект цих подій.
- Репозиторій HappyPro, його deployment, credentials і дані не належать journey та не змінюються. Destructive legacy cleanup потребує окремого JIT-дозволу після exact inventory і перевіреного backup/restore path.
- Settings мають рівно `Codex-агенти`, `Критик`, `Швидкість консиліуму`: незалежні підтверджені моделі/міркування, точні назви й версії, Claude у порядку `Opus → Sonnet → Haiku` з новішими версіями сімейства першими, provider-specific validation без спільної шкали чи silent downgrade; `За замовчуванням моделі` не передає явного effort. Пресети `швидко / збалансовано / ретельно` ніколи не вмикають Fast Mode, API/PAYG чи usage credits і не послаблюють mandatory safeguards. Неготові каталоги не блокують сам Google-вхід.
- Save та повернення standard values є all-or-nothing. Incompatible set, capability drift або write failure не змінюють effective values і не запускають нову сесію. Валідна зміна застосовується лише до наступної сесії; активні `Фактичні налаштування сесії` залишаються незмінними.

Підстава цього розділу — чинні PRD, контекст, словник і guardrails, успадковане трасування незмінених етапів та наведене в Source References пряме підтвердження Власника. Визначення `JOB-001`–`JOB-005` і `UC-001`–`UC-006` не змінювалися; непідтверджені механізми реалізації чи живий успіх до journey не додано.

## Open Questions

1. Чи потрібно надалі назвати протагоніста особистим ім'ям? До явного підтвердження використовується роль `Власник`; це не блокує journey.
2. Яка точна policy-класифікація визначає `Особливо чутливий документ`? До затвердження невизначений документ потребує окремого підтвердження. `[GR: Open Questions]`
3. Який один lifecycle-термін отримує попередня сесія після `Нова задача`: `Зупинена сесія` чи окрема `Закрита сесія`? До рішення користувацький текст буквально повідомляє, що поточну сесію закрито, а нову створено. `[CT: Open Vocabulary Questions; GR: Open Questions]`
4. Чи зберігається `Зупинена сесія` після `Стоп` в `Архіві сесій`, і якщо так, яким підтвердженим статусом? Джерела визначають зупинку та архів завершених сесій, але прямо не з'єднують ці стани.
5. Які точні користувацькі формулювання запускають експорт і запит видалення? V1 підтверджує обидві дії та обов'язкове підтвердження видалення, але не встановлює точних команд.

## Маршрутизатор Критика — узгодження 06.09.2026

Джерело: `UC-001/UC-004`, `FR-010/040–046`, чинні guardrails і канонічний `Критик`.

- **S2 → S3:** Власник бачить провайдера Критика й чинні параметри навіть за недоступного Claude. Обирає `Codex`, потім підтверджені `GPT-6 Astra` та `Extra High`; поля головного/спеціалістів не змінюються. Перемикання назад на Claude повертає збережені Claude-уподобання, не виконує їх автоматично й показує власну причину недоступності.
- **S3 → S4 → S5:** валідний вибраний маршрут зберігається атомарно; cancel повертає початковий draft, reset показує точний default до запису. Неактивний Claude не робить Codex-набір невалідним. Власник бачить підтвердження «для наступної сесії»; активна сесія лишається незмінною.
- **Stage 5 → 6B:** перевіряються Codex-агенти та вибраний Критик; Codex-маршрут не звертається до Claude. Публікуються ролі окремих агентів, потім фактичні позиції, критика й синтез. Провайдер не замінює роль і не є доказом незалежності висновків.
- **Помилка:** недоступні вибраний provider/model/effort → попередні уподобання збережені → зрозуміла причина й явний вибір доступного маршруту або позачатове відновлення підписки → нова перевірка → підтверджене збереження/новий запуск. Немає автоматичної заміни, втрати Google-сесії чи запиту оплатити Claude для Codex-маршруту.

Це уточнення наявних етапів/сценаріїв; нових поверхонь, JOB/UC або повсякденного runtime-дашборду не додано.
