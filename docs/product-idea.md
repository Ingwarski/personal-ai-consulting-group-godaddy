# Product Idea: Personal Consultant

## Позиціонування

`Personal Consultant` — приватний персональний бізнес-консультант і коуч у захищеній Element/Matrix-кімнаті. Він перетворює описану користувачем проблему на практичне рішення за допомогою головного консультанта, ситуаційно підібраних Codex-агентів-спеціалістів і окремого Claude Code-агента-критика.

Продукт має давати відчуття справжнього ділового консиліуму: агенти не готують ізольовані монологи, а досліджують проблему, адресовано обмінюються аргументами, критикують пропозиції та покращують їх до спільного висновку. Повсякденна консультація відбувається наживо у звичному месенджері без технічного шуму й окремого браузерного чату. Єдиний веб-виняток — вузька поверхня `Налаштування власника`, яка не є чатом, дашбордом або архівом.

Це персональна система для одного власника, а не публічний чатбот або багатокористувацький SaaS.

## Основний користувач і проблема

Основний і єдиний користувач V1 — власник системи. Він працює з Mac, iPhone, Samsung Flip7 і Windows PC та хоче щодня звертатися до консультанта через один доступний на всіх цих пристроях канал — Element із протоколом Matrix. Коли потрібно змінити параметри майбутніх консультацій, він відкриває адаптивну веб-поверхню `Налаштування власника`.

Поточний спосіб роботи не задовольняє користувача, тому що:

- міжагентна взаємодія виглядає як приховані доручення, а не як реальний консиліум;
- нові репліки не завжди з'являються наживо в одному постійному чаті;
- технічні журнали й сирий Markdown заважають читати зміст;
- статичний браузерний preview потрібно перезапускати або відкривати заново;
- процес надто повільний і не дає зрозумілого видимого поступу;
- довгі неструктуровані висновки перевантажують користувача замість того, щоб вести до дії.

## Результат для користувача

Користувач описує бізнесову, фінансову, управлінську, підприємницьку, продуктову, маркетингову, продажну або іншу практичну проблему в приватній Element/Matrix-кімнаті й у тій самій кімнаті отримує:

1. швидке підтвердження, що запит прийнято;
2. видимий склад консиліуму, підібраний під конкретне завдання;
3. повні дослівні репліки агентів із конкретними ролями та часом;
4. реальну дискусію, критику й покращення пропозицій;
5. один синтезований висновок головного консультанта;
6. до трьох конкретних наступних дій;
7. окрему технічну частину, коли вона потрібна для подальшого виконання в Codex.

Цінність продукту вимірюється практичною зміною рішення або дії користувача, а не кількістю тексту, агентів чи застосованих фреймворків.

## Основний сценарій

1. Користувач надсилає проблему текстом, зображенням або PDF в одну приватну Matrix-кімнату з наскрізним шифруванням у клієнті Element.
2. Система перевіряє точну дозволену пару `room_id` і `owner_mxid`, підтверджує отримання й створює одну активну сесію. Інші Matrix-акаунти або кімнати не можуть запускати агентів чи отримувати дані.
3. Головний консультант визначає, чи достатньо прямої відповіді, чи потрібен консиліум. Просте фактичне питання не запускає зайвих агентів.
4. Для консиліуму головний консультант ситуаційно добирає 2–5 Codex-агентів-спеціалістів. Claude Code-агент-критик перевіряє ключову пропозицію, її докази, ризики й умови провалу.
5. Агенти проводять необхідне дослідження, використовують доречні навички та першоджерела й обмінюються адресованими повідомленнями через протокол A2A.
6. Кожне фактично надіслане агентом доручення, проміжна репліка, критика або виправлення реєструється та одразу відображається в Element повністю, без редагування, переказу, приховування частини тексту або накопичення до фіналу.
7. Нові повідомлення користувача під час роботи додаються до активної сесії як уточнення.
8. Головний консультант завершує дискусію, коли подальші репліки вже не змінюють рішення, і формує одну цілісну рекомендацію.
9. Повна сесія зберігається в зашифрованому незмінному архіві.

## Досвід живого чату

- На початку кожної сесії показуються дата й час її старту.
- Над кожною реплікою агента показуються лише конкретна роль і час у форматі `HH:MM`.
- Технічні ідентифікатори, номери повідомлень, секунди, системні інструкції, внутрішні міркування моделей і журнали інструментів у чат не потрапляють.
- Фактичне повідомлення агента не скорочується і не «очищається». Виправлення можливе лише новою реплікою.
- У Matrix-кімнаті присутні лише Matrix-акаунт користувача та один Matrix-акаунт бота. Ролі окремих агентів і час `HH:MM` показуються всередині видимих повідомлень бота; окремі Matrix-акаунти для кожного агента не створюються.
- Приховані міркування моделей, системні промпти та журнали інструментів не є агентськими повідомленнями й не публікуються. Водночас жодну фактично надіслану агентами ділову репліку не можна приховати під цим винятком.
- Розмітка перетворюється на читабельне нативне форматування Element/Matrix, а не показується як Markdown-код.
- Продукт не створює власного звуку повідомлень. Сповіщення й звук, якщо користувач їх увімкнув, забезпечують штатні налаштування Element та операційної системи.
- Одночасно працює одна сесія. Команда `Стоп` припиняє її. Команда `Нова задача` закриває поточну сесію й запускає нову.
- Команда `Витрати` не вигадує ціну токенів для роботи, що входить у платні підписки. Вона показує налаштовані щомісячні платежі за підписки, фактичні інфраструктурні витрати та доступний статус використання, ліміту або часу його відновлення, якщо відповідний провайдер ці дані надає.

## Налаштування власника

`Налаштування власника` — єдина веб-поверхня V1. Вона потрібна лише для зміни параметрів майбутніх консультацій, адаптується до мобільних екранів і не дублює чат, живий консиліум, архів сесій, статус виконання або інший дашборд. Щоденний консультаційний UX залишається в Element/Matrix.

Доступ до цієї поверхні має лише Власник після входу через Google-акаунт із точною дозволеною email-адресою. Не підтримуються пароль, одноразовий код входу, magic link, інший провайдер ідентичності або публічна реєстрація. Продуктова межа доступу — Cloudflare Access із Google IdP, політикою для точної email-адреси та перевіркою JWT на origin; точну реалізацію визначає архітектура.

Google-вхід захищає лише `Налаштування власника`. Він не замінює, не об'єднує й не передає subscription OAuth-дані, через які Codex і Claude Code працюють у межах платних підписок Власника.

У V1 є рівно три групи налаштувань:

1. **Моделі.** Окремий вибір моделі для Codex-агентів і моделі для Claude Code-критика лише з типізованих списків дозволених значень. Довільного текстового введення назви моделі немає.
2. **Глибина міркування.** Єдина зрозуміла семантика `low / medium / high / xhigh` для обох provider runtimes. Перед запуском система перевіряє, чи підтримують обрані моделі потрібний рівень, і застосовує валідне provider-specific відображення без мовчазного зниження рівня.
3. **Швидкість.** Пресети `швидко / збалансовано / ретельно` керують оркестрацією: складом і паралельністю консиліуму, глибиною перевірок та очікуваним темпом. Це не платний Claude Fast Mode, не перемикання на API/PAYG і не придбання usage credits. Claude Fast Mode у V1 вимкнений, оскільки для підписних планів він потребує extra usage.

Будь-яке несумісне поєднання моделі, глибини міркування й пресету швидкості блокується до старту нової сесії з конкретним поясненням. Уже активна сесія продовжується з незмінним snapshot фактично застосованих налаштувань, навіть якщо Власник зберіг нові значення для наступних сесій.

З DAS Forge 4 перевикористовуються лише перевірені патерни: типізовані allowlists, атомарне збереження всього валідного набору, перевірка до запису, стандартні значення, скидання та явний показ фактично застосованих значень. Не переносяться його сім груп налаштувань, sound/motion, довільний ввід моделі або перевизначення параметрів для окремих агентів чи сесійних одиниць.

Жодне налаштування не може вимкнути або послабити обов'язкового Claude Code-критика, реальну A2A-взаємодію, Matrix E2EE, дослівну видимість фактично надісланих агентських реплік, правила безпеки, приватності чи зовнішніх дозволів.

## Фінальна рекомендація

Фінальний результат надсилається не одним довгим полотном, а послідовністю коротких самодостатніх повідомлень:

1. головний висновок або рішення;
2. до трьох конкретних наступних дій;
3. критичні ризики, припущення й умова перегляду;
4. окрема технічна частина, лише коли вона доречна, у вигляді цілісного блоку, який можна скопіювати в Codex без додаткового складання.

Головний консультант синтезує суперечності й не перекладає на користувача вибір між сирими висновками агентів.

## Межі V1

### Входить у V1

- одна приватна Matrix-кімната лише за запрошенням із обов'язковим наскрізним шифруванням;
- один Matrix-акаунт користувача, один Matrix-акаунт бота та allowlist точної пари `room_id` і `owner_mxid`;
- робота з Mac, iPhone, Samsung Flip7 і Windows PC через штатні клієнти Element;
- перевірка Matrix-пристроїв користувача й бота, відновлення доступу за recovery key та можливість відкликати втрачений або скомпрометований пристрій;
- текстові повідомлення, зображення та PDF;
- прямі відповіді на прості запити та ситуаційний консиліум для складних або високоризикових завдань;
- головний консультант, 2–5 Codex-агентів-спеціалістів і Claude Code-агент-критик;
- реальна адресована міжагентна взаємодія через протокол A2A;
- живе дослівне відображення всіх фактично надісланих агентських доручень, проміжних реплік, критики й виправлень без пакетування до фіналу;
- дослідження з перевіркою актуальних першоджерел, коли це потрібно;
- команди `Стоп`, `Нова задача` і `Витрати`;
- зашифрований безстроковий архів, експорт і видалення цілої сесії за явною командою з підтвердженням;
- прозоре повідомлення про збій, недоступного агента, брак доказів або неможливість скликати справжній консиліум;
- розміщення серверної частини на Cloudflare без залежності production-системи від локального Mac;
- керований hosted Matrix homeserver як транспорт і синхронізація кімнати без власного VPS або власного Matrix homeserver у V1;
- verified Matrix device бота в хмарному runtime як точка завершення E2EE для серверної обробки;
- Codex для головного консультанта та агентів-спеціалістів із входом через ChatGPT OAuth у межах платної підписки власника;
- Claude Code як окремий агент-критик із входом через OAuth платної підписки власника;
- закритий режим роботи в межах лімітів підписок без автоматичного переходу на API key, PAYG або додаткові usage credits;
- одна адаптивна веб-поверхня `Налаштування власника`, відокремлена від повсякденного Element/Matrix UX і доступна лише через Google login із точною allowlisted email-адресою Власника;
- рівно три групи параметрів: окремі дозволені моделі Codex і Claude Code, спільна семантика глибини `low / medium / high / xhigh` та оркестраційна швидкість `швидко / збалансовано / ретельно`;
- атомарне збереження лише валідного набору, стандартні значення, скидання, показ фактичних значень, fail-closed для несумісного поєднання та незмінний effective snapshot активної сесії.

### Не входить у V1

- публічний доступ, реєстрація інших користувачів або багатокористувацька модель;
- використання підписок власника для обслуговування клієнтів, працівників або будь-яких третіх осіб, перепродаж доступу чи робота продукту як SaaS;
- окремий браузерний чат, live-preview, архів або дашборд для повсякденної консультації; дозволена лише вузька поверхня `Налаштування власника`;
- пароль, OTP, magic link, інший IdP, публічна реєстрація або доступ до `Налаштувань власника` з будь-якої Google email-адреси поза точним allowlist;
- довільний текстовий ввід моделей, сім груп налаштувань DAS Forge 4, sound/motion-параметри або перевизначення налаштувань для окремого агента чи сесійної одиниці;
- WhatsApp, Meta WhatsApp Cloud API, Telegram, Slack, iMessage або сторонній конструктор чатботів;
- власний VPS, самостійне адміністрування Matrix homeserver або локальний Mac як production-сервер;
- окремі Matrix-акаунти для кожної ролі агента;
- власний звук повідомлень поверх нативних сповіщень Element;
- голосові повідомлення, аудіо, відео та інші типи вкладень;
- кілька паралельних консиліумів в одній Matrix-кімнаті;
- жорсткі ліміти вартості сесії або місяця;
- API-key/PAYG-білінг OpenAI чи Anthropic, автоматичне придбання usage credits або непомітний fallback на `OPENAI_API_KEY` чи `ANTHROPIC_API_KEY`;
- Claude Fast Mode або інший платний режим швидкості, що потребує extra usage;
- автоматичні зовнішні дії від імені користувача;
- публікації, покупки, надсилання повідомлень третім особам, зміни зовнішніх систем або репозиторіїв без окремого явного дозволу;
- показ прихованих внутрішніх міркувань моделей, системних промптів або технічних логів як частини агентського чату;
- приховування, редагування або відкладена пакетна публікація фактично надісланих між агентами ділових реплік.

## Правила даних і доступу

- Сервер приймає робочі повідомлення лише з allowlist-пари `room_id` і `owner_mxid` після перевірки Matrix-пристроїв. Інші акаунти й кімнати не можуть запускати агентів або отримувати дані.
- Веб-поверхню `Налаштування власника` захищає окремий Google-вхід через Cloudflare Access: доступ надається лише точній дозволеній Google email-адресі Власника, а origin приймає запит лише після перевірки Access JWT. Google-вхід не дає доступу до subscription OAuth-секретів Codex або Claude Code.
- Під час першого запуску користувач один раз підтверджує обробку звичайних бізнес-даних через оператора hosted Matrix homeserver, Cloudflare, OpenAI та Anthropic.
- E2EE захищає зміст на шляху між перевіреними Element/Matrix-пристроями. Hosted Matrix homeserver отримує зашифрований зміст повідомлень, але бачить службові метадані, зокрема учасників, час, розміри подій та мережеві дані.
- Наскрізне шифрування завершується на перевіреному Matrix-пристрої бота в Cloudflare runtime. Для виконання консультації розшифрований зміст існує в пам'яті цього runtime та передається OpenAI й Anthropic через TLS; ці провайдери обробляють надісланий їм зміст. Продукт не створює хибного враження, що Matrix E2EE поширюється на AI-провайдерів.
- Кожен агент отримує лише ту частину контексту, яка потрібна для його завдання.
- Непотрібні телефони, адреси, реквізити й інші ідентифікатори маскуються перед передаванням агентам.
- Паролі, API-ключі, повні платіжні картки, повні банківські реквізити, державні ідентифікатори та інші секрети система не приймає й не включає до незмінного журналу.
- OAuth-дані Codex і Claude Code є високочутливими секретами. Вони зберігаються лише в захищеному сховищі довіреного runtime, не передаються в Matrix, агентські промпти, журнали чи архів сесій і можуть бути відкликані власником.
- Runtime працює з єдиним захищеним OAuth-станом власника. Для Codex використовується керований самим Codex вхід ChatGPT через браузер або device-code та його штатне оновлення збереженого OAuth-стану; file-backed `auth.json`, якщо він використовується, не копіюється між незалежними runtime. Для автоматизованого Claude Code використовується subscription OAuth token, створений командою `claude setup-token` і переданий як `CLAUDE_CODE_OAUTH_TOKEN`.
- У runtime не встановлюються `OPENAI_API_KEY` і `ANTHROPIC_API_KEY`; також заборонені інші API/PAYG-облікові дані, які можуть отримати вищий пріоритет за OAuth підписки. Перед запуском система перевіряє активний спосіб автентифікації й припиняє роботу, якщо підтвердити subscription OAuth неможливо.
- Перед обробкою особливо чутливого документа система просить окреме підтвердження.
- У завершеній сесії не можна редагувати або видаляти окремі повідомлення. Повну сесію можна експортувати або остаточно видалити лише за явною командою користувача з підтвердженням.
- Агенти працюють у режимі аналізу й рекомендацій. Будь-яка зовнішня дія потребує окремого дозволу користувача.

## Модель використання підписок і витрат

- Це приватна автоматизація для звичайного особистого використання одним власником. Власник сам ініціює запити й є єдиним одержувачем результатів; продукт не надає третім особам доступ до його облікових записів, підписок або агентів.
- Codex використовує включене в підписку ChatGPT агентне використання через Codex-managed OAuth. Claude Code використовує включене в підписку Claude використання або окрему квоту Agent SDK/автоматизації, якщо саме так її показує поточний план власника.
- Вичерпання квоти не дає дозволу перейти на PAYG. Система чесно повідомляє про ліміт і час відновлення, якщо він відомий, та зупиняє залежну роботу до відновлення квоти або нового явного рішення власника.
- `Витрати` показують окремо: налаштовану щомісячну плату за ChatGPT/Codex; налаштовану щомісячну плату за Claude; фактичні рахунки Cloudflare, hosted Matrix та інших інфраструктурних сервісів; статус квот Codex і Claude, якщо він доступний. Неналаштована або недоступна сума позначається як невідома, а не оцінюється.
- Для конкретної сесії subscription AI usage позначається як «входить у підписку» разом зі статусом квоти. Вартість токенів за сесію не розраховується й не приписується, якщо провайдер її фактично не нарахував.
- Якщо правила провайдера перестають дозволяти такий приватний сценарій або продукт розширюється за межі одного власника, запуск блокується до окремого перегляду моделі автентифікації, ліцензування й білінгу.

## Стандарт швидкості

- підтвердження отримання запиту — не пізніше ніж через 5 секунд;
- перша видима репліка агента — не пізніше ніж через 30 секунд;
- під час активної роботи — не більше 60 секунд без видимого змістовного оновлення;
- фінальна відповідь стандартного консиліуму — не пізніше ніж через 10 хвилин;
- якщо потрібен довший аналіз, головний консультант до спливу десяти хвилин пояснює причину й просить дозвіл продовжити;
- якщо агент не демонструє видимого поступу, головний консультант звужує його завдання або замінює агента, не приховуючи цього від користувача.

## Критерій готовності V1

V1 вважається готовою, коли один реальний запит із дозволеної Matrix-кімнати від єдиного власника проходить повний наскрізний сценарій:

1. Приватна E2EE Matrix-кімната в Element приймає текст, зображення або PDF на Mac, iPhone, Samsung Flip7 і Windows PC.
2. Інша Matrix-кімната або акаунт поза allowlist-парою `room_id` і `owner_mxid` не може отримати доступ або запустити консиліум.
3. Дотримано стандарту швидкості `5 секунд / 30 секунд / 60 секунд / 10 хвилин`.
4. Головний консультант ситуаційно запускає потрібних Codex-агентів і Claude Code-критика.
5. Агенти фактично обмінюються адресованими повідомленнями, а не імітуються одним монологом.
6. Кожне фактично надіслане агентське доручення, проміжна репліка, критика або виправлення одразу з'являється повністю з роллю та часом і зберігається без редагування; публікація не відкладається до фіналу.
7. Працюють команди `Стоп`, `Нова задача` й `Витрати`; остання не показує вигаданої token-cost для підписок, а відокремлює щомісячні платежі, фактичну інфраструктуру та доступний статус квот.
8. Фінальна рекомендація надходить у погодженій короткій структурі, а технічний блок відокремлений, коли він потрібен.
9. Сесія доступна в зашифрованому архіві, експортується й видаляється лише цілою після підтвердження.
10. Збій будь-якого агента або неможливість виконати частину роботи повідомляються прямо й не маскуються вигаданою взаємодією.
11. `Налаштування власника` відкриваються після Google login лише для точної дозволеної email-адреси; інша адреса, інший спосіб входу або запит без валідного Access JWT доступу не отримує.
12. Поверхня однаково придатна для desktop і mobile, містить рівно три групи, не дублює чат/дашборд/архів і не пропонує довільних моделей, sound/motion чи per-unit overrides.
13. Перед новою сесією підтримка обраної моделі, глибини та швидкості перевіряється; несумісний набір блокує запуск, а активна сесія зберігає незмінний effective snapshot.
14. Claude Fast Mode, API/PAYG і usage-credit fallback не активуються жодним пресетом швидкості, а налаштування не можуть послабити critic/A2A/E2EE/дослівність/безпекові правила.

## Принципи продукту

- Практичний результат важливіший за демонстрацію фреймворків або кількість тексту.
- Консиліум означає кількох реально запущених агентів, які обмінюються повідомленнями.
- Головний консультант є єдиним відповідальним за підсумкову рекомендацію.
- Не більше трьох активних пріоритетів у фінальному плані.
- Факти, дані користувача, припущення та професійні судження відокремлюються одне від одного.
- Поточні, нестабільні й високоризикові твердження перевіряються за першоджерелами.
- Жодної псевдовпевненості, вигаданих агентів, джерел, даних або результатів.
- E2EE Matrix не скасовує мінімізацію даних, контроль доступу, прозорість метаданих і довіру до хмарного runtime та AI-провайдерів.
- Налаштовуваність не має вищого пріоритету за критика, реальну агентську взаємодію, дослівність, конфіденційність і fail-closed безпеку.

## Підтверджені рішення

- Повсякденний консультаційний канал у V1 — Element/Matrix, а не браузер, WhatsApp, Telegram, Slack або iMessage.
- Єдиний веб-виняток — адаптивна поверхня `Налаштування власника`; вона не є чатом, дашбордом або архівом.
- Доступ до `Налаштувань власника` можливий лише через Google login для точної allowlisted email-адреси Власника. Пароль, OTP, magic link, інший IdP і публічна реєстрація не підтримуються. Google auth для Settings відокремлений від subscription OAuth Codex і Claude Code.
- У Settings є рівно три групи: дозволені моделі Codex/Claude Code, семантична глибина `low / medium / high / xhigh` і оркестраційна швидкість `швидко / збалансовано / ретельно`.
- Несумісний набір налаштувань fail-closed блокує старт нової сесії; активна сесія зберігає immutable effective snapshot. Claude Fast Mode, API/PAYG і usage credits не є режимами швидкості V1.
- Користувач уже обрав змістовий і UX-напрям Candidate B — «Дослівний консиліум»: усе повне фактичне спілкування агентів видно наживо.
- V1 використовує одну приватну invite-only Matrix-кімнату з E2EE, один Matrix-акаунт користувача й один Matrix-акаунт бота.
- Доступ обмежується точною allowlist-парою `room_id` і `owner_mxid`; пристрої користувача й бота мають бути перевірені, а recovery та відкликання пристрою — передбачені.
- Використовується hosted Matrix homeserver; власний VPS або власний homeserver у V1 не потрібні.
- Серверна частина має розміщуватися на Cloudflare.
- Продукт є приватним і доступним лише з дозволеної Matrix-кімнати для одного власника.
- Codex використовується для головного консультанта й агентів-спеціалістів; Claude Code — для окремого агента-критика.
- Codex і Claude Code автентифікуються через OAuth платних підписок єдиного власника. Це приватне не-SaaS використання: підписки не обслуговують третіх осіб.
- Runtime не має fallback на `OPENAI_API_KEY`, `ANTHROPIC_API_KEY`, API PAYG або автоматичні usage credits. Якщо subscription OAuth чи квота недоступні, відповідний агент зупиняється з чесним повідомленням.
- Агенти мають реально спілкуватися між собою; протокол A2A є частиною задуму.
- V1 підтримує текст, зображення й PDF.
- Одночасно працює одна активна сесія.
- Швидкість має відповідати погодженому стандарту `5 / 30 / 60 секунд / 10 хвилин`.
- Архів зберігається безстроково й зашифровано; зміст завершеної сесії незмінний.
- Застосовується одноразова згода на звичайну обробку даних, мінімізація контексту й окреме підтвердження для чутливих документів.
- У V1 команда `Витрати` відстежує налаштовані місячні платежі за AI-підписки, фактичні інфраструктурні витрати та доступний статус квот. Вона не створює фіктивну вартість токенів або сесії для використання, що входить у підписку; жорсткі ліміти відкладаються до появи фактичних даних.
- Фінальна рекомендація надсилається короткими повідомленнями; технічна частина відокремлюється.
- Продукт не додає власних звуків; використовуються лише нативні сповіщення Element.

## Припущення та відкладені рішення

- Точний постачальник hosted Matrix homeserver, версія A2A, захищене сховище OAuth-стану, сховище даних, керування ключами, черги, повторні спроби й інші архітектурні рішення визначаються на наступних етапах SDD. Спосіб автентифікації AI-runtime вже зафіксований: лише subscription OAuth без API-key/PAYG fallback.
- Архітектура визначає точну реалізацію Cloudflare Access Google IdP, exact-email policy, origin JWT validation, схему атомарного збереження, дозволені значення моделей, capability map, provider-specific відображення рівнів міркування та стандартні значення трьох груп. Ці рішення не можуть розширити підтверджену продуктову межу.
- Точні грошові ліміти відкладаються до отримання фактичної статистики через команду `Витрати`.
- Підтримка голосових повідомлень, відео, додаткових типів файлів і паралельних сесій може розглядатися після перевірки V1.
- Монетизація та доступ інших користувачів не є частиною поточного продуктового задуму.

## Джерела задуму

- Підтверджені рішення користувача в інтерв'ю `grill-me` від 15.08.2026.
- `AGENTS.md` — роль, професійні межі, дисципліна правди й правила рекомендацій.
- `CONSILIUM.md` — склад агентів, вимоги до реальної дискусії, дослівного журналу й синтезу.
- `WORKING_CONTEXT.md` — підтверджені попередні домовленості про персонального консультанта та консиліум.
- Підтверджене рішення користувача від 15.08.2026 про заміну WhatsApp на Element/Matrix та вибір Candidate B «Дослівний консиліум».
- Підтверджене рішення користувача від 15.08.2026 використовувати Codex і Claude Code через OAuth його платних підписок без API-key/PAYG-білінгу.
- Підтверджене рішення користувача від 16.08.2026 залишити консультаційний UX в Element/Matrix і додати лише Google-захищену веб-поверхню `Налаштування власника` з трьома групами параметрів.
- `/Users/ingwar/Projects/DAS Forge 4` — джерело лише для патернів типізованих allowlists, атомарного збереження, валідації, стандартних значень/скидання та effective values; його ширший settings scope не переноситься.
- [Cloudflare: Google identity provider](https://developers.cloudflare.com/cloudflare-one/integrations/identity-providers/google/) — підключення Google як IdP для Cloudflare Access.
- [Cloudflare: Access policies](https://developers.cloudflare.com/cloudflare-one/access-controls/policies/) — політики доступу й точний email selector.
- [Cloudflare: Validate the Access token](https://developers.cloudflare.com/cloudflare-one/access-controls/applications/http-apps/authorization-cookie/validating-json/) — перевірка підпису, issuer та audience Access JWT на origin.
- [OpenAI: GPT-5.3-Codex](https://developers.openai.com/api/docs/models/gpt-5.3-codex) — офіційно заявлені рівні reasoning effort `low`, `medium`, `high`, `xhigh` для цієї моделі; доступні моделі й capability map мають лишатися типізованими та перевірятися перед запуском.
- [Anthropic: Claude Code model configuration](https://code.claude.com/docs/en/model-config) — модель і доступні моделі Claude Code конфігуруються явно; продукт обмежує вибір власним allowlist.
- [Anthropic: Claude Code fast mode](https://code.claude.com/docs/en/fast-mode) — для підписних планів Fast Mode потребує extra usage, тому V1 його не вмикає й не маскує під пресет швидкості.
- [OpenAI: Using Codex with your ChatGPT plan](https://help.openai.com/en/articles/11369540/) — Codex входить до відповідних планів ChatGPT, а використання має планові ліміти.
- [OpenAI Codex app-server authentication](https://github.com/openai/codex/blob/main/codex-rs/app-server/README.md#auth-endpoints) — Codex підтримує ChatGPT OAuth через браузер або device code, зберігає токени й штатно оновлює їх.
- [Anthropic: Claude Code authentication](https://code.claude.com/docs/en/authentication) — subscription OAuth, пріоритет способів автентифікації та `claude setup-token` для довіреної автоматизації.
- [Anthropic: Log in to your Claude account](https://support.claude.com/en/articles/13189465-log-in-to-your-claude-account) — підписка призначена для звичайного використання нативних застосунків, включно з Claude Code; продукти для інших користувачів мають іншу auth/billing-модель.
- `README.md` — початкове позиціонування системи; браузерний live-preview із нього та попереднє рішення про WhatsApp замінено підтвердженим рішенням про Element/Matrix.
